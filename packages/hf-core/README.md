# @halofire/core

HaloFire Studio core library — catalog schema types, SCAD annotation
parser, and stateless helpers shared across the editor and build
pipeline. No `fs`/`path` imports in schema modules; only the parser
reads files.
