// Auto-generated by scripts/author_from_manifest.py (Phase D.3).
// Source: data/phase_d_manifest.json (sku_intent='potter-psb-pressure-switch').
// Do not hand-edit — regenerate from the manifest.
//
// @part potter_psb_pressure_switch
// @kind device
// @category trim.pressure.switch
// @display-name "Potter PSB-1 Pressure Switch"
// @mfg potter_electric_signal
// @mfg-pn PSB-1
// @listing UL FM
// @price-usd 78.00
// @install-minutes 12
// @crew journeyman
// @param size_in number default=2 label="Size" unit="in"
// @port pipe position=[0,0,0] direction=[-1,0,0] style=grooved size_in=2 role=run_a

size_in = 1.0;

function od_mm(nps) =
    nps ==   1  ? 33.4 :
    nps == 1.25 ? 42.2 :
    nps == 1.5  ? 48.3 :
    nps ==   2  ? 60.3 : 33.4;

od = od_mm(size_in);
box_w = 100;
box_h = 70;
box_d = 45;

union() {
    // Enclosure
    translate([0, box_h/2 + od/2, 0])
        cube([box_w, box_h, box_d], center = true);
    // Pipe saddle stub
    rotate([0, 90, 0])
        cylinder(h = od * 2.2, d = od * 1.1, center = true, $fn = 32);
    // Conduit nipple
    translate([0, box_h + od/2, 0])
        cylinder(h = 20, d = 20, center = true, $fn = 16);
}
