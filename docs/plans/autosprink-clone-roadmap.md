# HaloFire AutoSprink-Clone — Roadmap to Done

**Goal:** A working, deployable internal-alpha AutoSprink/AutoCAD-class sprinkler
CAD + auto-bidder, built on the OpenGeometry kernel, that turns a floor plan
into a 3D-correct building + fire-sprinkler layout and a sourced bid.

This file is the autonomous driver's backlog. Each run: GX10 brain preflight →
pick the **first unchecked** task → TDD → focused verification → scoped commit →
check the box here → brain postflight. Never claim AHJ/PE/AutoSprink/manufacturer
parity without real evidence; those gates stay fail-closed.

Work dir: `C:/Users/dalla/OneDrive/Documents/HaloFire`. Rules: `E:/ClaudeBot/AGENTIC_RULES.md`.
Geometry MUST be built on OpenGeometry (npm `opengeometry`). Keep the 68+ tests green.

## Definition of Done
- [x] Floor plan (SVG/DXF) → building shell + full sprinkler system in 3D via OpenGeometry.
      DONE 2026-05-29 (re-assessed wft17pnu2 — d1 assessor met=true high-confidence + honesty
      meta-auditor confirmed; both prior gaps closed). (a) DXF wired end-to-end (T21):
      server.js runSprinklerPipeline resolves dxf/buildingDxf (floorPlanFromDxf/buildingFromDxf)
      alongside svg/buildingSvg; autosprink.html planSource offers dxf + building-dxf;
      tests/dxf-import-api.test.js passes (3). (b) live viewer renders REAL STL part meshes
      (R3/R6/V3): heads via headPartGeo, components via partGeoCache InstancedMeshes (honest
      marked wireframe only for the 12 unsourced of 34). Structural solids kernel-backed on
      OpenGeometry v2.0.10 (Cuboid/Cylinder). EVIDENCE: full suite 412 green / 35 targeted green;
      LIVE browser (preview :3201, builtin Home Depot) status "rendered 936 heads · 1010 pipe
      segments · heads: generated CAD part (best-effort, not mfg-exact) · 148 components (5 part
      types as meshes, 2 primitive fallback)", __headDebug usingPartMesh:true vertsPerHead:1716
      instances:936, cadModel solids wall 4/slab 2/pipe 1010/head 936/component 148. Best-effort,
      not mfg-exact; parity gate stays blocked.
- [x] Engineering-correct layout: NFPA-13 spacing + schedule pipe sizing + hydraulic check.
      (best-effort; single-path + full network balance + geometric compliance, all wired +
      tested. NOT a sealed PE/Hardy-Cross calc. Verified wqui7b5uv d2.)
- [x] CAD interchange export: DXF **and** STEP/IFC/STL from OpenGeometry. (DXF first-party +
      tested; STEP/IFC/STL via the OpenGeometry WASM kernel, wired but runtime-unverified in
      repo. Verified wqui7b5uv d3.)
- [ ] Full bid scope priced from real pricebooks (approaching real bid totals).
      RESOLVED 2026-05-30 as an ACCEPTED INTERNAL-ALPHA LIMITATION (intentionally left unchecked — the success criterion is
      NOT met; documented honestly, never falsely claimed). After T22→T33 (labor+OH&P model, real-takeoff parse for TWO real
      projects, ESFR class, drops/bracing, PDF ingestion with CTM fix, content-region + full-extent + building-outline
      extraction), the honest finding is firm: PART 1 (priced from real pricebooks) IS done and the labor model is accurate
      (matched the Home Depot real labor to ~$199); PART 2 ("approaching real bid totals") is NOT achievable within the honest
      bounds of this internal alpha. The dollar delta oscillated (Home Depot ESFR −72→−48%; Cooperative built-in −24% via
      offsetting netting; the REAL 1881 drawing −57% [CTM bug] → +215% [bbox over-capture] → +247% [outline method, blocked by
      un-layered annotation geometry]). Closing it to ±25% category-honest requires EITHER (a) PDF graphics-state
      layer/color/lineweight filtering to separate the wall layer from grid/dimension/annotation geometry (a deep capability —
      the real Bluebeam sheet has ~1,765 full-sheet-spanning axis-aligned annotation lines indistinguishable from walls by
      geometry alone), OR (b) per-project takeoff-model calibration — which is FITTING one GC's number and is explicitly
      forbidden by the honesty contract. The auto-bidder is correct + honest (right system class, accurate labor, real
      pricebook materials, real-drawing geometry pipeline, all claim gates fail-closed); it produces a defensible BEST-EFFORT
      estimate, but matching a SPECIFIC real submitted bid to ±25% is not honestly automatable here without fitting. This box
      stays UNCHECKED to reflect that honestly. Any future close MUST come from real layer-aware extraction (path a),
      validated against drawing geometry, never by tuning to the dollar figure. UPDATE 2026-05-30 (T34): path (a) was
      attempted via PDF graphics-state (lineweight + stroke color) layer extraction — it IMPROVED the real-1881 footprint
      (T33 4.6×/+247% → T34 2.76×/+105%) but did NOT close d4: lineweight does not cleanly isolate the wall layer (a
      lineWidth=0 hairline dominates the sheet and the structural-heavy bands are fragmented across groups). The autonomous
      extraction approaches (CTM-correct T31, full-extent T32, geometry-outline T33, graphics-state T34) are now EXHAUSTED;
      remaining honest paths are per-sheet manual building-region selection (operator-supplied, like the scale) or deep
      per-CAD-system layer reverse-engineering — neither automatable here without fitting. d4 remains the accepted documented
      limitation. Historical detail follows:
      PARTIAL — re-assessed 2026-05-29 (wft17pnu2). PART 1 (priced from real pricebooks) IS
      DONE: T22 wired buildFullScopeBid into runSprinklerPipeline; bare materials
      (heads/pipe/fittings/hangers) come from real ingested ARGCO/Victaulic/FFF rows; system
      components + soft costs surface; Home Depot calibration vs the real $792,543.84 bid-log
      total is computed + shown. tests/full-scope-bid-api.test.js passes (3).
      PART 2 ("approaching real bid totals") is NOT met and the box stays unchecked — HONESTY:
      after T23 (field labor + OH&P) the LIVE full-scope total is $291,379.18 vs the real
      $792,543.84 = −63.23% (improved from −71.65%/$224,683 but still ~63% below). A best-effort
      estimate ~63% below the real submitted bid is NOT "approaching" it. T23 added the defensible
      labor + OH&P model (parts 1+2); T24 then parsed the real ESI bid-line takeoff (the evidence trail)
      and proved WHERE the gap is: the T23 model labor ($144,112) ≈ the real labor ($143,473, ~0.4%),
      so the gap is almost ENTIRELY MATERIALS. T25 then added the ESFR/storage system class (NFPA-13
      storage rules + diameter-aware feed/cross/bulk/underground mains, real pricebook medians, untuned),
      improving the auto-estimate −63%→−49.16% ($402,927.70 vs $792,543.84; materials $113,494.78 vs real
      $396,098, still −71%). T26 then executed the last defensible material folds (drops + seismic bracing,
      untuned, in-rack correctly excluded) → −48.08%. PROGRESSION T23→T24→T25→T26: −72% → −63% → −49.16% → −48.08%,
      all via defensible untuned modeling. DECISION (T26, honesty verifier w5bjtsj0v): option (b) is now EXHAUSTED —
      LABOR+EQUIPMENT MATCHES REALITY essentially exactly (model $180,103.50 vs real $179,904.77, +$199), and the
      ENTIRE remaining shortfall is MATERIALS (~$120–137k model vs $396,098 real). That residual is GEOMETRY FIDELITY:
      the built-in Home Depot is a SIMPLIFIED single rectangle, so the large-diameter main-grid FOOTAGE is under-modeled;
      it CANNOT be closed by more honest pricing/BOM work — only by feeding the auto-bidder the REAL building geometry.
      the built-in Home Depot is a SIMPLIFIED single rectangle. UPDATE 2026-05-30 (T27): the user supplied a SECOND real
      project — "The Cooperative 1881 Apartments" (residential NFPA-13R, real total $538,792.35). Run on its REAL inputs
      (170,654 sqft, residential hazard), the untuned auto-estimate fullScopeTotal $407,377.23 = −24.39% — much closer than
      ESFR Home Depot. BUT this is NOT yet a category-honest pass: the −24% lands within range only by NETTING two
      offsetting category errors (materials UNDERSHOOT −50.5% $93,368 vs $188,778; labor OVERSHOOT +33% $203,543 vs $152,695),
      and the labor model did NOT match this project (unlike Home Depot). d4 therefore STAYS OPEN — a netting artifact is not
      genuine "approaching". The dominant residual (materials) traces to the placeholder rectangular footprint (no DXF; PDF
      plans only). HONEST PATH (T28, per the user "must use pdf" directive): ingest the real VECTOR PDF plans
      (floorPlanFromPdf via pdfjs-dist) → geometry-true footprint → re-measure Cooperative 1881; check d4 ONLY when materials
      AND labor BOTH approach real (not via offsetting) WITH the real evidence trail, never by tuning. AUTOSPRINK_PARITY stays
      blocked; calibration informational-only.
- [x] Studio UI usable end-to-end; evidence gates surfaced; nothing faked. (best-effort;
      gates fail-closed + surfaced; 45 studio/gate tests green. Verified wqui7b5uv d5.)
- [x] One-command verifier green; review packet current. (scripts/verify-internal-alpha.ps1
      runs the full suite — 34 files / 320 tests green; packet refreshed to
      docs/reviews/2026-05-29-autosprink-clone-review-current.md. Verified wqui7b5uv d6.)
- [x] Deployable (documented run/build path). (docs/DEPLOY.md local run path verified
      end-to-end; public VPS/HTTPS at halofire.rankempire.io still pending DNS+certbot.
      Verified wqui7b5uv d7.)

## Task Queue (do the first unchecked, one per run)

- [x] **T1 — OpenGeometry STEP/IFC/STL export.** DONE 2026-05-29. Studio builds an
      OGSceneManager from the rendered shapes (addCuboid/addCylinderToCurrentScene)
      and exports via exportCurrentSceneToStep/Ifc/Stl. Buttons for DXF/STEP/IFC/STL.
      Verified in browser: 40×30 room → STEP 79,068 bytes from 23 OpenGeometry
      entities (4 walls + floor + 12 pipes + 6 heads); IFC also non-empty.
- [x] **T2 — Hydraulic calc engine.** DONE 2026-05-29. `src/engine/hydraulics.js`
      (pure/deterministic): Hazen-Williams friction loss (psi/ft), velocity (fps),
      remote-area demand (NFPA 13 density × area — light 0.10/1500, ordinary
      0.18/1500, extra 0.30/2500), single-path requiredPressureAtRiser (riser →
      cross-main → remote branch friction + 0.433 psi/ft elevation + 7 psi min head),
      and flagSchedule velocity(>32 fps)/loss warnings. Hand-computed vitest cases.
      Studio surfacing still pending (engine + tests are the deliverable).
- [x] **T3 — Full bid scope.** DONE 2026-05-29. Add system components (alarm/check valve, FDC,
      backflow, riser trim, inspector's test, fire pump if required) + soft costs
      (permit/design/freight as labelled assumptions) priced via pricebook resolver,
      behind a clear "estimate" label. Tests. Show in bid.
- [x] **T4 — DXF/PDF floor-plan import.** DONE 2026-05-29. `floorPlanFromDxf` in
      floorplan-import.js parses the DXF ENTITIES section: LWPOLYLINE (code 10/20
      vertex pairs), POLYLINE/VERTEX, and best-effort closed-loop assembly from
      LINE segments by endpoint chaining. opts.layer (code 8) filter,
      opts.unitsPerDrawingUnit scale to feet, opts.hazard default ordinary; reuses
      normalizeFloorPlan; skips <3-vertex shapes. PDF import DEFERRED. Tests.
- [x] **T5 — Resolve-gate workflow.** DONE 2026-05-29. Admin-only route
      `POST /api/projects/:name/claim-gates/:code/resolve` in src/api/server.js
      (+ resolved_by/resolved_at/resolved_evidence_ref columns via ensureColumn in
      both server.js initDatabase and seed.js). Requires a real evidence object;
      only ahj_approval/professional_review/pe_signoff/manufacturer_approval/
      autosprink_packet/employee_signoff with status 'present' may clear. Rejects
      (400) best_effort_ai_layout type and best_effort status — AI output can NEVER
      clear a gate. On success inserts the evidence row (present) and flips the gate
      blocked→cleared with who/what/when. Studio UI deferred. Tests prove gate only
      clears with a real evidence row, never from AI output.
- [x] **T6 — Multi-floor / multi-zone.** DONE 2026-05-29. buildCadModel now accepts an
      optional floorPlan.floors array [{level,baseElevationFt,ceilingHeightFt?,rooms}];
      each floor builds its own independent riser->cross-main->branch->drop->head network
      with NFPA schedule sizing, then its solids are offset in Z by baseElevationFt so
      floors stack. counts aggregate across floors; legacy floorPlan.rooms still builds a
      single floor at base 0 unchanged. Walls carry baseZ (default 0) and dxf-export honors
      it. Tests in tests/cad-model-multifloor.test.js. Studio surfacing best-effort/deferred.
- [x] **T7 — OpenClaw CAD automation adapter.** `src/cad/openclaw-cad.js` that
      builds generate_3d_model / generate_dxf payloads from the cad-model and
      invokes the OpenClaw bridge when the GX10 gateway is live (graceful skip when
      unreachable). Tests on payload shape.
- [x] **T8 — Head perf + true-scale toggle.** DONE 2026-05-29. Studio renders the
      900+ heads + drops via Three InstancedMesh (one draw call each) instead of
      per-item OpenGeometry cylinders; CAD-structural geometry (walls/mains/
      branches/riser) stays on the OpenGeometry kernel. Home Depot render ~9s→~4s
      (~59 kernel shapes + 2 instanced meshes vs ~1900). Added "Exaggerate pipe
      radius (×6)" View toggle (off = true NFPA scale); re-renders from the stored
      model. STEP/IFC/STL export rebuilds the COMPLETE kernel scene on-demand
      (buildExportScene from cadModel.solids, true-scale) — verified Home Depot
      STEP = 6,805,478 bytes from 1950 entities, no regression. Browser-verified.
- [x] **T9 — Consolidate surfaces.** DONE 2026-05-29. Retired the 725-line legacy
      static fake-data app.html dashboard — app.html is now a tiny redirect to
      /workbench.html (old link still works, fake data gone). The SPA catch-all in
      server.js now serves index.html (landing) for unknown routes instead of the
      old dashboard, so all UI flows index -> login -> workbench -> studio.
      tests/consolidate-surfaces.test.js (4 spawned-server tests). Full suite
      26 files / 230 tests green.

## ACCURATE BUILDING epic (user priority): real buildings from drawings + full systems
Current gap: import treats each polygon as ONE room and grids its bounding box;
the "building" is just an extruded perimeter wall + flat slab. Real drawings have
interior walls, multiple spaces, doors/windows, and columns; the system must
cover EVERY space with a proper building-wide network. Build that.

- [x] **T11 — Building model + multi-space drawing parser.** DONE 2026-05-29. New
      `src/engine/building-model.js`: a building schema
      `{ name, units, stories:[{ level, baseElevationFt, ceilingHeightFt, spaces:[{name,polygon,hazard}], walls:[{a,b,thicknessFt,type:'exterior'|'interior',openings:[{offsetFt,widthFt,heightFt,sill?,type:'door'|'window'}]}], columns:[{x,y,sizeFt}] }] }`
      + `normalizeBuilding(spec)`. Extend floorplan-import.js with
      `buildingFromDxf(text,opts)` / `buildingFromSvg(text,opts)` that extract
      walls (LINE/LWPOLYLINE on a WALLS layer → segments w/ thickness+type),
      spaces (closed room polygons / labeled regions), doors+windows (DOOR/WINDOW
      layers or SVG attrs → wall openings), and columns (COLUMN layer / circles).
      Layer/attr-convention based, best-effort (NOT CAD recognition AI). Backward
      compat: a single-polygon input still yields one space. Deterministic; tests.
- [x] **T12 — Full per-space sprinkler system.** New `src/engine/system-layout.js`:
      `layoutBuilding(building)` lays out EACH space independently (reuse
      layoutRoom per space polygon, clipped to that space, column-aware: drop/shift
      heads that collide with columns), then builds a building-wide network — a feed
      main, per-space cross-mains/branch lines, drops, heads — so coverage fills all
      spaces. Returns per-space + whole-building head counts, pipe schedule, and a
      `coverageOk` flag (every space has heads). Deterministic; hand-checked tests.
- [x] **T13 — Accurate building CAD model.** DONE 2026-05-29. Extend `buildCadModel` to consume a
      building: emit interior + exterior wall solids (carry opening metadata
      {offsetFt,widthFt,heightFt,sill,type} so the viewer can cut them), column
      solids, per-space pipe networks, multi-story stacking. counts include
      spaces/walls/columns/openings. Backward compat with floorPlan.rooms/floors
      preserved. dxf-export emits walls/columns/openings on layers. Tests.
- [x] **T14 — Studio: render accurate building + full system.** DONE 2026-05-29.
      server.js sprinkler-bid accepts a multi-space building drawing (`buildingSvg`)
      -> buildCadModel(building) (isBuilding:true). Studio: "Building drawing
      (multi-space SVG)" source; renders walls colored by type (interior vs
      exterior), columns (Cuboid), the full per-space system, and a per-space
      breakdown panel; door/window openings cut via OpenGeometry boolean. FIXED a
      real bug: subtract requires an OPERAND ARRAY and returns a NEW cut mesh
      (cutWall now calls wallCuboid.subtract([openings], {}) and renders the
      returned mesh — the prior single-arg call threw + was swallowed -> solid wall).
      VERIFIED: tests/building-studio-api.test.js (2 tests); full suite 27 files /
      232 tests green; live studio render of a 2-space building (status "rendered 10
      heads · 22 pipe segments") with PROGRAMMATIC PROOF the door-cut worked —
      window.__wallDebug shows the interior wall (1 opening) at 84 verts vs the 4
      exterior walls at 36 verts (a hole was cut). Screenshot tool hung this session
      (WebGL rAF capture), so verified by geometry vertex counts instead.
- [x] **T10 — Deploy packaging + review packet refresh.** DONE 2026-05-29.
      New truthful packet docs/reviews/2026-05-29-autosprink-clone-review.md
      (what works w/ file cites, exact test count, explicit "still NOT done /
      NOT claimed" section). New docs/DEPLOY.md (env vars, seed + run path,
      one-command verifier). scripts/verify-internal-alpha.ps1 now runs the FULL
      `npx vitest run` (no file list) so it can't drift from the test count;
      seed + agentic-rules steps kept. Docs/script only — no behavior change.

## COMPONENT CAD LIBRARY + AUTOSPRINK PARITY epic (user priority)
Every fire-sprinkler component must have a CAD-accurate 3D model, sourced:
catalog download -> OpenSCAD-generated -> SAM 3.1-reconstructed -> gated
placeholder. Full AutoSprink parity (no missing modules/components), tracked +
fail-closed until real. Auto-bid = takeoff from the component set. Any doc we
can't auto-source (catalogs, cut sheets, approvals, AutoSprink ref, OpenSCAD
binary) is user-uploaded/linked in Settings, wired to the evidence/gate system.
Grounding: OpenSCAD NOT installed locally (generate .scad source; render needs
the binary = a Settings dependency). SAM 3.1 = GX10 'sam3' via the OpenClaw
bridge (best-effort, graceful skip). Parity claim stays BLOCKED until complete.

- [x] **T15 — Component registry + AutoSprink parity inventory.** `src/components/registry.js`:
      canonical list of every component/module (heads pendent/upright/sidewall/
      concealed/ESFR; pipe sch10/40; fittings tee/elbow/coupling/reducer/cap;
      grooved couplings; valves alarm/check/butterfly/OS&Y/PRV; FDC; backflow;
      riser+trim; gauges; hangers/seismic bracing; drains; inspector test; signs)
      + parity matrix (present vs missing) + fail-closed AUTOSPRINK_PARITY gate
      blocked until inventory complete w/ real evidence. Node-tested.
- [x] **T16 — Component 3D-model resolver pipeline.** `src/components/model-resolver.js`:
      resolve each component's model in priority order catalog-file -> OpenSCAD ->
      SAM3.1 -> placeholder+gate; record per-component source/status. Injected
      sourcers; Node-tested.
- [x] **T17 — OpenSCAD parametric generators.** `src/components/openscad/*`:
      deterministic .scad source per component type (Node string-gen, tested) +
      render adapter (openscad CLI when present, else gated 'openscad_not_installed').
- [x] **T18 — SAM 3.1 reconstruction adapter.** `src/components/sam-reconstruct.js`:
      catalog image -> SAM3.1 segment -> mesh via injected GX10/OpenClaw invoker;
      graceful skip when gateway down. Node-tested payload + skip.
- [x] **T19 — Settings + documentation upload/link API.** DONE 2026-05-29. Authenticated routes to
      upload OR link missing docs (catalogs, cut sheets, approvals, AutoSprink ref,
      OpenSCAD path); each -> project_evidence row / dependency status; missing-doc
      resolver queue. Node-tested (spawned-server pattern).
- [x] **T20 — Settings UI + component-library browser (BROWSER).** `settings.html` ships:
      doc upload/link slots (`/api/settings/documents`, admin add round-trip verified),
      stack dependencies (`/api/settings/dependencies`), the AUTOSPRINK parity matrix
      (`/api/parity` — 12 functional rows present, 3 gated rows shown gated, gate BLOCKED /
      parityAchieved false), and a component-library browser (registry.js
      `buildParityInventory({})` → all 34 components honestly shown "no model", 0 present).
      Linked from workbench + studio headers. Verified via preview_eval/DOM (1500px).
      NOTE (honesty): "render catalog/generated models in the 3D layout" is intentionally
      deferred — zero real models exist yet (all 34 gated/missing), so there is nothing to
      render; that lands when real catalog/generated models are attached and the gate is
      addressed. T20 surfaces parity/inventory but does NOT flip the gate.
- [x] **T21 — Wire DXF import end-to-end (DoD d1 gap).** DONE 2026-05-29. server.js now imports
      floorPlanFromDxf + buildingFromDxf and runSprinklerPipeline accepts `dxf` (single-space) and
      `buildingDxf` (multi-space, layer-mapped via req.body.dxfLayers or DEFAULT_DXF_LAYERS) parallel
      to svg/buildingSvg, with `unitsPerDrawingUnit` scaling; the building→floorPlan synthesis is now
      shared by both SVG and DXF building paths. autosprink.html planSource adds "Import DXF (single
      space)" + "Building drawing (multi-space DXF)" with a DXF-aware label/placeholder; currentBody
      sends dxf/buildingDxf + unitsPerDrawingUnit. TDD: tests/dxf-import-api.test.js (3 spawned-server
      tests — dxf floor plan → 3D model+bid, buildingDxf → 2 spaces/int wall/column, garbage dxf →
      400). Browser-verified (preview_eval/DOM 1500px): building-dxf option → model "2 spaces · 1 int
      walls · 1 cols · 1 openings", 8 heads, OpenGeometry solids 5w/20p/8h, rendered via the part mesh.
      Full suite 38 files/352 tests green (+1 file/+3 tests). Geometry still on the OpenGeometry kernel;
      no parity/AHJ/PE claim. (Screenshot skipped — preview_screenshot hangs on the WebGL rAF canvas
      this session; DOM verification is the established substitute.)
- [x] **T22 — Wire full-scope bid + calibrate (DoD d4 gap).** DONE 2026-05-29 (workflow
      wzdazh7pv, TDD, 3 adversarial verifiers all PASS). server.js imports buildFullScopeBid;
      runSprinklerPipeline now computes fullScopeBid = buildFullScopeBid(bid.pricing, {priceResolver
      (reuses the DB resolver), totalHeadCount, hazard, requiredPressure when hydraulics ran})
      in try/catch → {error} on failure, added to the bundle + /sprinkler-bid res.json
      (back-compat preserved). HOME_DEPOT_BID_LOG_TOTAL=792543.84 constant; for the Home Depot
      project fullScopeBid.calibration = {referenceTotal, fullScopeTotal, deltaUsd, deltaPct, note}
      where note states "informational comparison only — not an accuracy or parity claim, clears no
      gate". Studio bid panel (autosprink.html) now shows bare-materials → system components → soft
      costs → full-scope estimate + the calibration delta. Browser-verified (Home Depot): bare
      $196,017 → system $6,400 + soft $22,266 → full-scope $224,683; calibration "vs $792,544:
      −71.65%, informational only". TDD tests/full-scope-bid-api.test.js (3 spawned-server tests:
      honesty-flagged fullScopeBid + fallback/soft-cost flags, Home Depot calibration referenceTotal
      792543.84 + not-accuracy note, /submittal submittalReady still false). Full suite 39 files/355
      tests green (+1 file/+3). Honesty held — estimate flags + disclaimer intact, no gate flipped,
      no parity/accuracy claim. (Screenshot skipped — preview_screenshot hangs on the WebGL canvas;
      DOM verification is the established substitute.)
- [x] **T23 — Bid-fidelity estimating model (field labor + OH&P).** DONE 2026-05-29 (ultra
      workflow wzf4fjx1l, TDD, honesty + correctness verifiers both PASS). Delivered parts (1)+(2):
      src/engine/bid-scope.js adds LABOR_ASSUMPTIONS (hoursPerHead 0.8 / hoursPerPipeFt 0.05 /
      hoursPerFitting 0.3 / laborRatePerHour 90 — documented public conventions, NOT tuned) +
      buildLaborScope (HR lines, priceSource labor_assumption) and OHP_ASSUMPTIONS (overhead 0.10 +
      profit 0.10) + buildOverheadProfit (overhead on cost, profit on cost+overhead; ohp_assumption).
      buildFullScopeBid now bases the full scope on pricedBid.materialCost (NOT the marked-up total)
      so it does NOT double-count priceBid's crude $85/head + 25% markup; bareMaterialsTotal preserved.
      server.js derives pipeFootage (BOM branch_pipe FT) + fittingCount (BOM fitting) and passes them in;
      autosprink.html surfaces Field labor (est) + Overhead & Profit (est). Suite 45 files/419 tests
      green (+7). EVIDENCE: NEW live Home Depot full-scope $291,379.18 (materialsOnly $66,432.75 +
      components $6,400 + labor $144,112.50/1601.25hr + soft $23,863.99 + OH&P $50,569.94) vs real
      $792,543.84 = −63.23% (was −71.65%). Honesty held — all lines flagged estimate/assumption,
      calibration informational, no gate flipped. NOT YET DONE (→ T24): part (3) subcontract/site-scope
      line items + part (4) calibration to the real ESI bid-line takeoff. d4 stays UNCHECKED — −63%
      is a real improvement but still NOT "approaching" the real total.
- [x] **T24 — Real-takeoff bid calibration.** DONE 2026-05-29 (ultra workflow wv4y26ruh, TDD,
      honesty + correctness verifiers both PASS). Parsed the REAL ESI bid-line takeoff from the Home
      Depot proposal workbook "Building 1" Knowify SOV block (source cells cited): readHomeDepotRealTakeoff
      → cost {material $396,097.57 (N31), labor $143,473.33 (O32), equipment $36,431.44 (Q32),
      subcontractor $5,000 (P30), miscellaneous $2,500 (R30), total $583,502.34}; withMarkup {materials
      $514,926.85 (U31), labor+equip $233,876.19 (U32), design $9,750 (U30), sovTotal $758,553.04 (U34)};
      totalPrice $767,543.84 (G11); bidLogTotal $792,543.84; markupPct 0.30; sourceRefs cite all 10 cells.
      server.js enriches the Home Depot fullScopeBid.calibration with realTakeoff + byCategory (model vs
      real per category, informational), fail-soft (absent workbook → omit, never 500). autosprink.html
      shows a "Real ESI takeoff (source cells cited)" block beside the auto-estimate. TDD
      tests/home-depot-real-takeoff.test.js (3, skip-if-absent) + full-scope-bid-api.test.js (+2). Suite
      46 files/424 tests green (was 45/419). KEY HONEST FINDING: the T23 model labor $144,112 ≈ the real
      labor $143,473 (within ~0.4%) — the dominant gap is MATERIALS ($66k model vs $396k real) from the
      simplified geometry not modeling the ESFR/bulk-main scope. HONESTY: real takeoff is parsed real data
      (NOT a model achievement), gates stay blocked, no accuracy/AHJ/PE/parity claim. DoD d4 stays OPEN —
      the honesty verifier ruled d4ShouldBeChecked=FALSE: T24 added no new PRICING capability; the
      auto-bidder's own output is still −63%; re-reading the real total from the workbook is a
      calibration/evidence milestone, not the "priced-scope-approaching" milestone d4 names. → T25 is the
      real path to d4.
- [x] **T25 — Model the ESFR/storage material scope.** DONE 2026-05-29 (ultra workflow wrwqi2u9l, TDD,
      honesty + correctness verifiers both PASS; honesty verifier confirmed NO tuning/reverse-engineering).
      src/engine/sprinkler-layout.js: new STORAGE_RULES.esfr {maxAreaSqFt 100, maxSpacingFt 12, minSpacingFt 8,
      storage:true} (public NFPA-13 storage values) merged into getHazardRule; buildEsfrSystemScope(layout,
      piping, opts) emits diameter-aware BOM lines DERIVED from geometry — esfr_head (qty=heads, REPLACES
      sprinkler_head so no double-count), cross_main_pipe (3", qty=crossMainFt), feed_main_pipe (6",
      qty=round(max(bbox.w,bbox.h)) one feed main along the long axis), bulk_main_pipe (6", ESFR_SCOPE_ASSUMPTIONS.bulkMainFt
      40, labelled assumption), underground_main (8", undergroundFt 100, labelled assumption); ESFR FALLBACK_UNIT_COSTS.
      src/engine/pricebook-pricing.js: +5 PRICE_BANDS (esfr_head, cross/feed/bulk_main_pipe, underground_main)
      resolving REAL pricebook medians within defensible size-cost windows (monotone by size; esfr_head $42, cross
      $10.92, feed $21.56, bulk $27.02, underground $30.17). server.js: runSprinklerPipeline detects ESFR (Home Depot
      OR systemClass:"esfr"), lays out hazard esfr + appends the scope before pricing; non-ESFR projects byte-for-byte
      unchanged; fail-soft. TDD tests/esfr-scope.test.js (12) + pricebook-pricing.test.js (+3) + full-scope-bid-api.test.js
      (+2). Suite 47 files/441 tests green (was 46/424). Agentic-rules ok. MEASURED (seeded-pricebook, real medians, via
      preview /sprinkler-bid): Home Depot now systemClass esfr, 1230 ESFR heads, materialsOnly $113,494.78 (up from ~$66k),
      laborCost $180,103.50, fullScopeTotal $402,927.70 vs real $792,543.84 = −49.16% (improved from T23's −63.23%).
      HONESTY: all numbers from public NFPA rules / geometry / labelled assumptions / real pricebook medians — NOT tuned
      (a back-fit would land ~0%); gates stay blocked, estimate:true, no accuracy/AHJ/PE/AutoSprink/manufacturer claim.
      DoD d4 stays OPEN — the honesty verifier ruled d4ShouldBeChecked=FALSE: the auto-estimate improved (−63%→−49%) but
      is still materially short (materials −71%, total −49%). → T26. NOTE: the full-scope-bid-api test prices off
      FALLBACK_UNIT_COSTS (test-harness pricebook empty); the seeded path (measured above) uses real medians.
- [x] **T26 — Decide the honest path to DoD d4 (DECISION made with evidence).** DONE 2026-05-29 (ultra workflow
      w5bjtsj0v, TDD, honesty + correctness verifiers both PASS; honesty verifier confirmed NO tuning). Executed
      option (b) — folded the two GENUINELY-OMITTED, geometry/NFPA-derivable material categories into the ESFR scope:
      drop_armover (1 per ceiling head = 1230) + seismic_brace (qty = floor(totalMainFt/40), the public NFPA-13 lateral
      interval from supports.js). IN-RACK CORRECTLY EXCLUDED (real ESFR is ceiling-only; adding it would over-scope —
      test asserts no in-rack line). +2 PRICE_BANDS (real medians; labelled fallbacks when null). TDD: esfr-scope (+5),
      pricebook-pricing (+3), full-scope-bid-api (+1). Suite 47 files/450 tests green (was 47/441). Agentic-rules ok.
      MEASURED (seeded pricebook, via preview /sprinkler-bid): Home Depot materialsOnly $119,862.79 (was $113,494.78),
      fullScopeTotal $411,480.58 vs real $792,543.84 = −48.08% (progression −72%→−63%→−49.16%→−48.08%). DECISION (the
      task's deliverable): option (b) is now HONESTLY EXHAUSTED — the byCategory breakdown shows LABOR+EQUIPMENT NOW
      MATCHES REALITY essentially exactly (model $180,103.50 vs real $179,904.77, +$199), and the ENTIRE remaining
      shortfall is MATERIALS (model ~$120–137k vs real $396,098). That gap is GEOMETRY FIDELITY — under-modeled
      large-diameter main-grid footage from the placeholder single-rectangle floor plan — which cannot be closed by
      adding more honest material categories (T26 added the two real omissions and recovered only ~$6–24k of a ~$259k
      gap). The honest remaining paths are: (a) import a REAL DXF of the actual Home Depot building (engine supports DXF
      via T21) so the takeoff footage is geometry-true → the legitimate way to check d4 — REQUIRES THE USER to supply the
      real drawing; or (c) accept d4 as a documented internal-alpha limitation. RECOMMENDATION: pursue (a) when a real
      drawing is available; document (c) in the interim. NEVER pad/tune to reach the total. Gates stay blocked.
- [x] **T27 — Integrate the real Cooperative 1881 residential bid (source-of-truth).** DONE 2026-05-30 (ultra
      workflow wd0q4lxuw, TDD, honesty + correctness verifiers both PASS; NO engine/pricing edits — existing model on
      real inputs). The user supplied real bid docs for "The Cooperative 1881 Apartments" (1881 W North Temple, SLC UT;
      GC Kier Construction) — a RESIDENTIAL NFPA-13R-style system (residential heads + drops, ZERO ESFR). New
      src/data/cooperative-1881-bid-package.js readCooperative1881RealTakeoff column-sums the Building (1) Knowify SOV
      block (N=material 188,777.67 / O=labor 152,694.67 / P=sub 19,432 / Q=equip 25,525.50 / R=misc 18,677.19; cost.total
      405,107.03 == R38 Knowify Check), totalPrice G11 538,792.35, heads B9 1420, sqft G6 170,654, markup 0.33; sourceRefs
      cite N28:R33/R38/G11/B9/G6/I11; throws if workbook absent. src/data/floorplans.js + server.js add a built-in
      Cooperative 1881 project (REAL area 170,654 sqft, ordinary residential hazard — placeholder rectangular footprint,
      documented) + fail-soft calibration enrichment (realTakeoff + byCategory + delta). TDD tests/cooperative-1881-takeoff.test.js
      (6, skip-if-absent) + full-scope-bid-api (+1). Suite 48 files/457 green. Agentic-rules ok. MEASURED (untuned, real
      inputs): auto-estimate fullScopeTotal $407,377.23 vs real $538,792.35 = −24.39%. HONEST CAVEAT: that total is within
      ±25% only by NETTING two offsetting category errors — materials UNDERSHOOT −50.5% ($93,368 vs $188,778) while
      labor OVERSHOOTS +33% ($203,543 vs $152,695); they cancel. So it is NOT category-honest agreement, and the labor
      model did NOT match this project (unlike Home Depot's $199). Honesty held: workbook untracked (skip-if-absent), gates
      blocked, no parity/accuracy claim, nothing tuned (model heads 1332 UNDERSHOOT real 1420). d4 NOT checked on a netting
      artifact → T28.
- [x] **T28 — PDF plan ingestion (geometry-true input capability).** DONE 2026-05-30 (ultra workflow wflz7bih9, TDD,
      honesty + correctness verifiers both PASS). USER REQUIREMENT "must be able to use pdf" — the real plan sets are
      VECTOR PDFs (Bluebeam Revu), no DXF/DWG. Added pdfjs-dist@6.0.227 + src/engine/pdf-floorplan.js: extractSegmentsFromOpList
      (pure — walks pdfjs op lists incl. legacy moveTo/lineTo AND v6 batched OPS.constructPath DrawOPS buffers + rectangle/
      closePath → line segments in FEET via an operator-supplied scale) + segmentsToFloorPlan (bbox footprint → rooms[] the
      engine consumes) + floorPlanFromPdf (async; THROWS if scale missing/≤0 — never guessed; throws if 0 path ops). server.js
      wired a "pdf" planSource (lazy loadPdfjs, resolvePdfFloorPlan; bad/missing scale or unparseable pdf → 400 never 500);
      autosprink.html "Import vector PDF" (page index + drawing-scale ft/pt + file picker, honest bbox label). TDD
      tests/pdf-floorplan.test.js (18 pure, portable) + tests/pdf-floorplan-real.test.js (1 skip-if-absent). Full suite 50
      files/476 tests green (was 48/457; +2 files/+19). Agentic-rules ok. REAL-PDF SMOKE RAN (not skipped): floorPlanFromPdf
      on the actual "1881 - Plumbing.pdf" page 0 extracted segmentCount=64,600 real vector segments + a positive bbox via
      headless pdfjs v6 — PROVING genuine geometry extraction from the real Bluebeam plan. HONESTY: extracted geometry is
      real (the plan's own vector ops, never fabricated); PDF-point→feet scale is OPERATOR-SUPPLIED (throws if absent, no
      auto/guess — grep-confirmed no detectScale/inferScale); bbox footprint honestly labelled best-effort, NOT AHJ/PE/
      accurate; gates stay blocked, no parity/accuracy claim. CAVEATS (→ d4 still open): the page bbox currently includes the
      sheet BORDER (whole-sheet extent, not the building outline) — a category-honest d4 close needs the operator to pick the
      right page + the TRUE drawing scale + ideally a layer/region filter (future T29). The express.json 10mb limit means
      multi-hundred-MB plan sets must be split per page before upload (same cap as the DXF path).

- [x] **T29 — PDF content-region isolation (heuristic footprint tightener).** DONE 2026-05-30 (ultra workflow
      wjic0ps8t, TDD, honesty + correctness verifiers both PASS). Addresses T28's caveat (the PDF footprint was the whole
      sheet incl. frame/title block). Added pure isolateContentRegion(segments, {borderMarginFrac=0.02, borderSpanFrac=0.6,
      gridN=24}) to src/engine/pdf-floorplan.js: (a) full bbox; (b) strip axis-aligned perimeter-hugging frame segments
      (droppedBorderCount); (c) bin remaining segment midpoints to a gridN×gridN grid + 4-neighbour flood-fill → dominant
      cluster bbox; (d) fallbacks (post-border → full) so it never throws/zeroes; result clamped within the full bbox.
      segmentsToFloorPlan/floorPlanFromPdf gained opt-in `isolate` (default FALSE → byte-for-byte the T28 behavior, existing
      tests unchanged). TDD tests/pdf-floorplan.test.js +8 (separable synthetic sheet: border + building cluster + detached
      title block → isolated bbox excludes border + title block, strictly smaller than full; degenerate fallbacks) +
      pdf-floorplan-real.test.js +1. Suite 50 files/486 tests green (was 50/476). Agentic-rules ok. HONEST REAL-PDF FINDING:
      on "1881 - Plumbing.pdf" page 0 (64,600 segs) the DEFAULT gridN=24 isolation is a NO-OP (isolated bbox == full sheet;
      droppedBorderCount 18) — the sheet's vector geometry is one connected corner-to-corner mass (plan body continuous with
      detail/scale/title geometry), so the dominant cluster legitimately spans the whole sheet; gridN=48 tightens to ~96.8%.
      The defaults were NOT tuned to force a shrink (overfitting); the real test asserts truthful invariants (isoArea ≤ full,
      frame-strip engaged, clamped) + logs both bboxes; strict tightening is proven only on separable synthetic input. HONESTY:
      labelled a HEURISTIC APPROXIMATION (NOT a building outline / room segmentation / AHJ/PE drawing); NO scale guessing
      (scale stays operator-supplied, unchanged); default unchanged; gates blocked; no parity/accuracy claim; only 3 files
      touched (engine + 2 tests). REINFORCES the d4 conclusion: closing d4 needs the USER to point at a CLEAN single-building
      floor-plan PAGE + supply the TRUE drawing scale — content-region heuristics alone can't isolate the building from a
      busy multi-detail sheet without page/layer selection.

- [x] **T30 — PDF→bid end-to-end integration coverage.** DONE 2026-05-30 (ultra workflow wg00uhusd, TDD, honesty +
      correctness verifiers both PASS). T28 wired the server "pdf" planSource (POST /sprinkler-bid with base64 pdf +
      pdfPageIndex + pdfScale → resolvePdfFloorPlan → floorPlanFromPdf → normalizeFloorPlan → runSprinklerPipeline) but the
      PDF tests were engine-only — nothing exercised the server pdf→bid chain. NEW tests/pdf-to-bid-api.test.js (5, spawned
      server): a portable makeTinyVectorPdf() helper emits a structurally valid PDF (computed latin1 xref offsets) whose page
      Contents stream strokes two rectangles via real path ops (m/l/S) — parsed by the SAME pdfjs-dist/legacy build the
      server uses. Cases: (a) base64 pdf + pdfScale 0.05 ft/pt + hazard ordinary → 200 + REAL bid (40×30 ft footprint →
      cadModel heads 12 / pipes 20 / walls 4, bid.totalHeadCount>0, scene+cadModel present, pdfMeta surfaced with the honest
      best-effort note) — NO real-dollar/total asserted (coverage only); (b) missing/0/negative pdfScale → 400; (c) garbage
      base64 → 400 (asserted not 500); (d) gates unchanged (fullScopeBid.estimate true, no parity/approved/submittalReady
      flip, GET /api/parity gate.status blocked). NO WIRING BUG found — the chain was already correct (floorPlanFromPdf's
      4-vertex bbox room passes normalizeFloorPlan + layoutRoom); ZERO source edits, test-only. Full suite 51 files/491 tests
      green (was 50/486). Agentic-rules ok. HONESTY: integration coverage of the plumbing ONLY — the PDF + 0.05 scale are
      synthetic test inputs; NO d4/accuracy/parity claim; scale still operator-supplied (server 400s if absent); gates
      blocked. The PDF→bid pipeline is now end-to-end test-covered, READY the moment the user supplies a real floor-plan
      page + true drawing scale. DoD d4 stays OPEN (still needs that user input).

- [x] **T31 — Read the real drawing scale + measure d4 on the real 1881 floor plan.** DONE 2026-05-30 (ultra workflow
      w3uyu1i5g, TDD, honesty + correctness verifiers both PASS — T31 itself honest). Autonomously used the user's supplied
      plan set (no questions). Added pure parseArchitecturalScale(text) to src/engine/pdf-floorplan.js: parses the architect's
      STATED scale notation (e.g. 3/32"=1'-0") → feetPerPoint = (rhsFeet/lhsInches)/72 (arithmetic, NOT a lookup; handles
      mixed/fraction/decimal inches + curly glyphs + SCALE: prefix; junk→null). TDD tests/architectural-scale.test.js (7:
      3/32"=0.148148, 1/8"=0.111111, 1/16"=0.222222, 1"=20'=0.277778, prefixed, curly, null). NEW skip-if-absent
      tests/real-pdf-d4-measure.test.js: loads the real "1881 - Architecturals.pdf" p8 (pageIndex 7 "OVERALL FIRST FLOOR
      PLAN"), DETECTS the scale from the page text (asserts ≈0.148148 — PROVES read-from-sheet not hardcoded; sheet states
      "SCALE: 3/32"=1'-0""), runs floorPlanFromPdf(scale=detected, isolate) → footprint, runs the real auto-bidder, scales
      ×8 floors (from the sheet titles), measures vs the real Cooperative 1881 $538,792.35. Suite 53 files/499 tests green
      (was 51/491). Agentic-rules ok. MEASURED (honest, untuned): detected scale 0.148148 ft/pt; footprint bbox 129.47×85.61
      ft = 11,083.6 sqft; ×8 = 88,668 sqft vs real 170,654 = 0.520× (the sheet's own overall dim is 312 ft, so the footprint
      is under-captured); per-floor heads 88/pipe 1121ft/material $6,571; ×8 → heads 704; fullScopeTotal $229,933.95 vs real
      $538,792.35 = −57.32%; materialsOnly $52,572 vs $188,778 = −72.15%; labor $112,225 vs $152,695 = −26.50% (BOTH under —
      uniform undercount, NOT netting). HONESTY: scale is the sheet datum (test proves detection); floors=8 from sheet titles;
      footprint is the extracted geometry; nothing tuned; gates blocked (estimate true, parity undefined, not approved). d4
      stays OPEN (d4ShouldBeChecked=false): the auto-estimate is −57% short, ROOT CAUSE PINPOINTED = the T29 dominant-cluster
      isolation captured only ~0.52× the building footprint (129 ft wide vs the real 312 ft plan) → uniformly low. → T32: fix
      footprint extraction to capture the FULL plan extent (validate against the sheet's stated 312 ft overall dim + ~21,332
      sqft/floor — a real-drawing datum, NOT the dollar total).
- [x] **T32 — Footprint extraction fidelity (CTM fix + full-extent mode).** DONE 2026-05-30 (ultra workflow w3e1irbwn,
      TDD, honesty + correctness verifiers both PASS — T32 itself honest, validated against drawing geometry not dollars).
      REAL BUG FOUND + FIXED: extractSegmentsFromOpList IGNORED the PDF Current Transformation Matrix (CTM). The 1881
      Bluebeam sheet authors geometry in compact model space + a 2.83465× base transform + 418 nested transform/save/restore
      ops; without the CTM the building collapsed to a 915-pt local cluster — so T31's "0.52× under-capture" was actually
      this CTM bug, NOT clustering. Added CTM stack tracking (save/restore/transform; maps every moveTo/lineTo to page space
      before the feet/pt scale; identity CTM leaves synthetic op-lists unchanged). Added export isolatePlanExtent (full-extent
      mode: strip frame → grid/flood-fill → drop only small DETACHED outlier islands → union bbox); wired opts.isolate
      'fullExtent' through segmentsToFloorPlan/floorPlanFromPdf (default unchanged; T29 dominant path preserved). TDD
      tests/pdf-floorplan.test.js +9 (multi-wing synthetic: dominant→one wing, fullExtent→both, excludes title block) +
      real-PDF full-extent case. Suite 53 files/509 tests green (was 53/499). Agentic-rules ok. MEASURED (CTM-corrected,
      untuned): detected scale 0.148148; footprint bbox 367.00×242.67 ft (= the real sheet extent 384×256 ft minus margins —
      CTM fix confirmed); long-dim 367 vs sheet 312 ft = 1.176×. HONEST NEGATIVE RESULT: the dollar bid swung −57%→+215.15%
      ($1,697,987 vs $538,792; materials +109.6%, labor +464.6%) because a rectangular BBOX over-captures ~4.175× the real
      per-floor area (89,059 vs 21,332 sqft) — the bbox swallows dimension witness-lines/notes/title geometry; it is NOT the
      irregular building OUTLINE. fullExtent was a no-op on this single-connected-mass plan (retained 1 group, same bbox as
      T31). KNOWN LIMITATION (honest): the real-PDF geometry-validation uses an OR (long-dim OK ‖ area OK) that passed on the
      18%-overshoot long-dim while area was 4.175× off — it documents the measurement, does NOT claim d4. HONESTY: nothing
      tuned to $538,792; scale = parsed sheet datum; floors=8 from sheet; principled outlier-drop; gates blocked
      (estimate true, parity undefined, not-AHJ/AutoSprink disclaimer). d4 stays OPEN (d4ShouldBeChecked=false) — residual is
      now BBOX-vs-OUTLINE: need true building-outline polygon AREA (not bbox) + a residential takeoff model. → T33.
- [x] **T33 — Building-outline polygon extraction (area, not bbox).** DONE 2026-05-30 (ultra workflow wl8yy9dql, TDD,
      honesty + correctness verifiers both PASS — T33 itself honest, principled + untuned). Added pure buildingOutlinePolygon
      (wall-like filter: near-axis segments ≥ minWallFt 3 ft → drops dimension witness-lines/text/hatching; dominant connected
      wall network via union-find @ connectTolFt 1.5 ft → drops detached detail/legend drawings; occupancy-grid flood-fill
      @ gridN 140 → enclosed rectilinear footprint + shoelace area) + extract:"outline" wiring (default unchanged). SYNTHETIC
      TESTS PROVE THE METHOD IS GEOMETRICALLY CORRECT: an L-shape (true 12,000 sqft, all-segments bbox 193,600) was recovered
      to ~12,000 (±20%), dropping all witness/text segments + excluding the detached detail cluster; rectangle recovery + ×2
      scaling + degenerate fallback all pass. TDD +11; suite 53 files/520 tests green (was 53/509). Agentic-rules ok. HONEST
      NEGATIVE ON THE REAL SHEET (the load-bearing finding): on 1881 p8 the method CANNOT isolate the building — the sheet has
      ~1,765 axis-aligned segments spanning ≥70% of the sheet (drawing grid/match/section lines + Bluebeam-tessellated
      dimension strings) that are INDISTINGUISHABLE from walls by length+orthogonality+connectivity, so the wall network fills
      the whole 384×256 ft sheet, the occupancy grid fills 100% (enclosed/bbox = 1.000×, 0 sqft removed), area = 98,532
      sqft/floor = 4.619× the real 21,332, bid +247% ($1,871,213 vs $538,792; materials +130%, labor +524%). I did NOT tune
      minWallFt/gridN/connectTolFt to force 21,332 or $538,792 (verified the longest "walls" at every 3..40 ft threshold are
      384/256-ft sheet-spanning lines — no length threshold separates them). Root cause = vector COMPOSITION (no layer
      separation), not bbox-vs-outline. Gates blocked; no parity/accuracy claim. → DoD d4 resolved below as an accepted
      documented limitation; T34 pursues the ONE sanctioned reopen path (layer-aware extraction).
- [x] **T34 — PDF graphics-state layer extraction (the sanctioned d4 reopen path).** DONE 2026-05-30 (ultra workflow
      wcja132cs, TDD, honesty + correctness verifiers both PASS — principled, target-blind, untuned). HONEST NEGATIVE for d4
      (best-yet but still short). Added per-segment graphics-state capture to extractSegmentsFromOpList (lineWidth +
      strokeColor tracked through a parallel save/restore stack alongside the CTM; tags additive so legacy tests pass).
      Fixed the recon color-NaN: exported normalizeStrokeColorArg — pdfjs v6 normalizes all stroke-color ops to
      setStrokeRGBColor carrying a hex STRING "#rrggbb" (the recon decoded it as a number); helper handles hex/number/[r,g,b]
      → #rrggbb, pattern/transparent → null. Added selectWallLayer (groups by (color,lineWidth); baseline = modal-by-length
      band; wall candidates = strictly-heavier-than-baseline; pick the most coherent connected extent; explicit
      no-heavier-band honest-negative path) — a documented CAD convention decided from the drawing's own lineweight
      distribution, NEVER a search for the 21,332/538,792 target. Wired extract:"wallLayer" (default unchanged). TDD +10
      synthetic (two-layer op-list: heavy walls vs thin full-width annotation → tagged + wall layer selected, annotation
      excluded) + 1 real-PDF. Suite 53 files/530 tests green (was 53/520). Agentic-rules ok. REAL 1881 p8: 25 (color,lineWidth)
      groups (confirms the 10 lineweight bands); baseline = lineWidth=0 hairline (195,775 segs / 713,050 ft — pdfjs
      default-when-unset); SELECTED 0.09/#3f3f7f (6,858 segs) via heavier-lineweight-coherent-extent → footprint 304.54×193.61
      ft = 58,963 sqft (long-dim 304.5 vs sheet 312 = 0.976×; area 2.764× real 21,332); bid ×8 fullScopeTotal $1,106,217 vs
      real $538,792 = +105.31% (materials +35.74%, labor +267.39%). IMPROVED vs T33 (4.6×/+247% → 2.76×/+105%) but NOT closed:
      lineweight does NOT cleanly isolate the wall layer here — the structural-heavy bands (0.35/0.7 black) are fragmented
      across groups and the dominant baseline is a lineWidth=0 hairline, so no single band = the building outline. Verifier
      confirmed NO fitting (a fitter would land at 21,332/538,792; T34 overshoots + reports it). Gates blocked; no
      parity/accuracy claim. DoD d4 REMAINS the accepted documented limitation — the autonomous extraction approaches
      (geometry T33, full-extent T32, outline T33, graphics-state T34) are now EXHAUSTED; closing it needs per-sheet manual
      region selection (user) or deep per-CAD-system reverse-engineering, neither honestly automatable here without fitting.

- [x] **T35 — SAM 3.1 plan segmentation via OpenClaw (BUILT + mock-tested; real run deferred).** DONE 2026-05-30 (ultra
      workflow wmsl6bn2w, TDD, honesty + correctness verifiers both PASS — fail-soft, NO fabrication, scale-guard, no false
      real-run claim, gates untouched). NEW src/components/sam-floorplan.js: buildPlanSegmentationPayload({pdfRef,pageIndex,
      scale,targets=[building_outline,walls,rooms,layers]}) (deterministic) + segmentFloorPlanViaSam({invoker,...}) → injected
      OpenClaw bridge invoker → GX10 SAM 3.1; usable result {ok:true,source:"sam-3.1",layers:{building_outline,walls,rooms,...},
      label} else FAIL-SOFT {ok:false,skipped:true,reason} (invoker absent/non-function/throws/empty/no-outline/degenerate → NO
      fabricated polygon) + reconstructFloorPlanFromSam(samResult,{scale,hazard}) → rooms[] (SAM building_outline px→ft via the
      operator/drawing scale; THROWS if scale missing/≤0; throws if no usable outline — never fabricates) + bbox/areaSqft
      (shoelace) + layerMap. Wired floorPlanFromPdf extract:"sam" (fail-soft → {samSkipped:true,reason} when SAM down, caller may
      fall back to vector; default path unchanged). TDD tests/sam-floorplan.test.js (16) + pdf-floorplan.test.js (+5). Suite 54
      files/552 tests green (was 53/530; +1 file/+22). Agentic-rules ok. MOCK PATH: synthetic building_outline 600×400px @ scale
      0.1 ft/px → 60×40 ft / 2400 sqft footprint → layoutRoom → heads>0 (bid). HONESTY: SAM masks are REAL segmentation ONLY when
      the bridge runs — here mock-only, fail-soft degrades to skipped, nothing fabricated; scale operator/drawing-derived
      (throws if absent); label "best-effort … NOT AHJ/PE/AutoSprink/manufacturer parity"; gates blocked. REAL RUN DEFERRED:
      OpenClaw/SAM (GX10) is HTTP_UNREACHABLE this pass (gx10_status: HAL/OpenClaw/Brain all down — same as S5); NO real SAM run
      was performed and none is claimed. NEXT (when OpenClaw/SAM reachable): wire the production OpenClaw SAM invoker into
      runSprinklerPipeline's pdf path, run the REAL segmentation of the 1881 plans, and re-measure d4 — d4 closes ONLY if the
      real reconstructed footprint matches the drawing geometry (~21,332 sqft/floor, 312 ft) AND the bid approaches $538,792
      category-honest, untuned.

## AUTOSPRINK PARITY epic (the goal): close functional gaps to AutoSprink feature parity
HONESTY CONTRACT: "parity" is tracked, not asserted. The `AUTOSPRINK_PARITY_INCOMPLETE`
gate (registry.js) stays BLOCKED until every parity-matrix row is genuinely PRESENT
*and* the regulated/manufacturer rows have real evidence (manufacturer-exact catalog
models + licensed PE/AHJ review, user-supplied via Settings/T19). The P-tasks below
build the FUNCTIONAL engine parity we can build honestly; they NEVER flip the gate or
claim AutoSprink/AutoCAD parity, AHJ/permit/PE/manufacturer/fabrication readiness.

### Parity matrix (AutoSprink feature area -> HaloFire status)
- Drawing import (DXF/SVG) — PRESENT (T4/T11); PDF import — MISSING (deferred)
- Building + multi-space modeling, walls/openings/columns — PRESENT (T11-T14)
- Head layout (NFPA-13 spacing, hazard) — PRESENT (sprinkler-layout)
- Pipe routing + schedule sizing — PRESENT; demand-balanced hydraulic sizing — PARTIAL (single-path est, T2)
- Full hydraulic network balance (K-factor Q=K√P, node pressures, system demand) — PRESENT (P1, best-effort iterative est)
- NFPA-13 compliance checker (spacing/coverage/area/density/obstruction/branch limits) — PRESENT (P2, best-effort geometric, clears no gate)
- Hanger / seismic support placement + schedule — MISSING -> P3
- Component CAD model library (per-component models) — SCAFFOLD only (T15/16/17); generated models — MISSING -> P4; manufacturer-exact catalog models — GATED (user/Settings)
- Submittal package (head/pipe schedules, hydraulic summary, BOM) + PDF report — MISSING -> P5
- Parity matrix + status surface + gate wiring — MISSING -> P6
- 3D model + CAD export (DXF/STEP/IFC/STL) — PRESENT (T1/T8/T13)
- Bid / takeoff / BOM / full scope — PRESENT (T3)
- Evidence gates + resolve workflow + Settings doc upload — PRESENT (T5/T19)
- AHJ/PE stamp, manufacturer approval — GATED by design (real-world evidence only)

- [x] **P1 — Full hydraulic network balance.** DONE 2026-05-29. New
      `src/engine/hydraulic-network.js` (re-exported from hydraulics.js):
      `kFactorFlow(k,psi)=k√psi` head discharge + `balanceNetwork({network|cadModel,
      hazard,kFactor?,C?,sourcePsi?,minHeadPressurePsi?})` — picks the remote branch,
      orders heads remote-first, seeds the most-remote head at min operating psi, walks
      inward accumulating Q=K√P node-by-node (each upstream node = downstream psi +
      Hazen-Williams friction over the inter-head segment carrying the accumulated flow),
      then adds cross-main/main-tie/riser friction (full branch demand) + 0.433 psi/ft
      head elevation to get requiredSourcePsi; returns {heads[],totalDemandGpm,
      requiredSourcePsi,segments[],balanced}. Best-effort ITERATIVE estimate, NOT a sealed
      Hardy-Cross solve / stamped calc. Hand-computed vitest cases in
      tests/hydraulic-network.test.js. Studio Hydraulics panel surfacing best-effort/deferred.
- [x] **P2 — NFPA-13 compliance checker.** DONE 2026-05-29. `src/engine/nfpa-compliance.js`: check a
      layout/building against public NFPA-13 rules (max coverage area & spacing & min
      spacing & max-to-wall by hazard, max heads per branch/system, obstruction note) ->
      pass/fail findings list. Deterministic; tests. Surface as a compliance panel + a
      COMPLIANCE_REVIEW gate note (still requires PE sign-off; never auto-clears).
- [x] **P3 — Hanger + seismic support placement.** DONE 2026-05-29. `src/engine/supports.js`: place hangers
      along pipes at NFPA-13 max spacing (size-dependent) + seismic brace intervals -> support
      solids + counts feeding the BOM/takeoff. Tests. Render in studio + add to cad-model.
- [x] **P4 — Generate component models -> populate parity inventory.** DONE 2026-05-29. Pipeline that runs the
      T17 OpenSCAD generators for every registry component to produce a model (and renders STL
      where the openscad binary exists, e.g. VPS), marking each 'generated' in
      buildParityInventory. Generated != manufacturer-exact, so the PARITY gate stays blocked
      on catalog+PE evidence; this raises functional coverage + drives the BOM model refs. Tests.
- [x] **P5 — Submittal package + PDF report.** DONE 2026-05-29. `src/engine/submittal.js`: assemble a submittal
      package (project header, head schedule, pipe schedule, hydraulic summary, BOM, gate
      status) as structured data + drive OpenClaw generate_pdf_report via an injected invoker
      (graceful skip). Labelled best-effort, NOT a stamped submittal. Tests on package shape.
- [x] **P6 — Parity matrix module + status API + gate wiring.** DONE 2026-05-29. `src/engine/parity-matrix.js`:
      compute the matrix above from actual module/inventory state -> {area,status,evidence}.
      `GET /api/parity` returns it + AUTOSPRINK_PARITY gate (blocked until all required areas
      PRESENT with real evidence). Tests prove the gate stays blocked while any area is missing
      and that generated-only component models do NOT satisfy the manufacturer/PE rows.
- [x] **P7 — Parity + component-library + compliance UI (BROWSER).** DONE 2026-05-29.
      Parity matrix + component library shipped in T20 (settings.html). P7 adds the
      studio-side compliance + full-hydraulics + submittal surface. API (ultra workflow
      wn1mpkom8, TDD, 3 adversarial verifiers all PASS): sprinkler-bid now returns
      `hydraulicNetwork` (P1 balanceNetwork full balance) + `compliance` (P2 checkCompliance
      NFPA-13), and a new `POST /api/projects/:name/submittal` returns a downloadable
      buildSubmittal package (Content-Disposition attachment). UI (autosprink.html): the
      Hydraulics panel now shows the iterative network balance (req @ source, network demand)
      alongside the single-path estimate; a Compliance panel lists per-rule PASS/WARN/FAIL +
      the "professional review required" honesty note; a "Download submittal (.json)" button.
      Browser-verified (preview_eval/DOM, 1500px): Home Depot → network 509 gpm / 48.4 psi @
      source, compliance 2 fails surfaced honestly, submittal downloads with submittalReady
      false + all header honesty flags false + AUTOSPRINK_PARITY blocked. Full suite 34 files
      / 320 tests green (+1 file / +6 tests). HONESTY/fail-closed held — nothing flips a gate;
      compliance "pass" is geometric-only; building path skips the balance rather than
      fabricating numbers.

## REAL-PART RENDER epic: the 3D builder renders actual CAD part meshes, not primitives
USER REQUIREMENT (2026-05-29): "the 3d builder must use autocad 3d rendered parts for the
sprinklers and all other components used to construct the sprinkler system." Today the studio
(autosprink.html) draws PRIMITIVES — SphereGeometry heads + CylinderGeometry drops/pipes. The
component-model pipeline (T15–T19, P4) generates per-component OpenSCAD/SAM models but they are
NOT wired into the 3D render. This epic instances a real 3D part mesh at every component.
HONESTY CONTRACT (unchanged, fail-closed): parts come from (a) manufacturer/AutoCAD CATALOG
models (DWG/STEP→web mesh, user-supplied/licensed via Settings) — the true "AutoCAD parts"; or
(b) BEST-EFFORT recreations via the OpenSCAD/SAM stack. Generated parts render real geometry but
are NOT manufacturer-exact and NEVER satisfy AUTOSPRINK_PARITY / manufacturer / AHJ / PE gates —
those stay BLOCKED until real licensed catalog models + professional review are attached. Each
rendered part must show its true source (catalog | generated | missing); a generated part is
never labelled manufacturer-exact.
- [x] **R1 — Part-mesh build pipeline.** DONE 2026-05-29 (workflow we28777td, TDD). New
      src/components/part-mesh.js: buildPartManifest({scadRunner,outDir,write}) walks the registry,
      runs T17 generateScadFor + renderScadToStl through an INJECTED runner (never spawns openscad
      itself), writes parts/<key>.stl only when a real STL renders (never fabricated), and emits the
      manifest (key→{source:'generated'|'missing',file,format,manufacturerExact:false,present}).
      scripts/build-parts.mjs ("npm run build:parts") wraps the real openscad CLI, degrades to an
      all-'missing' manifest (exit 0) when the binary is absent. .gitignore ignores parts/*.stl
      (allows parts-manifest.json). With a mock runner: generated=22, missing=12 (34 total).
- [x] **R2 — Parts manifest API + resolver.** DONE 2026-05-29. `GET /api/parts` (authMiddleware,
      before the SPA catch-all) serves the on-disk manifest or a live all-'missing' registry view;
      manufacturerExactCount + a per-entry sanitize (coerce manufacturerExact→false unless source is
      a real catalog/manufacturer source) so a tampered manifest can't leak a false claim; parity
      gate hardcoded fail-closed 'blocked'. parts/<key>.stl reachable via the repo-root static mount.
      Verified live: all-missing branch (34 missing, count 0, blocked) + tamper test (generated
      +manufacturerExact:true → coerced false; catalog kept; count derived). Full suite 36 files /
      329 tests green (+2 files, +9 tests). Honesty/correctness/test-truth adversarially verified
      (the lone 'fail' verdict was a wrong-cwd false negative — searched E:/ClaudeBot, not HaloFire;
      independently re-confirmed files exist + suite green).
- [x] **R3 — Studio render swap (BROWSER).** DONE 2026-05-29 (heads). autosprink.html now loads
      the head part mesh from GET /api/parts via STLLoader (/parts/head_pendent.stl), normalizes
      (center, OpenSCAD Z-up→three Y-up, scale to ~1ft) and renders all 936 sprinkler heads with
      InstancedMesh using the real STL geometry (verified: usingPartMesh=true, 1716 verts/head vs
      ~150 for the old sphere), fail-soft to a primitive when no part mesh, status reads "heads:
      generated CAD part (best-effort, not mfg-exact)". Browser-verified via preview_eval/__headDebug.
      Generated locally with the official OpenSCAD portable (build:parts → 22 STLs); gate stays blocked.
      SCOPE NOTE (honest): only HEADS render as discrete part meshes today because buildCadModel
      only emits head solids as placeable points; fittings/valves/riser/FDC part placement needs
      the cad-model to emit those component positions → R6.
- [x] **R6 — cad-model emits discrete component placements + studio renders their part meshes.**
      DONE 2026-05-29 (cad-model engine via workflow wx8rn0b0g, TDD, 3 adversarial verifiers PASS;
      studio render + browser-verify this session). cad-model.js buildRoomCad + buildStoryCad emit
      kind:'component' placement solids {componentKey, position:[x,y,z], category} derived from the
      real riser/cross-main network: riser_assembly at the riser top, valve_alarm_check on the riser,
      fdc at the riser base, fitting_tee at each branch↔cross-main junction; counts.components added
      to both tally fns; offsetSolidZ lifts component markers per story; fail-closed via getComponent.
      autosprink.html: generalized the part loader to a partGeoCache + loadPartGeo(key,targetFt);
      addComponentParts instances each component key's part mesh (InstancedMesh) or a MARKED wireframe
      primitive when the part is missing; generate() preloads the model's component keys before render.
      Browser-verified (Home Depot): 39 components — fitting_tee×36 + valve_alarm_check×1 as real
      part meshes, fdc×1 + riser_assembly×1 as marked primitives (those parts are non-generatable/
      missing, also shown in S4 needs-import). Status: "39 components (2 part types as meshes, 2
      primitive fallback)". TDD tests/cad-model-components.test.js (11 tests). Full suite 40 files/366
      tests green. HONESTY: component solids are placement markers keyed to registry parts, positions
      derived from real geometry (never fabricated), NOT manufacturer-exact, flip NO gate. (Screenshot
      skipped — preview_screenshot hangs on the WebGL canvas; DOM/__componentDebug verification used.)
- [x] **R4 — Settings per-component part upload/link (BROWSER+API).** Attach a manufacturer
      AutoCAD/STEP/glTF/STL part per component (extends T19 doc slots to per-component assets);
      a real catalog part with evidence overrides the generated mesh and marks source 'catalog'
      (still PE/AHJ-gated; does not by itself clear AUTOSPRINK_PARITY). Browser-verified.
      DONE 2026-05-29 — API half (POST/DELETE /api/parts/:key/override) + browser half (V4).
- [x] **R5 — VPS parts render + serve + redeploy.** Ensure openscad on the VPS renders the parts,
      serve `parts/`, redeploy the stack, and confirm the studio loads real meshes over the web.
      DONE 2026-05-29 — VPS at dbd8be6, OpenSCAD 2021.01 rendered 22 STLs, /parts served over HTTP,
      /api/parts reports head_pendent present+file; manufacturerExactCount 0, gate blocked.

## AUTO-SOURCE epic: OpenClaw autonomously finds/creates every part; user only fills the residual
USER REQUIREMENT (2026-05-29): "openclaw [is] to be proactively finding all the cad parts or
creating them from image catalogs. this is to be ai automated. the user at most will point the
halofire cad studio at a document or url for parts that need to be imported but only ones
openclaw hasn't already found on its own." This plugs OpenClaw-driven sourcers into the existing
T16 model-resolver (resolveComponentModel tries injected sourcers in priority) and runs them as
an autonomous loop across the whole registry.
GROUNDED IN REAL OPENCLAW TOOLS (gx10_status capabilities, 2026-05-29): web_search, web_fetch,
browser (catalog discovery); generate_3d_model (bridge alias) + hal_sam_status / SAM 3.1
(create-from-image); generate_dxf; image_generate. All reached via the governed
HAL `/codex-bridge/invoke` path (mcp openclaw-gx10 / hal-bridge).
HONESTY CONTRACT (fail-closed, unchanged): autonomously FOUND (public web/TraceParts/GrabCAD)
or CREATED (SAM/generate_3d_model/OpenSCAD) parts are BEST-EFFORT with recorded provenance +
license; they are NOT manufacturer-exact and NEVER clear AUTOSPRINK_PARITY / manufacturer / AHJ
/ PE gates (stay BLOCKED). Only a genuinely licensed manufacturer model with real evidence is
'catalog/manufacturer-exact', and even then parity needs PE/AHJ review. The loop must be
FAIL-SOFT: OpenClaw is intermittently unreachable (direct probe HTTP_UNREACHABLE while the bridge
reports ready) — when it is down, sourcing degrades to OpenSCAD/missing, never fabricates a found
model or a license.
- [x] **S1 — OpenClaw catalog-discovery sourcer.** DONE 2026-05-29 (workflow wliod8ee5, TDD).
      src/components/auto-source.js makeCatalogSourcer({invoker}) + buildCatalogSearchQuery: drives
      the injected OpenClaw bridge to FIND public CAD; fail-soft (absent/non-function/throws/empty →
      null); on a usable {url,format,data?,license?,manufacturer?} returns source:'catalog' with
      provenance+license passed through (never fabricated), manufacturerExact:FALSE. Honesty:
      machine-found provenance is unverified → registrySourceFor downgrades resolver 'catalog' →
      registry 'generated' so it can never count toward the manufacturer gate.
- [x] **S2 — OpenClaw image→3D creation sourcer.** DONE 2026-05-29. makeImageReconstructSourcer
      ({invoker,gen3dInvoker}) reuses T18 reconstructComponentModel (SAM 3.1) with a generate_3d_model
      fallback; source='generated' subtype sam|gen3d, manufacturerExact:false; fail-soft → null.
- [x] **S3 — Autonomous sourcing loop.** DONE 2026-05-29. runAutoSource walks the registry via
      resolveComponentModel (priority catalog→openscad→sam), buckets found/created/generated/missing,
      builds fail-closed modelStatusByKey, optional mesh write + injected recorder (Brain logging),
      idempotent + fully fail-soft. HONESTY FIX (adversarial verifier caught a real violation): the
      returned parityGateStatus now derives from manufacturerParityComplete (gate-grade) — ALWAYS
      'blocked' for auto-sourced models — NOT from the functional inventory (which counts 'generated'
      as present and would wrongly read 'clear' at 100% coverage); functional coverage reported
      separately as `functionalCoverage` (explicitly NOT a parity claim). Test proves: catalog finds
      all 34 → functionalCoverage.complete=true BUT parityGateStatus='blocked'. Suite 37 files/349
      tests green (+1 file/+20 tests). NOTE: built+unit-tested with MOCK invokers; real autonomous
      run needs OpenClaw reachable (currently HTTP_UNREACHABLE) → validated on the VPS in S5. Cron
      scheduling of the loop is part of S5.
- [x] **S4 — "Needs import" surface (studio/Settings) (BROWSER).** DONE 2026-05-29. settings.html
      Component Library now reads the LIVE GET /api/parts manifest (was the registry all-missing
      inventory): shows "<N> auto-sourced (best-effort, <M> manufacturer-exact) · <K> need import.
      OpenClaw finds/creates parts automatically; you only import the rest. Parity gate stays
      blocked." A dedicated "Needs import (K)" block lists only the residual parts OpenClaw hasn't
      sourced; clicking any needs-import pill jumps to + prefills the documentation importer
      (manufacturer_cut_sheets slot) for that part. Browser-verified (22 auto-sourced/12 need import,
      0 manufacturer-exact, gate blocked; click fitting_cap → docType=manufacturer_cut_sheets,
      placeholder "catalog URL / file for: fitting_cap", input focused). NOTE: the current per-part
      import path is the existing Settings doc slots; a dedicated per-component asset-upload endpoint
      that OVERRIDES the generated mesh with a user-attached catalog model is R4 (still pending).
- [x] **S5 — VPS ↔ OpenClaw wiring + schedule.** The VPS HaloFire stack reaches OpenClaw (GX10)
      via the governed bridge/SSH for agentic runs; schedule the S3 loop; fail-soft + observable
      (records last-run, found/created/failed counts). Confirm end-to-end on the VPS.
      DONE 2026-05-29 — auto-source-runner.js (bridge invoker + status) + scripts/auto-source-run.mjs
      + GET /api/auto-source/status; VPS cron every 6h; live run degraded fail-soft to OpenSCAD
      (bridge invoke 500/refused) → generated 22 / missing 12, gate blocked.

## VISUAL FIDELITY epic: plan-mode drawings + demo-mode rise + CAD-correct connectors
USER REQUIREMENT (2026-05-29, with screenshots): "the pipes themselves and all connectors must
be autocad correct... the autosprink clone and the auto bid 3d demo must accurately reflect the
actual components being used... i must be able to see the actual drawings in the plan mode and
see the structure and sprinklers rising out of the plan in the demo mode." Also a fair process
callout: VISUALLY confirm renders, don't just check vertex counts.
DIAGNOSIS (verified by an actual headless capture this session, not DOM hooks): the studio
renders NO roof slab; the gray plane is the floor. Heads/components ARE real part meshes (R3/R6)
but are specks at the ~350ft building overview zoom; plan mode shows the pipe network over a bare
floor with no building structure (the Home Depot built-in is a simplified rectangle — no interior
walls/spaces/doors); there is no demo presentation. Connectors: only tees emit (R6); elbows/
couplings/reducers don't, and head orientation isn't pendant-down.
HONESTY (unchanged): generated OpenSCAD parts are best-effort SHAPES, NOT the exact manufacturer
products — "accurately reflect the actual components" is fully true only once real catalog/
manufacturer models are attached (R4 override / OpenClaw auto-source). We build toward functional
AutoSprink coverage (tracked in the parity matrix) but never CLAIM 1:1 AutoSprink/AutoCAD parity;
the AUTOSPRINK_PARITY gate stays fail-closed BLOCKED.
- [x] **V0 — Capturable studio for headless visual verification.** DONE 2026-05-29. autosprink.html
      WebGLRenderer now uses preserveDrawingBuffer:true + window.__snapshot(w,h,q) renders at a
      capped size and returns a compact JPEG data URL (preview_screenshot hangs on the rAF canvas).
      Verified: captured the Home Depot iso view to disk and viewed it (this is how the diagnosis
      above was made). Every subsequent V-task is verified by capturing + looking, not just DOM.
- [x] **V1 — Plan mode = readable floor-plan drawing.** DONE 2026-05-29. New tested helper
      src/engine/plan-view.js planOrthoFrustum(bbox,aspect,margin) fits the WHOLE plan bbox to the
      viewport (aspect-correct, centered, margin) so the top-down drawing frames edge-to-edge;
      planCenter. autosprink.html: frameCamera/setView/onResize use it (fitPlanOrtho), orthoCam.up
      set north-up, + setPlanStyle (lighter plan backdrop + brighter floor, defensive). TDD
      tests/plan-view.test.js (7: centered, aspect-correct, fits-with-margin, width/height-limited,
      degenerate-safe, planCenter). CAPTURE-VERIFIED (forced a 64KB high-res __snapshot to a file,
      decoded + viewed): a 2-space DXF building renders as a real plan drawing — two outlined rooms +
      interior wall, gray column markers in each space, sprinkler network overlaid (red riser/cross-
      main, blue branches, red heads), amber riser components, north-up, framed with margin. Full
      suite 42 files/385 tests green (+1 file/+7). Agentic-rules ok. Geometry on the OpenGeometry
      kernel; no parity/AHJ/PE claim. Built-in Home Depot is still a bare rectangle (data) — richer
      structure shows on real DXF/SVG imports; enriching the built-in demo can ride with V2.
- [x] **V2 — Demo mode = structure + sprinklers rising out of the plan.** DONE 2026-05-29. New
      tested helper src/engine/demo-timeline.js demoTimeline(elapsedMs,{wallsMs,systemMs,gapMs}) +
      easeInOut → {wallsT,systemT,cameraT,done} phasing the structure rise THEN (after a gap) the
      sprinkler-system rise. autosprink.html: "▶ Demo" header button → playDemo() switches to iso,
      flattens the layer groups, and animates layerGroups.{WALLS,COLUMN} then {MAIN,BRANCH,DROPS,
      HEADS,COMPONENTS} via group scale.y about world y=0 (geometry grows UP from the floor plane,
      which stays put) using the timeline in a rAF loop; status narrates the phases. Added a
      deterministic window.__applyDemoFrame(ms) hook (rAF-throttle-proof). VERIFIED on a 2-space DXF
      via __applyDemoFrame against the REAL geometry groups: t=0 nothing risen → t=1300 structure
      fully up + system still flat on the plan → t=2250 structure up + system 50% → t=3000 all up +
      done; live playDemo confirmed starting ("structure rising out of the plan…") + the canvas
      rendered the rising frames. (Headless preview throttles rAF, so completion was verified via the
      deterministic hook; animates normally in a focused browser — confirmed in the preview window.)
      TDD tests/demo-timeline.test.js (7). Full suite 43 files/392 tests green (+1 file/+7). Agentic-
      rules ok. Geometry on the OpenGeometry kernel; no parity/AHJ/PE claim. Original task text:
      walls extrude up from the floor plan, then mains/branches/drops/heads appear/descend; framed
      + scaled so components are visible. Capture-verified.
- [x] **V3 — CAD-correct connectors + pipe/head fidelity.** cad-model emits elbow (branch ends),
      coupling/reducer (size changes) component markers in addition to tees; studio renders each as
      its oriented part mesh; pendant heads hang below the branch; pipe radii reflect the schedule.
      TDD on the emission + capture-verified render. DONE 2026-05-29.
- [x] **V4 — Settings "attach catalog part" UI (completes R4 browser half).** Per-component button
      (from the S4 needs-import list) POSTing to /api/parts/:key/override so a real catalog model
      replaces the generated shape; the demo then renders the ACTUAL component. Capture-verified.
      DONE 2026-05-29.

## Log
- 2026-05-30: T35 SAM 3.1 plan segmentation via OpenClaw BUILT (mock-tested, fail-soft; real run DEFERRED — OpenClaw HTTP_UNREACHABLE) (ultra workflow wmsl6bn2w, TDD; honesty + correctness verifiers BOTH PASS — fail-soft, NO fabrication, scale-guard, no false real-run claim, gates untouched). USER DIRECTIVE: OpenClaw must read the PDFs + extract plans/diagrams via SAM 3.1 to reconstruct the building + every layer ("that is the standard"). NEW src/components/sam-floorplan.js (mirrors T18 sam-reconstruct.js injected-invoker + fail-soft pattern): buildPlanSegmentationPayload({pdfRef,pageIndex,scale,targets=[building_outline,walls,rooms,layers]}) deterministic; segmentFloorPlanViaSam({invoker,...}) → injected OpenClaw governed-bridge invoker → GX10 OpenClaw PDF-read + SAM 3.1; usable → {ok:true,source:"sam-3.1",layers,label}, else FAIL-SOFT {ok:false,skipped:true,reason} for invoker absent/non-function/throws/empty/no-outline/degenerate — NO fabricated polygon (tests assert layers undefined on every skip path); reconstructFloorPlanFromSam(samResult,{scale,hazard}) scales the SAM building_outline px→ft via the operator/drawing scale (THROWS if scale missing/≤0; throws if no usable outline — never fabricates) → rooms[] + bbox + areaSqft (shoelace) + layerMap. Wired floorPlanFromPdf extract:"sam" (fail-soft → {samSkipped:true,reason}; default vector path unchanged). TDD tests/sam-floorplan.test.js (16) + pdf-floorplan.test.js (+5). Suite 54 files/552 tests green (was 53/530; +1 file/+22). Agentic-rules ok. MOCK PATH: synthetic building_outline 600×400px @ scale 0.1 ft/px → 60×40 ft/2400 sqft footprint → layoutRoom → heads>0. HONESTY: SAM masks REAL only when the bridge runs (mock-only here, fail-soft degrades to skipped, nothing fabricated); scale operator/drawing-derived (throws if absent); label "best-effort … NOT AHJ/PE/AutoSprink/manufacturer parity"; gates blocked. REAL RUN DEFERRED: gx10_status shows OpenClaw/SAM (GX10) HTTP_UNREACHABLE (HAL/OpenClaw/Brain all down, same as S5) — NO real SAM run performed or claimed, exactly like S2/S3/S5 were built-with-mocks-then-validated-live. NEXT (when OpenClaw/SAM reachable — poll mcp__openclaw-gx10__gx10_status): wire the production OpenClaw SAM invoker into runSprinklerPipeline's pdf path, run the REAL SAM segmentation of the 1881 plan set, reconstruct the building + every discipline layer, re-measure d4 — d4 closes ONLY if the real reconstructed footprint matches the drawing geometry (~21,332 sqft/floor, 312 ft) AND the bid approaches $538,792 category-honest, untuned. DoD d4 stays OPEN (documented limitation) until that live run. ROADMAP: Task Queue T1–T35 all checked; d4 = open pending the live SAM run.
- 2026-05-30: T34 PDF graphics-state layer extraction shipped — HONEST NEGATIVE for d4 (best-yet, still short) (ultra workflow wcja132cs, TDD; honesty + correctness verifiers BOTH PASS — principled, target-blind, untuned). Added per-segment graphics-state capture to extractSegmentsFromOpList (lineWidth + strokeColor via a parallel save/restore stack alongside the CTM; tags additive → legacy tests unchanged). Fixed the recon color-NaN: exported normalizeStrokeColorArg (pdfjs v6 normalizes stroke-color ops to setStrokeRGBColor carrying a hex STRING "#rrggbb"; handles hex/number/[r,g,b]→#rrggbb, pattern/transparent→null). Added selectWallLayer (group by (color,lineWidth); baseline=modal-by-length band; wall candidates=strictly-heavier-than-baseline; pick most coherent connected extent; explicit no-heavier-band honest-negative path) — documented CAD convention decided from the drawing's own lineweight distribution, NEVER a 21,332/538,792 target search. Wired extract:"wallLayer" (default unchanged). TDD +10 synthetic + 1 real-PDF; suite 53 files/530 green (was 53/520). Agentic-rules ok. REAL 1881 p8: 25 (color,lineWidth) groups (confirms 10 lineweight bands); baseline lineWidth=0 hairline (195,775 segs/713,050 ft, pdfjs default-when-unset); SELECTED 0.09/#3f3f7f (6,858 segs) → footprint 304.54×193.61 ft = 58,963 sqft (long-dim 304.5 vs 312 = 0.976×; area 2.764× real 21,332); bid ×8 $1,106,217 vs $538,792 = +105.31% (materials +35.74%, labor +267.39%). IMPROVED vs T33 (4.6×/+247% → 2.76×/+105%) but NOT closed — lineweight doesn't cleanly isolate the wall layer (lineWidth=0 hairline dominates; heavy structural bands 0.35/0.7 fragmented). Verifier confirmed NO fitting. Gates blocked; no parity/accuracy claim. DoD d4 stays the accepted documented limitation; the autonomous extraction approaches (T31 CTM / T32 full-extent / T33 outline / T34 graphics-state) are EXHAUSTED — remaining honest paths are per-sheet manual region selection (operator) or deep per-CAD reverse-engineering, neither automatable here without fitting. STANDING: do NOT spawn further autonomous d4-extraction tasks; only operator-supplied region selection or a new user request would honestly advance it. ROADMAP: Task Queue T1–T34 all checked; d4 = documented limitation (unchecked, resolved). Project functionally complete.
- 2026-05-30: T33 building-outline polygon extraction shipped + DoD d4 RESOLVED as an accepted internal-alpha limitation (ultra workflow wl8yy9dql, TDD; honesty + correctness verifiers BOTH PASS — T33 honest/principled/untuned). Added pure buildingOutlinePolygon (wall-like filter near-axis ≥3 ft → drops witness-lines/text/hatching; dominant connected wall network union-find @1.5 ft → drops detached detail/legend; occupancy-grid flood-fill @gridN 140 → enclosed rectilinear footprint + shoelace area) + extract:"outline" wiring (default unchanged). SYNTHETIC TESTS PROVE GEOMETRIC CORRECTNESS: L-shape true 12,000 sqft (all-segments bbox 193,600) recovered ~12,000 ±20%, witness/text dropped, detached detail excluded; rectangle + ×2-scale + degenerate-fallback pass. TDD +11; suite 53 files/520 green (was 53/509). Agentic-rules ok. HONEST NEGATIVE on the REAL sheet: on 1881 p8 the method cannot isolate the building — ~1,765 axis-aligned segments span ≥70% of the sheet (grid/match/section lines + Bluebeam-tessellated dimension strings) INDISTINGUISHABLE from walls by length+orthogonality+connectivity → wall network fills the whole 384×256 ft sheet, occupancy grid fills 100% (enclosed/bbox 1.000×), area 98,532 sqft/floor = 4.619× real 21,332, bid +247.30% ($1,871,213 vs $538,792; materials +130%, labor +524%). NOT tuned (verified longest walls at every 3..40 ft threshold are sheet-spanning lines — no threshold separates them); root cause = vector COMPOSITION (no layer separation). Gates blocked, no parity/accuracy claim. DoD d4 DECISION (d4ShouldBeChecked=false): per the honesty verifier this is a GEOMETRY failure on dense un-layered annotated sheets, fixable only by (a) PDF graphics-state layer/color/lineweight filtering (deep) or (b) per-project takeoff calibration = FITTING (forbidden). RESOLVED d4 as an ACCEPTED INTERNAL-ALPHA LIMITATION — left UNCHECKED (criterion genuinely not met, never falsely claimed), documented in the DoD section with the full evidence trail + the only honest future path (real layer-aware extraction, path a, validated against drawing geometry not the dollar figure). The dollar delta oscillated across all attempts (−72→−48 / −24 netting / −57→+215→+247) — matching a specific real bid to ±25% is not honestly automatable here without fitting. Per the user directive ("stop asking, use the docs"), this DECISION was made autonomously (no question). STANDING: do NOT spawn further d4-chasing tasks or tune; only real layer-aware extraction would honestly reopen it. ROADMAP STATUS: Task Queue T1–T33 ALL CHECKED; DoD d1/d2/d3/d5/d6/d7 checked; d4 = accepted documented limitation (unchecked, resolved). The HaloFire AutoSprink-clone is functionally complete + honest; d4 is the one documented limitation.
- 2026-05-30: T32 footprint extraction fidelity (CTM fix + full-extent mode) shipped (ultra workflow w3e1irbwn, TDD; honesty + correctness verifiers BOTH PASS — validated against drawing geometry not dollars). REAL BUG FIXED: extractSegmentsFromOpList ignored the PDF CTM; the 1881 Bluebeam sheet uses model-space coords + a 2.83465× base transform + 418 nested transform/save/restore ops, so without the CTM the building collapsed to a 915-pt cluster — T31's "0.52× under-capture" was THIS CTM bug, not clustering. Added CTM stack tracking (save/restore/transform → map moveTo/lineTo to page space before the feet/pt scale; identity CTM leaves synthetic op-lists unchanged). Added isolatePlanExtent (full-extent: strip frame → flood-fill → drop only small detached outlier islands → union bbox); wired opts.isolate 'fullExtent' (default unchanged, T29 dominant preserved). TDD +9 (multi-wing synthetic) + real-PDF full-extent case. Suite 53 files/509 green (was 53/499). Agentic-rules ok. MEASURED (CTM-corrected, untuned): scale 0.148148; bbox 367.00×242.67 ft (= the real sheet extent, CTM confirmed; long-dim 367 vs sheet 312 ft = 1.176×). HONEST NEGATIVE RESULT: dollar bid swung −57%→+215.15% ($1,697,987 vs $538,792; materials +109.6%, labor +464.6%) because a rectangular BBOX over-captures ~4.175× the real per-floor area (89,059 vs 21,332 sqft) — bbox swallows dimension/note/title geometry, it is NOT the building OUTLINE. fullExtent no-op on this single-mass plan. KNOWN LIMITATION: the real-PDF geometry validation uses an OR (long-dim ‖ area) that passed on the 18%-overshoot long-dim while area was 4.175× off — documents the measurement, does NOT claim d4. HONESTY: nothing tuned to 538,792; scale parsed; floors 8 from sheet; principled outlier-drop; gates blocked. d4 stays OPEN (d4ShouldBeChecked=false) — residual is BBOX-vs-OUTLINE. Added Task Queue T33 (building-outline POLYGON extraction — enclosed area not bbox; validate the polygon area vs real ~21,332 sqft/floor via a principled method, never tune; if geometry then matches but dollars still miss, accept d4 as a documented limitation rather than fit). Task Queue T1–T32 all checked; remaining = T33 + DoD d4. NEXT: T33 (building-outline polygon).
- 2026-05-30: T31 real-drawing scale-detection + d4 measurement shipped (ultra workflow w3uyu1i5g, TDD; honesty + correctness verifiers BOTH PASS). Autonomously used the user's supplied 1881 plan set (user said stop asking). Added pure parseArchitecturalScale(text) in src/engine/pdf-floorplan.js — parses the architect's stated scale (3/32"=1'-0" etc.) → feetPerPoint=(rhsFeet/lhsInches)/72 arithmetically (NOT a lookup; the only 0.148148 in source is a doc-comment example; verified on off-table scales 5/64"→0.17778, 3/16"→0.07407). TDD tests/architectural-scale.test.js (7). NEW skip-if-absent tests/real-pdf-d4-measure.test.js: loads real 1881 - Architecturals.pdf p8 (pageIndex 7 OVERALL FIRST FLOOR PLAN), DETECTS scale from page text (asserts ≈0.148148 — proves read-from-sheet), floorPlanFromPdf(detected scale, isolate) → footprint, real auto-bidder, ×8 floors, vs real Cooperative 1881 $538,792.35. Suite 53 files/499 tests green (was 51/491; +2/+8). Agentic-rules ok. MEASURED (untuned): detected scale 0.14814815 ft/pt; footprint 129.47×85.61 ft = 11,083.6 sqft; ×8 = 88,668 vs real 170,654 sqft = 0.520× (sheet overall dim is 312 ft → footprint under-captured); fullScopeTotal $229,933.95 vs $538,792.35 = −57.32%; materialsOnly $52,572 (−72.15%), labor $112,225 (−26.50%) — BOTH under (uniform undercount, NOT netting). HONESTY: scale=sheet datum (detection proven, not hardcoded); floors=8 from sheet titles; footprint=extracted geometry; nothing tuned; gates blocked (estimate true, parity undefined, not approved, NOT-AHJ/AutoSprink disclaimer). DoD d4 stays OPEN (d4ShouldBeChecked=false): −57% short, ROOT CAUSE pinpointed = T29 dominant-cluster isolation captured only ~0.52× the building (129 ft wide vs real 312 ft plan). Added Task Queue T32 (footprint extraction fidelity — capture the FULL plan extent, validate against the sheet's 312 ft dim + ~21,332 sqft/floor, NOT the dollar total; never tune). Task Queue T1–T31 all checked; remaining = T32 + DoD d4. NEXT: T32 (footprint extraction fidelity).
- 2026-05-30: T30 PDF→bid end-to-end integration coverage shipped (ultra workflow wg00uhusd, TDD; honesty + correctness verifiers BOTH PASS). Closed the coverage gap: T28 wired the server "pdf" planSource but no test exercised the pdf→bid chain end-to-end (PDF tests were engine-only). NEW tests/pdf-to-bid-api.test.js (5, spawned server on an ephemeral port + admin login, mirroring full-scope-bid-api/dxf-import-api): makeTinyVectorPdf() builds a portable, structurally-valid PDF (computed latin1 xref offsets; page Contents strokes outer 0,0..800,600 + inner 100,100..700,500 rects via m/l/S ops) parsed by the SAME pdfjs-dist/legacy build the server uses. (a) base64 pdf + pdfScale 0.05 + hazard ordinary → 200 + REAL bid (40×30 ft → cadModel heads 12/pipes 20/walls 4, totalHeadCount>0, pdfMeta segmentCount 8 + best-effort note); (b) missing/0/negative pdfScale → 400; (c) garbage base64 → 400 (not 500); (d) gates unchanged (estimate true, /api/parity blocked, parityAchieved not true). NO wiring bug — chain already correct (bbox room passes normalizeFloorPlan + layoutRoom); ZERO source edits, test-only. Full suite 51 files/491 tests green (was 50/486; +1 file/+5). Agentic-rules ok. HONESTY: integration coverage ONLY — synthetic PDF + 0.05 scale are test inputs, NO real-dollar/d4/accuracy/parity claim asserted; scale stays operator-supplied (server 400s if absent, grep-confirmed no detect/infer/guessScale); gates blocked. RESULT: the user's "must use pdf" pipeline (PDF → vector extract → footprint → layout → priced bid) is now proven end-to-end through the HTTP route — READY the moment the user supplies a real floor-plan page + true scale. DoD d4 STILL OPEN — needs (1) the clean single-building floor-plan PDF page + (2) the true drawing scale (operator-supplied, never guessed). Task Queue T1–T30 all checked; remaining = DoD d4 (blocked on user page+scale, or accept-as-limitation).
- 2026-05-30: T29 PDF content-region isolation shipped (ultra workflow wjic0ps8t, TDD; honesty + correctness verifiers BOTH PASS). Pure isolateContentRegion(segments,{borderMarginFrac=0.02,borderSpanFrac=0.6,gridN=24}) in src/engine/pdf-floorplan.js: strips perimeter sheet-frame segments (droppedBorderCount) + flood-fills a gridN×gridN midpoint grid → dominant-cluster bbox; fallbacks (post-border→full), clamped within full, never throws. segmentsToFloorPlan/floorPlanFromPdf opt-in `isolate` (default FALSE = unchanged T28 behavior). TDD +8 synthetic (separable sheet: isolated bbox excludes border + detached title block, strictly < full; degenerate fallbacks) + 1 real smoke. Suite 50 files/486 green (was 50/476). Agentic-rules ok. HONEST REAL-PDF FINDING (reported, not overfit): 1881 Plumbing page 0 (64,600 segs) at default gridN=24 → isolation is a NO-OP (isoArea == full; 18 border segs stripped) because the sheet geometry is one connected corner-to-corner mass; gridN=48 → ~96.8%. Defaults deliberately NOT tuned to force a shrink; real test asserts isoArea ≤ full + frame-strip engaged + logs both; strict tighten proven on synthetic separable input only. HONESTY: heuristic approximation (NOT building outline/room-seg/AHJ/PE); NO scale guessing (operator-supplied, unchanged); default byte-for-byte unchanged; gates blocked; no parity/accuracy claim; 3 files touched (engine + 2 tests). DoD d4 STILL OPEN — content-region heuristics can't isolate the building from a busy multi-detail sheet without page/layer selection; closing d4 needs the USER to supply (1) the clean single-building floor-plan PAGE and (2) the TRUE drawing scale (feet/point) — never guessed. Task Queue T1–T29 all checked; remaining = DoD d4 (blocked on user page+scale, or accept-as-limitation).
- 2026-05-30: T28 PDF plan ingestion shipped (ultra workflow wflz7bih9, TDD; honesty + correctness verifiers BOTH PASS). User requirement "must be able to use pdf" — the real plan sets are VECTOR Bluebeam PDFs (no DXF/DWG). Added pdfjs-dist@6.0.227 (loads headless in Node via the legacy build pdfjs-dist/legacy/build/pdf.mjs, worker via GlobalWorkerOptions.workerSrc=pathToFileURL(require.resolve('pdf.worker.mjs')), no canvas). NEW src/engine/pdf-floorplan.js: extractSegmentsFromOpList(opList,{scale}) PURE — walks pdfjs path ops and emits line segments in FEET (pts×scale); handles THREE shapes incl. the v6 OPS.constructPath DrawOPS-coded Float32 buffer (0 moveTo/1 lineTo/2 curveTo→endpoint/3 quad→endpoint/4 closePath) discovered this run (the real page 0 is 62,692 constructPath ops, zero top-level moveTo/lineTo); rectangle→4 segs; returns {segments,bbox,count}. segmentsToFloorPlan(segments,{hazard}) → {rooms:[{polygon:bbox,hazard}], bbox, wallCandidates, note}. floorPlanFromPdf(source,{pageIndex,scale,hazard,pdfjs}) async — THROWS if scale missing/≤0 (operator-supplied, NEVER guessed) and if 0 path ops (raster page). src/api/server.js: "pdf" planSource (lazy loadPdfjs + resolvePdfFloorPlan; runSprinklerPipeline(req,prebuilt) gained a prebuilt arg; /sprinkler-bid + /submittal await PDF resolution; bad/missing scale or unparseable pdf → 400 never 500; pdfMeta + honest note surfaced). autosprink.html: "Import vector PDF" option + page-index + drawing-scale(ft/pt) + file picker + honest bbox-footprint label; refuses submit when scale not >0. TDD tests/pdf-floorplan.test.js (18 pure synthetic-oplist: legacy + batched + v6 buffer + rectangle, scale→feet, segmentsToFloorPlan rooms, floorPlanFromPdf throws on missing/≤0 scale via injected fake pdfjs) + tests/pdf-floorplan-real.test.js (1 skip-if-absent). Full suite 50 files/476 tests green (was 48/457; +2 files/+19, additive). Agentic-rules ok. REAL-PDF SMOKE RAN (HAVE_PDF true): floorPlanFromPdf on the real "1881 - Plumbing.pdf" page 0 → segmentCount=64,600 + positive bbox (@scale=1 arbitrary, NOT real-world ft — proves extraction only) via real pdfjs v6 — GENUINE vector geometry extracted from the actual Bluebeam plan. HONESTY (fail-closed, verified by grep + tests): geometry is the plan's own vector ops (never fabricated); scale operator-supplied (throws if absent, no detectScale/inferScale/autoScale anywhere); bbox footprint labelled best-effort NOT AHJ/PE/accurate; AUTOSPRINK_PARITY stays blocked (parity-matrix untouched, 14/14 green), no parity/accuracy claim. DoD d4 STILL OPEN: the capability exists, but the page bbox currently includes the sheet BORDER (whole-sheet, not building outline) — a category-honest d4 close needs the operator to pick the right page + TRUE drawing scale + a layer/region filter to isolate the building (future T29), then re-run a real project (Cooperative 1881 / Home Depot) on the extracted footprint and re-measure so materials AND labor BOTH approach (not netting), never by tuning. Next unchecked Task Queue item: (none — T1–T28 checked); remaining = DoD d4 (needs T29 building-outline isolation + real page/scale, or accept-as-limitation).
- 2026-05-30: T27 Cooperative 1881 real-project integration shipped (ultra workflow wd0q4lxuw, TDD; honesty + correctness verifiers BOTH PASS; NO src/engine or src/components edits — confirmed empty git diff there — so this is the existing T26 model applied to REAL inputs, untuned). User supplied source-of-truth bid docs for "The Cooperative 1881 Apartments" (1881 W North Temple, Salt Lake City UT; GC Kier Construction) — a RESIDENTIAL NFPA-13R system (residential heads + drops, ZERO ESFR; wet residences / dry parking). New src/data/cooperative-1881-bid-package.js: readCooperative1881RealTakeoff column-sums the Building (1) Knowify SOV cost block (N=material 188,777.67, O=labor 152,694.67 [RoughIn O31 + Trim O32], P=sub 19,432, Q=equip 25,525.50, R=misc 18,677.19; cost.total 405,107.03 == R38 "Knowify Check" exact-match) + totalPrice G11 538,792.35 (== Proposal K10/K39) + heads B9 1420 + sqft G6 170,654 + markup I11 0.33 + sourceRefs (N28:R33, R38, G11, B9, G6, I11, all #Building (1)!) + honest disclaimer; throws if workbook absent. src/data/floorplans.js + src/api/server.js: built-in Cooperative 1881 project (COOPERATIVE_1881_PROJECT_NAME) on a floor plan of REAL area 170,654 sqft (placeholder rectangular footprint 413×413.21, documented — no DXF, PDF plans only), ordinary residential hazard (NOT esfr); fail-soft calibration enrichment (realTakeoff + byCategory + deltaUsd/deltaPct vs the real 538,792.35; absent workbook omits, never 500); Home Depot + generic projects untouched. Proposal workbook copied into repo root UNTRACKED (Home Depot convention; tests skip-if-absent). TDD tests/cooperative-1881-takeoff.test.js (6) + full-scope-bid-api.test.js (+1). Full suite 48 files/457 tests green (was 47/450). Agentic-rules ok. MEASURED (untuned, real inputs; model heads 1332 UNDERSHOOT real 1420 → no fit-to-target): auto-estimate fullScopeTotal $407,377.23 vs real $538,792.35 = −24.39% (deltaUsd −$131,415.12); materialsOnly $93,368.07 vs real material $188,777.67 (−50.5%); labor $203,543.10 vs real labor $152,694.67 (+33%). HONESTY/d4 DECISION: the honesty verifier returned d4ShouldBeChecked=true (total within ±25%), BUT I OVERRODE to KEEP d4 OPEN — per the verifier's OWN flagged issues, the −24% total is a NETTING ARTIFACT of two offsetting category errors (materials −50%, labor +33%) that cancel, NOT genuine category-level agreement, and the labor model did NOT match this project (contrast Home Depot's +$199). Marking d4 "done" on offsetting errors would overclaim. The dominant residual (materials, placeholder footprint) is exactly what real geometry fixes. Added Task Queue T28 (PDF plan ingestion — floorPlanFromPdf via pdfjs-dist on the real VECTOR Bluebeam plans → geometry-true footprint → re-measure category-honest; the user's "must use pdf" requirement, brain ep 16446) as the honest path to close d4. Gates blocked, estimate:true, no parity/accuracy/AHJ/PE claim, no tuning. Next unchecked Task Queue item: T28.
- 2026-05-29: T26 honest d4 decision (option (b) material folds + DECISION) shipped (ultra workflow w5bjtsj0v, TDD; honesty + correctness verifiers BOTH PASS; honesty verifier audited the diff + confirmed NO tuning, NO in-rack over-scoping, NO band widening). Executed option (b): src/engine/sprinkler-layout.js buildEsfrSystemScope now appends two GENUINELY-OMITTED real material lines (ESFR-only, additive) — drop_armover (EA, qty=headCount=1230, one drop/armover per ceiling head, geometry-derived) + seismic_brace (EA, qty=floor(totalMainFt/40)=21 where totalMainFt=cross+feed+bulk, the public NFPA-13 lateral interval imported from supports.js SEISMIC_BRACE_INTERVAL_FT, assumption:true, SUPPORTS_NOTE disclaimer). IN-RACK CORRECTLY EXCLUDED (real ESFR is ceiling-only — over-scoping would be dishonest; test asserts no in-rack line). src/engine/pricebook-pricing.js +2 PRICE_BANDS (drop_armover {nipple/drop/reducer/fitting, 1-40}, seismic_brace {brace/seismic/sway, 5-120}) resolving real medians; labelled fallbacks ($8/$45) when the seeded book has no match (fail-soft, flagged fallback_estimate — NOT tuned). No server.js structural change (lines flow through the existing append + priceBid re-price). TDD esfr-scope (+5) + pricebook-pricing (+3) + full-scope-bid-api (+1). Full suite 47 files/450 tests green (was 47/441; +9). Agentic-rules ok. MEASURED (seeded pricebook, preview /sprinkler-bid, NO image transfers): Home Depot materialsOnly $119,862.79 (was $113,494.78), fullScopeTotal $411,480.58 vs real $792,543.84 = deltaPct −48.08% (deltaUsd −$381,063.26); drop_armover 1230 + seismic_brace 21 confirmed in BOM, hasInRack false, estimate true. PROGRESSION T23→T24→T25→T26 = −72% → −63% → −49.16% → −48.08%, all untuned. DECISION (the task deliverable): option (b) HONESTLY EXHAUSTED — the byCategory breakdown shows LABOR+EQUIPMENT now matches reality essentially exactly (model $180,103.50 vs real $179,904.77, +$199), and the ENTIRE remaining shortfall is MATERIALS (model ~$120–137k vs real $396,098, −$259k). That gap is GEOMETRY FIDELITY (under-modeled large-diameter main-grid footage from the placeholder single-rectangle), NOT closable by more honest pricing/BOM work. The honest remaining paths require the USER: (a) supply a REAL DXF of the actual Home Depot building (engine supports DXF via T21) → the legitimate way to check d4; or (c) accept d4 as a documented internal-alpha limitation. Recommended (a) when a drawing is available; (c) interim. HONESTY held throughout: gates blocked, estimate:true, no accuracy/AHJ/PE/AutoSprink/manufacturer claim, non-ESFR projects byte-for-byte unchanged. ROADMAP: Task Queue T1–T26 ALL CHECKED; the ONLY remaining unchecked item is DoD d4, which is now a documented USER DECISION (a real building DXF, or accept-as-limitation) — NOT an open engineering task. Next: await user input on d4 path (a)/(c); no further auto-task without it (adding more would risk padding/tuning).
- 2026-05-29: T25 ESFR/storage material scope shipped (ultra workflow wrwqi2u9l, TDD; honesty + correctness verifiers BOTH PASS; honesty verifier confirmed NO tuning/reverse-engineering after auditing the git diff). src/engine/sprinkler-layout.js: new STORAGE_RULES.esfr {maxAreaSqFt 100, maxSpacingFt 12, minSpacingFt 8, storage:true} (public NFPA-13 storage ESFR values) merged into getHazardRule (light/ordinary/extra untouched, unknown still throws); buildEsfrSystemScope(layout,piping,opts) returns diameter-aware BOM lines DERIVED from geometry — esfr_head (qty=heads, REPLACES sprinkler_head so heads aren't double-counted), cross_main_pipe (nominalIn 3, qty=piping.crossMainFt), feed_main_pipe (6, qty=round(max(bbox.width,bbox.height)) = one feed main along the long axis), bulk_main_pipe (6, ESFR_SCOPE_ASSUMPTIONS.bulkMainFt 40 — labelled assumption), underground_main (8, undergroundFt 100 — labelled assumption); ESFR FALLBACK_UNIT_COSTS (fallback-flagged when pricebook null). src/engine/pricebook-pricing.js: +5 PRICE_BANDS (esfr_head, cross_main_pipe, feed_main_pipe, bulk_main_pipe, underground_main) that resolve REAL pricebook medians within defensible size-cost windows — confirmed non-null + monotone by size class against the seeded 7,208-row book (esfr_head $42, cross $10.92, feed $21.56, bulk $27.02, underground $30.17). server.js: runSprinklerPipeline detects ESFR (built-in Home Depot OR request systemClass:"esfr") → lays out hazard esfr + appends buildEsfrSystemScope to the BOM before priceBid; non-ESFR projects byte-for-byte unchanged; fail-soft. TDD tests/esfr-scope.test.js (12: esfr rule values, ESFR layout coverage ≤100 sqft/head, scope line derivations from a known layout/piping/bbox, assumption defaults, non-ESFR unaffected) + pricebook-pricing.test.js (+3 ESFR band medians + size monotonicity) + full-scope-bid-api.test.js (+2: Home Depot ESFR lines + materials growth + honest negative delta; generic project has no ESFR lines + gates unchanged). Full suite 47 files/441 tests green (was 46/424; +1 file/+17). Agentic-rules verifier ok. MEASURED authoritatively (seeded pricebook real medians, via preview :3201 /sprinkler-bid, NO image transfers): Home Depot now systemClass esfr, 1230 ESFR heads (100 sqft/head over 121,500 sqft), materialsOnly $113,494.78 (up from ~$66k), laborCost $180,103.50, fullScopeTotal $402,927.70 vs real $792,543.84 = calibration deltaPct −49.16% (deltaUsd −$389,616.14); realTakeoff material $396,097.57 intact; ESFR BOM lines confirmed (esfr_head 1230, cross 405ft/3", feed 405ft/6", bulk 40ft/6", underground 100ft/8"); estimate:true. PROGRESSION T23→T24→T25 = −72% → −63% → −49%, all untuned. HONESTY: ESFR rules public NFPA-13; prices real pricebook medians (or labelled fallbacks); feed-main footage geometry-derived; bulk/underground labelled documented assumptions; nothing tuned to hit $396k (a back-fit lands ~0%); no gate flips, AUTOSPRINK_PARITY stays blocked, no accuracy/AHJ/PE/AutoSprink/manufacturer claim. DoD d4 DECISION: honesty verifier ruled d4ShouldBeChecked=FALSE — the auto-estimate improved but is still materially short (materials −71%, total −49%, well beyond ~±25% "approaching"). d4 stays OPEN; I abided. Added Task Queue T26 (decide the honest path to d4: import the real building DXF via T21, OR fold remaining geometry/NFPA-derivable storage scope, OR accept d4 as a documented internal-alpha limitation — never pad/tune). Transparency: the full-scope-bid-api test prices off FALLBACK_UNIT_COSTS because the test-harness pricebook is empty; the seeded production path (measured above) uses real medians. Next unchecked Task Queue item: T26.
- 2026-05-29: T24 real-takeoff bid calibration shipped (ultra workflow wv4y26ruh, TDD; honesty + correctness verifiers BOTH PASS). src/data/home-depot-bid-package.js: new readHomeDepotRealTakeoff(rootDir) parses the REAL ESI takeoff from the proposal workbook "Building 1" Knowify SOV block (source cells cited, throws clean if workbook absent) → cost {material $396,097.57=N31, labor $143,473.33=O32, equipment $36,431.44=Q32, subcontractor $5,000=P30, miscellaneous $2,500=R30, total $583,502.34} + withMarkup {materials $514,926.85=U31, labor+equip $233,876.19=U32, design $9,750=U30, sovTotal $758,553.04=U34} + totalPrice $767,543.84=G11 + bidLogTotal $792,543.84 + markupPct 0.30 (=round(U31/N31−1)) + sourceRefs citing all 10 cells + honest disclaimer. server.js: Home-Depot-only try/catch enriches fullScopeBid.calibration with realTakeoff + byCategory (model materialsOnly/laborCost/components/soft+ohp vs real material/labor+equip/design+sub, each with delta, informational); fail-soft (absent/parse-fail → omit realTakeoff, never 500; generic projects get none). autosprink.html: bid panel renders a "Real ESI takeoff (parsed from the proposal — source cells cited)" block beside the auto-estimate with the honest "actual submitted breakdown, NOT an auto-bidder result, no accuracy/AHJ/PE/parity claim" note. TDD tests/home-depot-real-takeoff.test.js (3, skip-if-absent: asserts the parsed cell values + sourceRefs + no-parity disclaimer) + full-scope-bid-api.test.js (+2, skip-if-absent: Home Depot calibration includes realTakeoff with real numbers + sourceRefs + gates still blocked; generic project has none). Full suite 46 files/424 tests green (was 45/419). Agentic-rules verifier ok. KEY HONEST FINDING (preserved in byCategory): the T23 model labor $144,112 ≈ the real labor $143,473 (within ~0.4%) — the −63% gap is almost ENTIRELY MATERIALS ($66k model basic-spray vs $396k real ESFR warehouse system). DoD d4 DECISION: the honesty verifier explicitly ruled d4ShouldBeChecked=FALSE — T24 added no new PRICING capability; the auto-bidder's own output is still −63%; re-parsing the real total from the workbook is a calibration/evidence milestone, NOT the "priced-scope-approaching" milestone d4 names (checking it would let a reader believe the auto-bidder now approaches real bids). d4 stays OPEN; I abided by the verdict. Added T25 (model the ESFR/storage material scope on OpenGeometry, priced from real pricebooks, calibrated to the T24 realTakeoff without inflating unit prices) as the real path to close d4. HONESTY: real takeoff is parsed real data not a model achievement, /api/parity gate stays blocked + parityAchieved not true, /submittal submittalReady false, no accuracy/AHJ/PE/AutoSprink/manufacturer claim, AUTOSPRINK_PARITY fail-closed. Next unchecked Task Queue item: T25.
- 2026-05-29: T23 bid-fidelity estimating model (field labor + OH&P) shipped (ultra workflow wzf4fjx1l, TDD; honesty + correctness verifiers BOTH PASS). src/engine/bid-scope.js: LABOR_ASSUMPTIONS {hoursPerHead 0.8, hoursPerPipeFt 0.05, hoursPerFitting 0.3, laborRatePerHour 90} (documented public fire-sprinkler conventions, explicitly NOT reverse-engineered to hit the target) + buildLaborScope({totalHeadCount,pipeFootage,fittingCount},opts) → 3 HR lines (head_install/pipe_handling/fitting_install, hours×rate, priceSource labor_assumption, opts-overridable, zero-driver→0 no throw, {lines,laborHours,laborCost}); OHP_ASSUMPTIONS {overhead 0.10, profit 0.10} + buildOverheadProfit(costSubtotal) → overhead on cost then profit on cost+overhead (ohp_assumption). buildFullScopeBid extended to base the full scope on pricedBid.materialCost (un-marked-up) NOT pricedBid.total — so it does NOT double-count priceBid's crude $85/head allowance + 25% markup; softBase = materialsOnly+components+labor; costSubtotal = materialsOnly+components+labor+soft; fullScopeTotal = costSubtotal+OH&P; bareMaterialsTotal (=pricedBid.total) preserved + new fields materialsOnly/laborScope/laborCost/ohp added; disclaimer updated to flag labor+OH&P as documented assumptions. server.js runSprinklerPipeline derives pipeFootage (BOM branch_pipe FT qty) + fittingCount (BOM fitting qty) and passes them into buildFullScopeBid (still inside the fail-closed try/catch). autosprink.html bid panel now shows Field labor (est) + Overhead & Profit (est) lines with the assumption-based caption. TDD tests/bid-scope.test.js +7 (18 total: buildLaborScope hours/cost/override/zero-driver, buildOverheadProfit order/math, full-scope now includes laborScope+ohp and fullScopeTotal>bare on the materialsOnly basis, labor/ohp lines flagged); full-scope-bid-api + pricebook-pricing still green. Full suite 45 files/419 tests green (was 45/412). Agentic-rules verifier ok. MEASURED (honest, both verifiers independently reproduced via the seeded-DB pipeline): NEW Home Depot full-scope $291,379.18 (materialsOnly $66,432.75 + components $6,400 + labor $144,112.50/1601.25hr + soft $23,863.99 + OH&P $50,569.94) vs real $792,543.84 = −63.23% (deltaUsd −$501,164.66) — up from −71.65%/$224,683 but STILL ~63% short. HONESTY: constants are documented conventions not tuned (a back-fit would land ~0%); no double-count (basis materialsOnly not the marked-up total); every line flagged estimate/labor_assumption/ohp_assumption; calibration informational; nothing flips a gate; AUTOSPRINK_PARITY stays blocked. DoD d4 stays UNCHECKED — −63% is a real improvement but not "approaching". Added T24 (real-takeoff calibration: parse the actual ESI bid-line rows from the Home Depot proposal workbook + add subcontract/site scope; only then can d4 honestly close — never by tuning constants). Next unchecked Task Queue item: T24.
- 2026-05-29: DoD re-assessment (ultra workflow wft17pnu2 — independent d1 + d4 assessors + an adversarial honesty meta-auditor; no code change, roadmap only). d1 CHECKED, d4 stays unchecked — honest split. d1 ("Floor plan SVG/DXF → building shell + full sprinkler system in 3D via OpenGeometry"): MET (assessor met=true high-confidence; meta-auditor independently re-ran 35/35 targeted + confirmed). Both prior gaps closed — DXF wired end-to-end (T21; tests/dxf-import-api.test.js green) and the live viewer renders REAL STL part meshes (R3/R6/V3: heads via headPartGeo, components via partGeoCache, honest wireframe only for the 12 unsourced/34) on the OpenGeometry v2.0.10 kernel. EVIDENCE: full suite 412/45 green; LIVE browser (preview :3201, builtin Home Depot, admin/halofire-alpha-verify, NO image transfers) status "rendered 936 heads · 1010 pipe segments · heads: generated CAD part (best-effort, not mfg-exact) · 148 components (5 part types as meshes, 2 primitive fallback)", __headDebug {usingPartMesh:true, source:'generated', vertsPerHead:1716, instances:936}, __componentDebug {total:148, partKeys:5, primitiveKeys:2}, cadModel solids wall 4/slab 2/pipe 1010/head 936/component 148. d4 ("Full bid scope priced from real pricebooks (approaching real bid totals)"): PART 1 done (T22 wires buildFullScopeBid from real ARGCO/Victaulic/FFF rows), but PART 2 "approaching real totals" NOT met — LIVE full-scope $224,683.21 vs real $792,543.84 = −71.65% (−$567,860.63); a ~72%-below estimate is not "approaching", so the box stays UNCHECKED (the d4 assessor agent failed to emit a verdict, but I measured the delta directly via the live /sprinkler-bid calibration and the honesty meta-auditor independently set d4=false on the same rule — no rubber-stamp). Added Task Queue T23 (bid-fidelity estimating model: field labor + OH&P + subcontract/site scope, ideally calibrated to the real ESI bid-line takeoff) as the honest path to close d4. Agentic-rules verifier ok. HONESTY held — d1 checked only on real passing evidence; d4 left open rather than overclaim "approaching"; AUTOSPRINK_PARITY stays fail-closed blocked; no parity/AHJ/PE/manufacturer claim. Roadmap is NOT complete — T23 + DoD d4 remain. Next unchecked Task Queue item: T23.
- 2026-05-29: S5 VPS↔OpenClaw wiring + schedule shipped + LIVE end-to-end on the VPS (ultra workflow wxi6y6f6y, TDD; honesty + correctness verifiers BOTH PASS). CODE (committed 26bb290): new src/components/auto-source-runner.js (pure/injectable) — makeBridgeInvoker({bridgeUrl,fetchImpl,timeoutMs}) POSTs {tool,args} to OPENCLAW_BRIDGE_URL+'/codex-bridge/invoke' with an AbortController timeout and THROWS on non-2xx/network-error (so the S1/S2 sourcers catch→null and fail-soft; NEVER fabricates a model/url/license); probeBridge GETs /status → {reachable, openclaw} fail-soft; buildAutoSourceStatus is PURE + FAIL-CLOSED (FORCES parityGateStatus 'blocked' + manufacturerExactCount 0 regardless of runResult). New scripts/auto-source-run.mjs wires global fetch + a spawnSync OpenSCAD scadRunner (OPENSCAD_BIN) + the catalog/sam/gen3d sourcers + runAutoSource({sourcers:{catalog,sam},scadRunner,outDir:'parts',write:true,recorder}), writes out/auto-source-status.json, ALWAYS exits 0 (degrades to OpenSCAD/missing when the bridge errors). New GET /api/auto-source/status (auth, read-only) surfaces the status file, re-forcing blocked/0 (defends a tampered file), never 500s; npm run auto-source; out/ gitignored. TDD tests/auto-source-runner.test.js (11) + tests/auto-source-status-api.test.js (3): status builder + endpoint re-force blocked/0 from a tampered 'clear'/9 input; invoker throws on 500 + on a throwing fetch + returns the object on 2xx; probeBridge online/unreachable; integration — sourcers over an always-500 bridge with no scadRunner → all missing, blocked, no throw. Full suite 45 files/412 tests green (was 43/398; +2 files/+14 tests). LIVE on the VPS (root@187.124.234.28, /opt/openclaw/halofire-autosprink): git pull→26bb290; the governed bridge is OPENCLAW_BRIDGE_URL=http://127.0.0.1:15000 (an SSH tunnel to GX10) — /status reports openclaw 'online' but POST /codex-bridge/invoke currently 500s (the tunnel's GX10 upstream refuses), the exact degraded case the loop handles. `node scripts/auto-source-run.mjs` (sourcing .env): exit 0, out/auto-source-status.json = {openclawReachable:true, openclawStatus:'online', invokeAttempted:true, report:{found 0, created 0, generated 22, missing 12}, manufacturerExactCount 0, parityGateStatus 'blocked', durationMs ~28s} — i.e. it reached the bridge, attempted OpenClaw sourcing, the tools errored, and it fail-soft-degraded to OpenSCAD (22 generated). systemctl restart halofire.service; GET /api/auto-source/status (admin auth) returns that status (generated 22/missing 12, blocked/0); no-auth → 401. Scheduled: VPS crontab '0 */6 * * *' runs the loop (bash -lc sourcing .env) → /var/log/halofire-autosource.log, fail-soft. No image transfers (verified via SSH/HTTP). HONESTY/fail-closed (independently verified): manufacturerExactCount 0 + parityGateStatus 'blocked' forced in BOTH the status object AND the endpoint regardless of input; bridge failure throws/degrades, never fabricates; disclaimer + note on every status; no AutoSprink/AHJ/PE/manufacturer claim. AUTOSPRINK_PARITY stays fail-closed BLOCKED. ROADMAP: Task Queue T1-T22 ✅, REAL-PART RENDER R1-R6 ✅, AUTO-SOURCE S1-S5 ✅, VISUAL FIDELITY V0-V4 ✅. Only the two Definition-of-Done success-criteria boxes (d1 SVG/DXF→3D, d4 full-scope bid) remain to re-assess against the now-shipped T21/T22/R6 work.
- 2026-05-29: R5 VPS parts render + serve + redeploy shipped + LIVE-verified (ultra workflow wvr384oms — 2 independent adversarial VPS re-probes, honesty + correctness BOTH PASS). Also closes R4 (its API half was the R4-API commit; its browser half is V4 — both done, R4 box checked). DEPLOY (direct, sequential — deploy is stateful infra, not a fan-out): pushed 27 local commits 8039133..dbd8be6 to origin github.com/dallasteele/halofire-autosprink (the documented VPS deploy mechanism = git pull); on the VPS (root@187.124.234.28, /opt/openclaw/halofire-autosprink, Ubuntu 22.04, Node v22, OpenSCAD 2021.01, served PORT 3301 behind nginx, systemd halofire.service): git pull --ff-only → HEAD dbd8be6 (was 8039133/T19, 27 behind); npm install up-to-date (three/opengeometry already present); OPENSCAD_BIN=openscad npm run build:parts → rendered 22 real STLs (generated=22 missing=12 manufacturerExact=0 parityGate=blocked openscadInstalled=true); systemctl restart halofire.service → active, /api/health 200. VERIFIED OVER HTTP (mine + 2 independent verifier re-probes): /parts/head_pendent.stl serves 200, 92,891 bytes, content-type application/vnd.ms-pki.stl, body magic "solid OpenSCAD_Model" (genuine OpenSCAD ASCII STL, not placeholder/0-byte/HTML); live admin /api/parts → 34 components, 22 present, head_pendent {source:'generated', present:true, file:'parts/head_pendent.stl', format:'stl', manufacturerExact:false}; the studio's loadPartMeshes() path (GET /api/parts → head_pendent present+file → STLLoader.loadAsync('/parts/head_pendent.stl')) is therefore satisfied over the web (identical code was browser-verified locally via __headDebug usingPartMesh:true; full in-browser check against the public URL not run — preview points local). HONESTY (independently confirmed on the VPS): live /api/parts disclaimer "Best-effort parametric massing; NOT manufacturer-exact; no AutoSprink/AutoCAD/AHJ/PE approval", manufacturerExactCount 0, parityGateStatus 'blocked', distinct sources exactly ['generated','missing'] (no 'catalog'/'manufacturer'), studio header "INTERNAL ALPHA · NOT AHJ/PE/AutoSprink parity" + user-visible "heads: generated CAD part (best-effort, not mfg-exact)" — render of generated meshes did NOT flip the gate. No code change this task (deploy + verify only; suite remains 43 files/398 tests green from V4). Next: S5 — VPS↔OpenClaw wiring + schedule the S3 auto-source loop (OpenClaw was HTTP_UNREACHABLE on direct probe earlier; S5 is the live-on-VPS validation + cron), then DoD d1/d4 success-criteria boxes.
- 2026-05-29: V4 Settings "attach catalog part" UI shipped — completes R4's browser half (ultra workflow w0vztrimh; adversarial honesty + correctness verifiers both PASS; LIVE end-to-end verified via the preview window). settings.html (only file touched; additive over the already-tested POST/DELETE /api/parts/:key/override endpoint — R4): new "Attach catalog part (admin)" card under the Component Library panel with 12 fields (attKey + attKeyList datalist of all components, attMode link|upload select, attRef with attRefLabel that swaps "Catalog URL"/"Filename" + placeholder on mode change, attFormat + attFormatList datalist stl/glb/gltf/obj/step/dwg, attManufacturer, attLicense, attNotes, attSubmit, attMsg). Re-wired loadLibrary's [data-import] pill click: clicking any part now prefills attKey + focuses + scrolls the attach form into view (replacing S4's jump-to-doc-importer). Submit builds a body with ONLY allowed fields (mode + url|filename by mode + optional format/manufacturer/license/notes, empties omitted — never a stray field that would 400), api()-POSTs to /parts/<key>/override, on success echoes the server's source + manufacturerExact + "parity gate stays blocked" and calls loadLibrary() to refresh the pills (web-mesh attach flips the pill to catalog/present; non-mesh stays needs-import with evidence recorded), errors (400/404) caught + shown without throwing. LIVE preview verification (admin login halofire-alpha-verify on the running server :3201, no image transfers): web-mesh attach (fitting_cap, glb + manufacturer Tyco + license) → POST 200 source 'catalog', present true, manufacturerExact true, file=the glb URL (demo renders the ACTUAL component); non-mesh attach (fitting_cross, step, manufacturer only) → POST 200 source 'catalog' but present FALSE + file null (evidence only, NOT rendered) + manufacturerExact FALSE (no license); parityGateStatus stayed 'blocked' even with manufacturer+license attested; DELETE reverted (removed 1 → source 'missing', present false). Static fetch of /settings.html confirmed all 12 field IDs present + honesty copy (gate-stays-blocked, web-mesh-vs-evidence-only, manufacturer-exact-needs-both) + correct POST path. Full suite 43 files/398 tests green (UNCHANGED — static HTML surface over the tested endpoint, matching prior HTML-surface verification practice). HONESTY/fail-closed: attaching ONE catalog part never clears the AUTOSPRINK parity gate (server hardcodes parityGateStatus 'blocked' in both /api/parts branches), no parity/AHJ/PE/permit/fabrication claim anywhere, manufacturer-exact is per-part and ONLY with manufacturer+license — all upheld in copy AND proven live. Next: R5/S5 (VPS — npm run build:parts real OpenSCAD render + serve parts/ + cron auto-source loop + live OpenClaw run; OpenClaw currently HTTP_UNREACHABLE so live sourcing validates on the VPS), then DoD d1/d4 success-criteria boxes can be revisited.
- 2026-05-29: V3 CAD-correct connectors shipped (engine; ultra workflow wkt7bmx11, TDD, 3 adversarial verifiers honesty/correctness/test-truth all PASS). src/engine/cad-model.js: pushComponentMarkers extended signature pushComponentMarkers(solids, prefix, mainX, y0, mainZ, branchZ, mainDia, branchLines) — now emits, in addition to R6's riser_assembly/valve_alarm_check/fdc/fitting_tee, three NEW connector marker kinds derived from REAL network geometry: fitting_elbow_90 at each branch far end [round(b.endX), b.y, branchZ] PLUS the riser-top [mainX, y0, mainZ]; fitting_coupling at each branch midpoint [round((b.startX+b.endX)/2), b.y, branchZ]; fitting_reducer ONLY when b.diameterIn < mainDia (a real size step-down) at [mainX, b.y, branchZ]. All via place()→getComponent(key) fail-closed (unknown keys skipped, never fabricated); offsetSolidZ already has case 'component'; tallyCounts/tallyBuildingCounts already filter kind==='component' so counts.components auto-includes the new markers. STUDIO: NO autosprink.html change needed — R6's generic addComponentParts already instances ANY component key's part mesh (or marked wireframe primitive), pendant heads already render as the head_pendent mesh hanging below the branch (per the user's screenshot), and pipe radii already use s.diameterIn/12/2 (schedule-reflective) — V3 is purely an emission change that flows through the existing generic render path. TDD tests/cad-model-components.test.js extended +6 (17 total): elbow at branch ends + riser top, coupling at midpoints, reducer ONLY on size step-down (none when uniform), all positions in-bbox + derived from network, counts.components includes connectors, building-path emission, back-compat counts unchanged. VERIFIED: full suite 43 files/398 tests green (was 42/392); verify-internal-alpha.ps1 PASSED (AGENTIC_RULES_VERIFY ok); preview restarted (server.js imports cad-model.js) and API on the Home Depot project returns totalComponents 148 byKey {fitting_coupling:36, fitting_elbow_90:37, fitting_reducer:36, fitting_tee:36, riser_assembly:1, valve_alarm_check:1, fdc:1} — connectors render via the R6 generic path. Image-to-chat transfer is broken this session (per user: "stop trying to show images") — visual confirmed via the live preview window + deterministic DOM/API checks, not image files. HONESTY/fail-closed: connectors are placement markers keyed to registry parts, positions derived from real branch/main geometry (never fabricated/random), NOT manufacturer-exact, flip NO gate; cad-model disclaimer intact; AUTOSPRINK_PARITY stays fail-closed BLOCKED. Next: V4 (Settings "attach catalog part" button → POST /api/parts/:key/override, completes R4 browser half), then R5/S5 (VPS build:parts render + serve parts/ + cron auto-source loop + live OpenClaw run — OpenClaw HTTP_UNREACHABLE).
- 2026-05-29: V2 demo mode (structure + sprinklers rising out of the plan) shipped. New src/engine/demo-timeline.js: easeInOut(t) clamped quadratic + demoTimeline(elapsedMs,{wallsMs=1200,systemMs=1200,gapMs=150}) → {wallsT,systemT,cameraT,done,totalMs} where the structure rises first (wallsT) and the sprinkler system rises only after wallsMs+gapMs (systemT), camera eases over the whole run. autosprink.html: import it; "▶ Demo" header button → playDemo() switches to iso 3D, flattens layer groups (scale.y≈0), and in a rAF loop scales layerGroups.{WALLS,COLUMN} (structure) then {MAIN,BRANCH,DROPS,HEADS,COMPONENTS} (system) by wallsT/systemT — scaling a group's Y about world y=0 (groups sit at origin) grows its geometry UP from the floor (FLOOR group left at scale 1 as the ground); status narrates "structure rising…"/"system rising…"/"complete"; resets to scale 1 on done; viewSeg buttons cancel a running demo. Added window.__applyDemoFrame(ms) — deterministic frame applier (no rAF) for verification + rAF-throttle resilience. TDD tests/demo-timeline.test.js (7: easeInOut clamp/endpoints/monotonic; timeline t=0 zero+not-done, structure-before-system, walls-finish-before-system-starts, completes-at-totalMs, default ~2.55s). VERIFIED: deterministic __applyDemoFrame on a live 2-space DXF against the REAL geometry groups returned t=0 {wallsT:0,systemT:0,done:false} → t=1300 {wallsT:1,systemT:0} (structure up, system still flat) → t=2250 {wallsT:1,systemT:0.5} → t=3000 {wallsT:1,systemT:1,done:true}; live playDemo confirmed starting (status "demo: structure rising out of the plan…") and the canvas rendered the rising frames (capture produced valid JPEGs). NOTE: the headless preview throttles requestAnimationFrame so the rAF auto-advance stalls in background — verified the rise via the deterministic hook instead; it animates normally in a focused browser (preview window). The image-to-file transfer pipeline was flaky this run (per user: rely on the preview window) — visual confirmed live in-preview + via the deterministic group-scale checks. Full suite 43 files/392 tests green (was 42/385). Agentic-rules ok. HONESTY: geometry on the OpenGeometry kernel; pure-math timeline; no AutoSprink/AutoCAD/AHJ/PE/manufacturer parity claimed; gates fail-closed. Next: V3 (CAD-correct connectors — cad-model emits elbow/coupling/reducer + pendant-down heads + schedule pipe radii; studio renders them), then V4 (Settings attach-catalog-part button → completes R4 browser half), R5/S5 (VPS + OpenClaw, HTTP_UNREACHABLE).
- 2026-05-29: V1 plan-mode readable floor-plan drawing shipped + CAPTURE-VERIFIED (actually viewed the render). New src/engine/plan-view.js: planOrthoFrustum({minX,maxX,minY,maxY}, aspect, margin=0.08) returns centered frustum half-extents (left=-right, bottom=-top) sized so the WHOLE plan fits the viewport with margin and (right-left)/(top-bottom)==aspect (width-limited vs height-limited branch; degenerate-bbox safe); planCenter. autosprink.html: import it; fitPlanOrtho(minX,maxX,minZ,maxZ,aspect) sets the orthoCam frustum from the plan bbox (cad planX→world x, planY→world z); frameCamera + setView('top') + onResize all re-fit + set orthoCam.up=(0,0,-1) north-up; new setPlanStyle(isPlan) lightens the scene backdrop (0x0e1320) + floor (0x232a38, defensive traverse) for a drawing look, restores dark for 3D; window._frame stores the bbox. TDD tests/plan-view.test.js (7 tests, red→green before wiring). CAPTURE: preview_screenshot still hangs on the rAF canvas; used window.__snapshot at high res (1600x1080 q0.96 ≈ 64KB) → eval result auto-saved to a tool-results file (complete, vs lossy inline copy) → python base64-decoded → Read the .jpg. VIEWED: a 2-space building DXF renders as a readable plan — two outlined rooms + interior wall, gray column markers per space, sprinkler network overlaid (red cross-main/riser, blue branch lines, red heads), amber riser components, north-up, framed with 8% margin (vs the prior bare-rectangle-of-blue-lines). Full suite 42 files/385 tests green (was 41/378). Agentic-rules ok. HONESTY: geometry stays on the OpenGeometry kernel; plan-view helper is pure math; no AutoSprink/AutoCAD/AHJ/PE/manufacturer parity claimed; gates fail-closed. Built-in Home Depot remains a simplified rectangle (data limitation) — real structure shows on DXF/SVG imports; enriching the built-in demo will ride with V2. Next: V2 (demo mode — structure + sprinklers rising out of the plan).
- 2026-05-29: V0 capturable studio + actual visual verification of the render. autosprink.html: WebGLRenderer preserveDrawingBuffer:true; window.__snapshot(mw=760,mh=500,q=0.6) sets a capped renderer size, renders, returns a compact JPEG data URL, then restores via onResize(). Reconfirmed preview_screenshot TIMES OUT on the WebGL rAF canvas (30s) — the snapshot hook is the workaround: eval window.__snapshot() at a small size (440x300 q0.4 ≈ 50KB) → base64 saved to a tool-results file (too big for direct context) → decode via python base64 → Read the .jpg. CAPTURED + VIEWED the Home Depot iso render: gray floor plane + blue branch lines + red cross-main/riser edge; heads/components render (R3/R6) but are specks at building-overview zoom; NO roof slab exists in the studio (the studio has no slab/roof branch — the gray is the floor Cuboid). This corrected the earlier assumption and drove the VISUAL FIDELITY epic (V1 plan drawing, V2 demo rise, V3 connectors, V4 catalog-attach UI). Honesty: generated parts are best-effort shapes not exact products; parity gate stays blocked. No suite impact (capture hook is render-only). Next: V1 plan-mode drawing.
- 2026-05-29: R4 catalog part-override API shipped (workflow wftz28l1s, TDD, honesty/correctness/test-truth PASS) — API half of R4 (Settings UI button pending, folding into the visual-fidelity work). New part_overrides table (key/mode/ref/format/manufacturer/license/notes/evidence_id/created_by) in initDatabase. POST /api/parts/:key/override (admin, mirrors settings/documents): validates key via getComponent (404), mode link|upload + required ref (400), transaction inserts a PRESENT catalog_part project_evidence row + the override row. DELETE /api/parts/:key/override (admin) reverts. GET /api/parts merges overrides via mergePartOverrides: an overridden key → source 'catalog', manufacturer/license/provenance carried, file=ref + present=true ONLY when format is a web mesh (stl|glb|gltf|obj) else file=null/present=false (STEP/DWG recorded as evidence, never fabricated into a mesh), manufacturerExact=true ONLY when BOTH manufacturer+license attested; re-run through sanitizePartEntry; counts recomputed via recountParts. HONESTY (critical, tested): parityGateStatus stays hardcoded 'blocked' in BOTH /api/parts branches AFTER overrides — a catalog upload supplies one component + evidence, never manufacturer-exact for every required component + PE/AHJ, so AUTOSPRINK_PARITY can never clear from an upload; /api/parity unaffected (empty inventory). This is the ONLY path to source 'catalog'/manufacturerExact:true (user-attested). Fixed a stale comment that claimed manufacturerExactCount is always 0. tests/part-override-api.test.js 12 spawned-server tests (PORT 3200): non-admin 403, unknown key 404, bad field/mode/missing-ref 400, web-mesh override → catalog+manufacturerExact+file+count≥1, STEP/DWG → catalog but file null/not present, no-license → manufacturerExact false, parityGateStatus blocked after overrides, DELETE reverts. Full suite 41 files/378 tests green (was 40/366). Agentic-rules ok. REMAINING for R4: the Settings "attach catalog part" button (browser) → folding into the VISUAL FIDELITY epic.
- 2026-05-29: R6 component placements + studio multi-part render shipped — the 3D builder now renders discrete CAD parts for valves/tees/riser/FDC (not just heads). ENGINE (workflow wx8rn0b0g, TDD, honesty/correctness/test-truth all PASS): src/engine/cad-model.js buildRoomCad + buildStoryCad emit kind:'component' solids {kind,name,layer:'COMPONENTS',componentKey,category,position:[x,y,z]} via a shared pushComponentMarkers(solids,prefix,mainX,y0,mainZ,branchLines) — riser_assembly=[mainX,y0,mainZ], valve_alarm_check=[mainX,y0,mainZ-1], fdc=[mainX,y0,0], fitting_tee=[mainX,b.y,mainZ] per branch junction; fail-closed via getComponent(key) (unknown keys skipped); offsetSolidZ gained case 'component'; tallyCounts + tallyBuildingCounts gained components count. UI (autosprink.html): replaced the single headPartGeo loader with partsManifest + partGeoCache + loadPartGeo(key,targetFt) (normalizePartGeo: center, OpenSCAD Z-up→three Y-up, scale to targetFt); addComponentParts groups component solids per key and InstancedMeshes each key's part mesh (metalness 0.55) OR a MARKED wireframe BoxGeometry fallback when the part is missing; COLORS.COMPONENTS amber + a COMPONENTS layer group; generate() preloads the model's distinct component keys (await loadPartGeo at 2.5ft) before renderModel; window.__componentDebug for verification; status appends "N components (P part types as meshes, K primitive fallback)". Browser-verified (preview restarted to load new cad-model; preview_eval/DOM, Home Depot): API emits 39 component solids (counts.components=39) across riser_assembly/valve_alarm_check/fdc/fitting_tee; __componentDebug = {total:39, byKey:{fitting_tee:{count:36,usingPartMesh:true}, valve_alarm_check:{count:1,usingPartMesh:true}, fdc:{count:1,usingPartMesh:false}, riser_assembly:{count:1,usingPartMesh:false}}, partKeys:2, primitiveKeys:2}; heads still part-mesh (936, 1716 verts). Honest split: tee+valve are OpenSCAD-generatable (real mesh), fdc+riser_assembly are non-generatable/missing (marked wireframe fallback, also in S4 needs-import). TDD tests/cad-model-components.test.js (11 tests: component keys present, in-bbox derived positions, counts.components, building-path emission, Z-lift, back-compat counts unchanged). Full suite 40 files/366 tests green (was 39/355). Agentic-rules ok. HONESTY: placement markers keyed to registry parts, positions derived from real network geometry (never fabricated/random), NOT manufacturer-exact, flip NO gate; cad-model disclaimer intact. (Screenshot skipped — WebGL canvas hangs preview_screenshot this session; DOM/__componentDebug is the substitute.) Remaining epics: R4 (per-component upload override), R5/S5 (VPS render+serve+cron + OpenClaw live run — OpenClaw HTTP_UNREACHABLE).
- 2026-05-29: T22 full-scope bid wired + Home Depot calibration (DoD d4 gap closed; ultra workflow wzdazh7pv, TDD, 3 adversarial verifiers honesty/correctness/test-truth all PASS). src/api/server.js: import buildFullScopeBid; HOME_DEPOT_BID_LOG_TOTAL=792543.84 (documented real ESI bid-log total, informational-only). runSprinklerPipeline computes fullScopeBid=buildFullScopeBid(bid.pricing, {priceResolver:opts.priceResolver (the DB resolver), totalHeadCount, hazard, requiredPressure:(hydraulics&&!error?requiredPressurePsi:undefined)}) in try/catch→{error}; availablePressure left undefined so no fire pump is fabricated (fail-closed). Added to the bundle + /sprinkler-bid res.json (additive; all prior fields unchanged). For projectName===HOME_DEPOT_PROJECT_NAME attaches fullScopeBid.calibration={referenceTotal:792543.84, fullScopeTotal, deltaUsd, deltaPct, note} (note: informational comparison only, not an accuracy/parity claim, clears no gate); other projects no calibration. /submittal does NOT pass fullScopeBid into buildSubmittal — submittalReady stays false. autosprink.html bid panel: destructure fullScopeBid, render System components + Soft costs + Full-scope estimate beneath bare-materials, with best-effort/NOT-a-quoted-bid caption + the calibration delta (colored gold within ±25% else red). Browser-verified (preview restarted; preview_eval/DOM, Home Depot built-in): bare-materials $196,017 → system $6,400 + soft $22,266 → full-scope $224,683; calibration "vs real bid-log $792,544: −71.65% — informational comparison only — not an accuracy or parity claim". TDD tests/full-scope-bid-api.test.js (PORT 3199, 3 tests): generic project honesty-flagged fullScopeBid (systemComponentLines fallback_estimate, softCostLines soft_cost_assumption, estimate:true, disclaimer, fullScopeTotal>=bareMaterialsTotal, no parity/approved/submittalReady truthy, no calibration), Home Depot calibration.referenceTotal===792543.84 + numeric deltaPct + not-accuracy note, /submittal submittalReady false. Full suite 39 files/355 tests green (was 38/352). Agentic-rules verifier ok. HONESTY held — best-effort estimate, all priceSource flags + disclaimer intact, calibration informational only, nothing flips AUTOSPRINK_PARITY/AHJ/PE/submittalReady. Next: Task Queue now CLEAR; remaining epics R6 (cad-model fitting/valve/riser/FDC placements → render as parts), R4 (per-component upload override), R5/S5 (VPS render+serve+cron + OpenClaw live run — OpenClaw HTTP_UNREACHABLE).
- 2026-05-29: T21 DXF import wired end-to-end (DoD d1 gap closed for DXF). src/api/server.js: import floorPlanFromDxf + buildingFromDxf; runSprinklerPipeline now resolves `buildingDxf` (buildingFromDxf with layers=req.body.dxfLayers||DEFAULT_DXF_LAYERS, unitsPerDrawingUnit) and `dxf` (floorPlanFromDxf with unitsPerDrawingUnit/dxfLayer/hazard) parallel to buildingSvg/svg; refactored so the building→flat-floorPlan synthesis + empty-check is shared by both SVG and DXF building inputs (added DEFAULT_DXF_LAYERS const: ROOMS/SPACES/A-AREA/PLAN, WALLS-EXT/A-WALL-EXT/WALLS, WALLS-INT/A-WALL-INT/PARTITIONS, DOOR/DOORS/A-DOOR, COLUMN/COLUMNS/COLS/A-COLS). autosprink.html: planSource +2 options (dxf single, building-dxf multi); currentBody sends dxf/buildingDxf + unitsPerDrawingUnit; planSource.onchange shows the box + swaps label/placeholder to DXF guidance; generate() guard generalized to "paste the drawing first" for any import source. TDD: tests/dxf-import-api.test.js — 3 spawned-server tests (PORT 3198): single-space dxf → 200 isBuilding:false bid.totalHeadCount>0 cadModel heads>0 walls>=4; multi-space buildingDxf (default layers) → 200 isBuilding:true spaces>=2 interiorWalls>=1 columns>=1; garbage dxf → 400 (no 500). Red-then-green confirmed (dxf/buildingDxf returned 400 before wiring). Browser-verified (preview restarted to load new server.js; preview_eval/DOM at 1500px): building-dxf path → layout "2 spaces · 1 int walls · 1 cols · 1 openings", 8 heads, OpenGeometry solids 5w/20p/8h, status "rendered 8 heads · 20 pipe segments · heads: generated CAD part (best-effort, not mfg-exact)" (__headDebug usingPartMesh:true). Full suite 38 files/352 tests green (was 37/349). Agentic-rules verifier ok. Geometry stays on the OpenGeometry kernel; no AutoSprink/AutoCAD/AHJ/PE/manufacturer claim. (Screenshot skipped — preview_screenshot hangs on the WebGL rAF canvas this session; DOM/eval is the established substitute.) Next unchecked Task Queue item: T22 (wire full-scope bid + calibrate vs Home Depot $792,543.84).
- 2026-05-29: S4 residual "needs import" surface shipped (browser-verified). settings.html loadLibrary now fetches the LIVE GET /api/parts manifest instead of the registry all-missing inventory: groups components by category with per-part source pills (catalog=green / auto-built=gold / needs-import=red), a summary "<N> components · <gen> auto-sourced (best-effort, <mfg> manufacturer-exact) · <miss> need import. OpenClaw finds/creates parts automatically; you only import the rest. Parity gate stays <status>.", and a dedicated "Needs import (K)" block listing only the residual parts OpenClaw hasn't sourced. Clicking any needs-import pill sets docType=manufacturer_cut_sheets, prefills the importer placeholder "catalog URL / file for: <key>", and focuses+scrolls to the doc importer. Verified via preview_eval/DOM: 34 components, 22 auto-sourced / 0 manufacturer-exact / 12 need import, gate 'blocked'; click fitting_cap → importer focused + prefilled. HONESTY held: auto-built parts labeled best-effort/not-manufacturer-exact, gate stays blocked, the residual is exactly what the user imports (matches the requirement "user at most points the studio at a doc/URL for parts OpenClaw hasn't already found"). Static page over the tested /api/parts endpoint; suite unchanged (37 files/349 green). NOTE: per-part import currently routes to the existing Settings doc slots; a dedicated per-component asset-upload endpoint that overrides the generated mesh = R4 (pending). Next: R4 (per-component upload override), S5 (VPS real OpenClaw run + cron), R6 (cad-model component placements), T21/T22.
- 2026-05-29: S1+S2+S3 OpenClaw autonomous part-sourcing shipped (ultra workflow wliod8ee5, TDD; 37 files/349 tests green, +1 file/+20 tests). New src/components/auto-source.js (no .html touched; reuses T16 resolveComponentModel, T18 reconstructComponentModel, T17 generators, R1 part-mesh handoff, T7 injected-invoker shape). makeCatalogSourcer (S1: OpenClaw web_search/web_fetch find public CAD, fail-soft, provenance+license passed through never fabricated, manufacturerExact:false), makeImageReconstructSourcer (S2: SAM 3.1 via T18 + generate_3d_model fallback, fail-soft), makeOpenScadSourcer (wraps T17), runAutoSource (S3: resolveComponentModel priority catalog→openscad→sam, buckets found/created/generated/missing, fail-closed modelStatusByKey, optional mesh-write + injected Brain recorder, idempotent, fully fail-soft). HONESTY: registrySourceFor DOWNGRADES resolver 'catalog'→registry 'generated' (machine-found provenance unverified; never registry 'catalog'/'manufacturer') so machine coverage can't satisfy the manufacturer gate. ADVERSARIAL VERIFIER CAUGHT A REAL VIOLATION (honesty verdict FAIL, correctly): the returned parityGateStatus was computed from registry.buildParityInventory→parityGateStatus, and since registry hasRealModel counts 'generated' as a real source, a sourcer covering all required components flipped parityGateStatus to 'clear' — an overclaim for best-effort geometry (the core manufacturer gate via manufacturerParityComplete stayed false, but the exposed field could read 'clear'). FIX (this session): runAutoSource.parityGateStatus now derives from manufacturerParityComplete(modelStatusByKey) (gate-grade) → ALWAYS 'blocked' for auto-sourced models; functional coverage moved to a separate `functionalCoverage:{present,total,complete}` field explicitly labeled NOT a parity claim; docstrings corrected; added a test asserting that with catalog finding ALL 34 components functionalCoverage.complete=true YET parityGateStatus='blocked' (would have failed pre-fix). correctness + test-truth verdicts PASS (confirmed reuse signatures, fail-soft, no fabrication, no .skip/.only, assertions non-vacuous). Full suite 37 files/349 tests green. NOT manufacturer/AutoSprink/AHJ/PE parity — gate stays BLOCKED. CAVEAT: built+tested with MOCK invokers; OpenClaw currently HTTP_UNREACHABLE so the REAL autonomous run + cron scheduling is validated on the VPS (S5). Next: S4 (residual "needs import" UI), S5 (VPS run + cron + wiring), R6 (cad-model component placements), T21/T22 (DoD gaps).
- 2026-05-29: R3 studio render swap (heads) shipped + the 3D builder now renders a real CAD part mesh for the sprinklers. autosprink.html: import STLLoader (three/examples/jsm/loaders/STLLoader.js — served via /vendor/three → node_modules/three); new loadPartMeshes() fetches GET /api/parts, finds head_pendent (present+file), STLLoader.loadAsync('/parts/head_pendent.stl'), normalizes geometry (computeBoundingBox → center at origin, rotateX(-90°) OpenSCAD Z-up→three Y-up, scale to ~1ft for visibility, computeVertexNormals), stores headPartGeo + partsSource ('generated'|'catalog'); fail-soft (load error → primitive). addInstancedHeads uses headPartGeo (InstancedMesh, metalness 0.55) when present, else SphereGeometry(0.6) fallback. initStudio awaits loadPartMeshes after kernel; status shows head-part source; end-of-generate status appends "heads: generated CAD part (best-effort, not mfg-exact)" or "primitive (no part mesh)". window.__headDebug hook for verification. To get real STLs locally (openscad not installed): downloaded the official OpenSCAD 2021.01 portable (files.openscad.org, 21.8MB) and made scripts/build-parts.mjs honor OPENSCAD_BIN (the VPS .env already sets it) — ran OPENSCAD_BIN=<portable> npm run build:parts → 22 STLs generated, 12 missing, manufacturerExact 0, gate blocked. .gitignore now ignores parts/ wholesale (per-host build artifact; a committed manifest would reference uncommitted STLs). Browser-verified (preview_eval/DOM): init "OpenGeometry ready · head part: generated"; after generate, __headDebug = {usingPartMesh:true, source:'generated', vertsPerHead:1716, instances:936} (1716 verts >> ~150 for the old sphere, proving the STL geometry is in use), status "rendered 936 heads · 1010 pipe segments · heads: generated CAD part (best-effort, not mfg-exact)". /api/parts serves the on-disk manifest (22 generated/12 missing, manufacturerExactCount 0, gate blocked); /parts/head_pendent.stl served 200 (96,897 bytes). Full suite 34→ still 36 files / 329 tests green (HTML + build-script edits don't touch tests). HONESTY/fail-closed held: generated head mesh is best-effort, NOT manufacturer-exact; status + gate say so; AUTOSPRINK_PARITY stays blocked. SCOPE: only HEADS render as discrete parts (cadModel only emits head solids as placeable points) — fittings/valves/riser/FDC placement deferred to R6. Next: R6 (cad-model component placements) and/or AUTO-SOURCE S1–S5 (OpenClaw autonomous sourcing). NOTE: openscad portable lives at %TEMP%\\openscad-portable (not committed); real render on the VPS is R5.
- 2026-05-29: R1+R2 part-mesh pipeline + GET /api/parts shipped (ultra workflow we28777td, TDD, 36 files/329 tests green; +2 files/+9 tests). R1 src/components/part-mesh.js buildPartManifest({scadRunner,outDir,write}) walks registry COMPONENTS, runs T17 generateScadFor + renderScadToStl via an INJECTED scadRunner (openscad binary never spawned inside the module), writes parts/<key>.stl ONLY when a real STL renders (write+outDir+rendered.ok) — never fabricated; null generator / no runner / throw / empty → source:'missing', file:null. manufacturerExactCount hardcoded 0; parityGateStatus derived from buildParityInventory of present generated entries and STRUCTURALLY 'blocked' (required non-generatable parts identification_sign/riser_trim/fdc/gauge/fitting_cap/... return null in generateScadFor → can never be 'generated'). scripts/build-parts.mjs ("npm run build:parts") builds a real spawnSync openscad runner, degrades to all-'missing' (exit 0) when absent; writes parts/parts-manifest.json. .gitignore: parts/*.stl ignored, parts-manifest.json allowed. R2 GET /api/parts (authMiddleware, registered after /api/parity, before SPA catch-all) serves on-disk manifest else live buildPartManifest({}) all-missing; added a per-entry sanitizePartEntry that coerces manufacturerExact→false unless source∈{catalog,manufacturer} (closes the honesty verdict's tampered-manifest leak), derives manufacturerExactCount from sanitized entries, and hardcodes parityGateStatus 'blocked' (found/generated never clears parity). parts/<key>.stl reachable via existing repo-root express.static. HONESTY/fail-closed VERIFIED (3 adversarial agents: honesty PASS, test-truth PASS; the correctness 'fail' was a WRONG-CWD false negative — that agent globbed E:/ClaudeBot not C:/Users/dalla/OneDrive/Documents/HaloFire; I independently re-confirmed all 4 files exist, /api/parts at server.js:929, suite 36/329 green). Live preview verification: all-missing branch (34 missing, generated 0, manufacturerExactCount 0, gate blocked, openscad absent locally) + tamper test (generated entry falsely flagged manufacturerExact:true → coerced FALSE; catalog entry kept true; missing false; count derived=1; gate still blocked). openscad NOT installed locally → real STL render is a VPS/R5 step; pipeline is openscad-agnostic via injected runner. NOT AutoSprink/AutoCAD/manufacturer/AHJ/PE parity — generated meshes are best-effort, gate stays BLOCKED. Next: R3 studio render swap (instance real part meshes; needs real STLs — render on VPS or install openscad locally to verify), then AUTO-SOURCE S1–S5 (OpenClaw autonomous sourcing).
- 2026-05-29: Definition-of-Done adversarial verification (ultra workflow wqui7b5uv — 7 parallel DoD verifiers + a review-packet refresh, 8 agents). HONEST RESULT: 5/7 criteria met, 2 genuine gaps found. SATISFIED: d2 engineering layout (NFPA-13 spacing + cad-model.js schedule sizing + single-path hydraulics + balanceNetwork full balance + geometric compliance, all wired+tested, best-effort), d3 CAD export (DXF first-party+tested; STEP/IFC/STL via OpenGeometry WASM kernel, wired but runtime-unverified in repo), d5 studio UX (usable end-to-end, gates fail-closed+surfaced, 45 studio/gate tests green), d6 verifier+packet (scripts/verify-internal-alpha.ps1 runs full suite — independently re-ran: 34 files/320 tests green; packet refreshed), d7 deployable (docs/DEPLOY.md local run path verified end-to-end: seed exit 0, server boots, /, /workbench.html, /autosprink.html, /api/health all 200). NOT SATISFIED: d1 (SVG→3D live + structural solids kernel-backed, BUT floorPlanFromDxf/buildingFromDxf are engine-only/unwired in server.js, and the live viewer heads/drops are plain Three primitives not rendered parts) → added T21 (wire DXF) + the REAL-PART RENDER epic R1–R5 (render real part meshes); d4 (bare materials ARE priced from real ARGCO/Victaulic/FFF rows, BUT buildFullScopeBid is unwired — system components + soft costs are fallback constants/percentages — and no calibration to the real Home Depot $792,543.84 total) → added T22. Overclaim audit: CODE is conspicuously honest/fail-closed everywhere (every module disclaims AutoSprink/AHJ/PE/permit/manufacturer/fabrication; AUTOSPRINK_PARITY stays blocked from empty inventory; best_effort evidence rows are not gate-clearing); the only overclaim risks were in the DoD WORDING vs reality (d1 "DXF works"/"everything kernel-rendered", d4 "full scope...approaching real totals") — now annotated honestly in the Definition of Done. Stale prior review packets (claimed 17 files/131 tests, listed committed P1-P7/T8/T9 as "not done") superseded by docs/reviews/2026-05-29-autosprink-clone-review-current.md (320 tests, maps all 7 DoD criteria to verdicts, explicit "still NOT done" list incl. 0/34 real component models, public HTTPS pending). No code changed by this verification run. Honesty constraint upheld — "project complete" is NOT yet true; the verified remaining backlog is T21 (DXF wiring), T22 (full-scope bid), and the REAL-PART RENDER epic (the user's AutoCAD-3D-parts requirement).
- 2026-05-29: P7 studio compliance + full-hydraulics + submittal surface shipped (ultra workflow wn1mpkom8, TDD, 3 adversarial verifiers honesty/correctness/test-truth all PASS). API (src/api/server.js): extracted runSprinklerPipeline(req) so sprinkler-bid + a new submittal route share input-resolution/bid/scene/cadModel; imported balanceNetwork (../engine/hydraulic-network.js), checkCompliance (../engine/nfpa-compliance.js), buildSubmittal+renderSubmittalPdf (../engine/submittal.js). POST /api/projects/:name/sprinkler-bid response ADDS (back-compat preserved — bid/scene/cadModel/hydraulics/isBuilding unchanged): hydraulicNetwork = balanceNetwork({network: cadModel.rooms[0].network, hazard}) in try/catch -> {error} (CRITICAL: balanceNetwork resolves cadModel.network at TOP LEVEL which buildCadModel never produces — the network is at rooms[0].network on the floorPlan path; the buildingSvg path has no rooms[].network so the balance SKIPS gracefully with {error} rather than fabricating numbers), and compliance = checkCompliance({stories:[{spaces: bid.rooms.map(r=>({name,hazard,...r.layout}))}]}, hazard) in try/catch (real heads/spacing/bbox geometry). NEW POST /api/projects/:name/submittal (authMiddleware, async): runs the same pipeline, returns buildSubmittal({project:{name},bid,cadModel,hydraulics:(network balance if it ran else single-path),compliance}) as a Content-Disposition: attachment JSON download; optional body.pdf calls renderSubmittalPdf (no server invoker wired -> {skipped}, never throws). UI (autosprink.html): Hydraulics panel now shows the iterative network balance (network demand gpm, network friction, req @ source psi balanced) beneath the single-path estimate, each labelled best-effort/NOT-a-sealed-PE-calc; new Compliance (NFPA-13, geometric) panel lists per-rule PASS/WARN/FAIL + the always-present "Professional review required" honesty note; new "Download submittal (.json)" button (disabled until a layout generates) hitting /submittal via the existing blob-download pattern + a NOT-stamped/AHJ/PE/permit caption. HONESTY/fail-closed VERIFIED in tests + by 3 adversarial agents: buildSubmittal.submittalReady stays HARDCODED false; header.{stamped,ahjApproved,peReviewed,permitReady} all false; AUTOSPRINK_PARITY_INCOMPLETE stays blocked (buildParityInventory({}) empty); checkCompliance always appends the 'warn' NFPA13_REVIEW_NOTE (a geometric pass is NOT approval and clears NO gate); every surfaced result carries its module disclaimer; the only evidence write is the pre-existing best_effort row (not a gate-clearing type). New tests/p7-studio-surface.test.js (6 spawned-server tests, PORT 3196): compliance+numeric network balance on the floorPlan path, honest {error} skip on the building path, the NFPA13_REVIEW_NOTE warn finding, back-compat fields, /submittal honesty flags + attachment header, and legacy-SVG path never 500s / never submittalReady true. Browser-verified (preview_eval/DOM at 1500px; preview server restarted to load the new routes): Home Depot -> hydraulics single-path 270gpm/19.0psi + network 509gpm/48.4psi@source, compliance "2 fail(s)" (min-spacing FAIL, max-distance-to-wall FAIL, max-heads WARN) + honesty note, submittal button downloads halofire-submittal.json with status "submittal downloaded (best-effort package)". Direct API check: submittalReady false, all header flags false, AUTOSPRINK_PARITY blocked. Full suite 34 files / 320 tests green (was 33/314; +1 file / +6 tests). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — gates stay fail-closed BLOCKED. P7 was the last Task-Queue item; remaining = Definition-of-Done verification + review-packet refresh.
- 2026-05-29: T20 Settings UI + component-library browser shipped — new settings.html (no new endpoint; consumes the already-tested T19/P6 APIs + the statically-served registry module). Four panels: (1) AUTOSPRINK parity — GET /api/parity renders the 15-row matrix (12 functional rows 'present', the 3 regulated rows AHJ/PE/manufacturer-exact shown with a 'gated' badge + status 'gated'), with a banner reading "AUTOSPRINK PARITY GATE: BLOCKED · parityAchieved: false"; (2) Required Documentation — GET /api/settings/documents lists all 6 slots (catalogs/manufacturer_cut_sheets/ahj_approval/autosprink_reference/openscad_binary/pricebook_updates) with satisfied/missing, plus an admin add form (doc_type+link/upload) POSTing to /api/settings/documents; (3) Stack Dependencies — GET /api/settings/dependencies (openscad_installed/sam_gateway/autosprink_reference); (4) Component Library — dynamic import('/src/components/registry.js') + reg.buildParityInventory({}) (honest empty-status inventory) groups all 34 components across 15 categories, each shown with its real present/missing model status. Linked from workbench.html + autosprink.html headers ("Settings & Parity"). HONESTY/fail-closed: the page SURFACES parity/inventory but never flips a gate — the library inventory is derived from buildParityInventory({}) exactly like the server's /api/parity gate, so it honestly reports 0/34 with real models, all 34 'no model', gate BLOCKED, parityAchieved false; a catalog/cut-sheet link records evidence but does NOT clear a regulated gate. Browser-verified via preview_eval/DOM at 1500px against the running preview server (had to restart the stale 64-min preview process so /api/parity — added in P6 — was loaded; route order on disk confirmed correct, parity@787 before SPA catch-all@856): gate banner BLOCKED + parityAchieved false; 15 matrix rows incl. 3 gated; 6 doc slots; admin link add round-trip flips catalogs -> satisfied with no error; deps openscad_installed=false / sam_gateway=unknown / autosprink_reference=missing; library 34 components / 0 with models / 34 missing; both nav links present. DEFERRED (honesty, not overclaimed): "render catalog/generated models in the 3D layout" — zero real models exist yet (all 34 gated/missing), so there is nothing to render; lands when real models are attached. Full suite 33 files / 314 tests green (unchanged — settings.html is a static page over already-tested APIs, browser-verified like the other studio HTML surfaces). Backward compat preserved (new file + 2 one-line header link insertions; no JS/route change). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — AUTOSPRINK_PARITY gate stays fail-closed BLOCKED. Next: P7 parity + component-library + compliance UI (studio-side: surface compliance findings + hydraulic summary + submittal download in the workbench/studio, beyond the Settings overview T20 ships).
- 2026-05-29: P6 parity matrix module + status API + gate wiring shipped — new src/engine/parity-matrix.js + tests/parity-matrix.test.js (14 tests: 12 unit + 2 spawned-server). buildParityMatrix(state) -> { areas:[{key,name,status:'present'|'partial'|'missing'|'gated',evidence,gated}], requiredComplete:boolean, gated:[...], disclaimer } covering all 15 roadmap parity areas (drawing_import, building_modeling, head_layout, schedule_sizing, hydraulic_network, nfpa_compliance, supports, component_library, submittal, cad_export, bid_bom, evidence_settings + the three GATED rows ahj_approval, pe_review, manufacturer_exact). Non-gated areas derive present/missing from the corresponding state flag; the three GATED rows are NEVER auto-present — status stays 'gated' unless their state evidence is REAL (one of catalog/pe/ahj/manufacturer; generated/best-effort/AI does NOT qualify). PARITY_AREA_KEYS exports the frozen ordered key list. parityAchieved(matrix, inventory) is fail-closed: false unless requiredComplete (every non-gated area present) AND every gated row is cleared with real evidence; a generated-only inventory ({generatedOnly:true}) NEVER achieves parity. New GET /api/parity route (authMiddleware) in src/api/server.js returns { matrix, parityAchieved:false, gate:{code:'AUTOSPRINK_PARITY_INCOMPLETE',severity,status:'blocked',blockedClaims,reason}, disclaimer } — the gate is derived from buildParityInventory({}) (empty inventory -> no manufacturer-exact models -> parityGateStatus 'blocked'), so the AUTOSPRINK_PARITY gate stays fail-closed BLOCKED and the surface never asserts AHJ/PE/manufacturer evidence. Pure/deterministic, browser-free, no binaries. Tests: matrix lists every area with status+evidence; AutoSprink feature areas covered; AHJ/PE/manufacturer-exact gated (never auto-present); a missing functional area -> missing + requiredComplete false; parityAchieved false while a required area missing, false when gated rows lack evidence, false for generated-only inventory, false when gated rows carry only 'generated' evidence, true ONLY with catalog/pe/ahj real evidence on every gated row, re-blocks when one gated row loses evidence; PARITY_MATRIX_DISCLAIMER on the matrix; spawned-server /api/parity requires auth, lists areas, keeps gate 'blocked' + parityAchieved false. HONESTY/fail-closed: PARITY_MATRIX_DISCLAIMER labels this best-effort functional coverage only — NOT AutoSprink/AutoCAD parity, NOT AHJ-approved, NOT PE-reviewed, NOT permit-ready, NOT manufacturer-approved, NOT fabrication-ready; generated (OpenSCAD/SAM) models never satisfy the gated rows; clears NO claim gate and never flips AUTOSPRINK_PARITY. Full suite 33 files / 314 tests green (was 32/300). Backward compat preserved (new engine file + new test; server.js only gained imports + the /api/parity route). UI surfacing deferred to P7. Next: P7 parity + component-library + compliance UI (browser).
- 2026-05-29: P5 submittal package + PDF report shipped — new src/engine/submittal.js + tests/submittal.test.js (16 tests). buildSubmittal({project, bid, cadModel, hydraulics?, compliance?}) -> structured package { header, headSchedule, pipeSchedule, hydraulicSummary, billOfMaterials, gateStatus, bidSummary, disclaimer }. Pure/deterministic, browser-free, no binaries. header carries project/client/address/units + EXPLICIT honesty flags stamped:false, ahjApproved:false, peReviewed:false, permitReady:false (none asserted). headSchedule groups head solids by orientation -> [{orientation,count}] (counts sum to the cadModel head total). pipeSchedule groups pipe solids by nominal diameter -> [{diameterIn,count,totalLengthFt}] (3D segment length, ordered ascending). billOfMaterials is a BEST-EFFORT takeoff (head line per orientation; pipe footage + a rough fitting estimate per diameter — every line estimate:true, NOT a manufacturer-quoted material list). hydraulicSummary is a compact view of the supplied balanceNetwork() result (requiredSourcePsi/totalDemandGpm/friction/elevation/kFactor/hazard/balanced/source margin + note); NULL when hydraulics omitted (graceful — package still assembles). gateStatus: AUTOSPRINK_PARITY gate is FAIL-CLOSED (buildParityInventory({}) -> empty inventory -> parityGateStatus 'blocked', always in blockedGates), compliancePassed reflects the supplied compliance.passed (null when omitted; a failed compliance adds an NFPA13_COMPLIANCE_FAILED blocked gate), and submittalReady is ALWAYS false — a best-effort package can never satisfy a regulated submittal gate. buildPdfReportPayload(pkg) is a pure cadModel-style builder -> { tool:'generate_pdf_report', title:'HaloFire Submittal (best-effort) — <project>', sections:[Project, Head Schedule, Pipe Schedule, Hydraulic Summary, Bill of Materials, Gate Status, (Bid Summary)], disclaimer }. async renderSubmittalPdf(pkg, {invoker}) builds that payload and calls the INJECTED async invoker (supports both (toolName,payload) and (payload) signatures) — on success { ok:true, result }; absent/non-function/throwing invoker (or a build error) -> { ok:false, skipped:true, reason } WITHOUT throwing (graceful degradation; the OpenClaw generate_pdf_report gateway on GX10 is external, wired in production, mocked in tests). HONESTY/fail-closed: SUBMITTAL_DISCLAIMER on every package + the PDF payload labels this a best-effort internal alpha — explicitly NOT a stamped/sealed submittal, NOT AHJ-approved, NOT PE-reviewed, NOT permit-ready, NOT manufacturer-approved, NOT fabrication-ready, NOT AutoSprink/AutoCAD parity; clears NO claim gate and never reads as an approved submittal. Tests cover: all sections present + disclaimer + honesty-flag header; head schedule count == cadModel head total; pipe schedule grouped by diameter w/ lengths; hydraulic summary mirrors the balance result; BOM lists head + pipe items; gateStatus.autosprinkParity 'blocked' + blockedGates non-empty + submittalReady false; a failing compliance reflected in compliancePassed; graceful with omitted hydraulics/compliance (hydraulicSummary null, still blocked); renderSubmittalPdf ok with a 2-arg mock invoker (asserts generate_pdf_report toolName + title + sections + disclaimer), ok with a 1-arg invoker, and ok:false skipped:true (no throw) for no opts / empty opts / non-function invoker / throwing invoker. Full suite 32 files / 300 tests green (was 31/284). Backward compat preserved (new files only; no existing file touched). Next: P6 parity matrix module + status API + gate wiring.
- 2026-05-29: P4 generate component models -> populate parity inventory shipped — new src/components/model-pipeline.js + tests/model-pipeline.test.js (10 tests). generateLibrary(components=COMPONENTS, {scadRunner?}) runs the T17 generateScadFor() across the registry, KEEPS only components T17 can honestly model (heads, pipe, tee/elbow/coupling/reducer fittings, grooved, valves, hanger — fdc/backflow/riser/trim/gauge/drains/signs/inspector_test/seismic_brace/cross return null and are SKIPPED, no fake geometry), optionally renders each .scad via the injected scadRunner through generators.renderScadToStl (absent -> source-only, hasStl:false; a throwing runner fails closed to hasStl:false — never fabricates STL). Returns { results:[{key, source:'generated', hasScad:true, hasStl:boolean, scad}], modelStatusByKey:{ [key]:{ source:'generated', model:{format:'scad',source,stl,manufacturerExact:false} } } }. Feeding modelStatusByKey to registry.buildParityInventory raises FUNCTIONAL present-count (0 -> the modelable subset) but parityComplete STAYS false because required signs/trim/fdc/etc have no honest generator (still MISSING). Added gate-grade helpers isManufacturerExact(entry) (true ONLY for source 'catalog'|'manufacturer' WITH a real model — 'generated' deliberately excluded) and manufacturerParityComplete(modelStatusByKey) (true ONLY when EVERY required component has manufacturer-exact evidence). Test proves: even an all-'generated' map where buildParityInventory.parityComplete==true still yields manufacturerParityComplete()==false, so the AUTOSPRINK_PARITY / manufacturer / PE claim stays gated; it flips true only with catalog/manufacturer evidence on every required row, and knocking one required row back to 'generated' re-blocks it. No real binary call (injected runner or source-only). Pure/deterministic, browser-free. HONESTY/fail-closed: generated geometry is BEST-EFFORT massing, explicitly NOT manufacturer-exact, confers NO AutoSprink/AutoCAD/manufacturer parity and NO AHJ/PE approval; disclaimer rides in every .scad header. Full suite 31 files / 284 tests green (was 30/274). Backward compat preserved (new files only; registry.js + generators.js + model-resolver.js untouched). BOM model-ref wiring + parity matrix surfacing deferred to P6/P7. Next: P5 submittal package + PDF report.
- 2026-05-29: P3 hanger + seismic support placement shipped — new src/engine/supports.js + tests/supports.test.js (13 hand-counted tests). placeSupports(cadModel, opts?) walks the SAME pipe solids buildCadModel/buildRoomCad emit ({kind:'pipe', role, from:[x,y,z], to:[x,y,z], diameterIn}) and returns { hangers:[{position:[x,y,z],pipe,role:'hanger',diameterIn,spacingFt}], braces:[{position,pipe,role:'seismic_brace',braceType:'lateral',diameterIn,intervalFt}], counts:{hangers,braces}, note }. maxHangerSpacingFt(nominalIn): NFPA-13 public max hanger spacing by nominal size — <=1.25" -> 12 ft, >=1.5" -> 15 ft. Hangers placed on HORIZONTAL branch/main runs only (drops/risers/ties skipped): offsets = end hanger near each end (within ~1ft: 0 and len) + ceil(len/spacing)-1 interior nodes at the max spacing, deduped, clamped to >=1 per run; no consecutive gap exceeds the max spacing. SEISMIC_BRACE_INTERVAL_FT=40: a lateral sway brace every ~40 ft (floor(len/interval) nodes) along branch/main runs; opts.seismicIntervalFt overrides. Pure/deterministic, browser-free, no binaries — same cad-model in, identical supports out. Hand-checked cases: 30ft 2" branch -> 3 hangers at {0,15,30} (ceil(30/15)=2 + ends, deduped) & 0 braces (run < 40ft); 12ft 1" branch -> 2 end hangers at {0,12} (one 12ft span = max); tiny 0.5ft run -> >=1 hanger (clamp); empty model -> 0/0; vertical drop+riser -> 0 hangers; 100ft 4" main -> 2 lateral braces at {40,80}; injected interval 25 -> 4 braces at {25,50,75,100}; real 60x40 light buildRoomCad model -> deterministic (a==b), hangers>0 all on branch/cross-main pipes. Feeds the BOM/takeoff (hanger + seismic_brace components already in src/components/registry.js). HONESTY/fail-closed: SUPPORTS_NOTE export + note field on every result label this a BEST-EFFORT spacing estimate — NOT a seismic engineering design, NOT PE-reviewed, NOT AHJ-approved, NOT AutoSprink parity; clears NO claim gate. Full suite 30 files / 274 tests green (was 29/261). Backward compat preserved (new files only; no existing file touched). Studio render + cad-model wiring deferred to P7 (UI). Next: P4 generate component models -> populate parity inventory.
- 2026-05-29: P2 NFPA-13 compliance checker shipped — new src/engine/nfpa-compliance.js + tests/nfpa-compliance.test.js (11 tests). checkCompliance(layoutOrBuilding, hazard) -> { findings:[{code,severity:'pass'|'warn'|'fail',rule,detail}], passed:boolean, note }. Accepts EITHER a single layoutRoom output / hand-built layout ({heads,spacingX,spacingY,coveragePerHeadSqFt,bbox}) OR a building/system-layout ({stories:[{spaces:[{name,hazard,heads,...}]}]}) via collectSpaces (each space checked under its own hazard, falling back to the supplied default). Reuses HAZARD_RULES/getHazardRule from sprinkler-layout.js (light 225/ord 130/extra 100 sqft max area; light&ord 15ft / extra 12ft max spacing; 6ft min spacing all hazards; max-to-wall = half max spacing). Rules emitted as findings: NFPA13_MAX_COVERAGE_AREA (coveragePerHeadSqFt or spacingX*spacingY vs hazard max), NFPA13_MAX_SPACING (actual nearest-neighbour max gap from head positions vs hazard max), NFPA13_MIN_SPACING (closest head gap vs 6ft), NFPA13_MAX_DISTANCE_TO_WALL (worst head's nearest-wall distance from bbox vs half max spacing), NFPA13_MAX_HEADS_PER_SYSTEM (total heads vs ~100 light/ordinary guideline). passed=false iff ANY finding is severity 'fail'. HONESTY/fail-closed: NFPA_NOTE export + an always-present 'warn'-severity NFPA13_REVIEW_NOTE finding state passing these geometric checks is NOT AHJ approval / NOT PE review / NOT permit-ready / NOT AutoSprink parity and clears NO gate — geometric pass NEVER reads as approval. Pure/deterministic, browser-free, no binaries. Hand-checked vitest cases: 20x20 ordinary layoutRoom -> passed true & all rule findings 'pass'; over-spaced 20ft / over-area 400sqft layout -> NFPA13_MAX_SPACING + NFPA13_MAX_COVERAGE_AREA 'fail' & passed false; over-area-only 140>130 -> fail; 3ft min-spacing -> NFPA13_MIN_SPACING fail; center head 10ft from nearest wall (>7.5) -> NFPA13_MAX_DISTANCE_TO_WALL fail; 130 heads -> NFPA13_MAX_HEADS_PER_SYSTEM fail; building input with an over-spaced space -> fail; empty/no-heads -> no fails + note present; unknown hazard -> throws. Full suite 29 files / 261 tests green (was 28/250). Backward compat preserved (new files only; no existing file touched). NOT AHJ/PE/AutoSprink/manufacturer parity; AUTOSPRINK_PARITY gate stays fail-closed BLOCKED. Surface as a compliance panel + COMPLIANCE_REVIEW gate note deferred to P7 (UI). Next: P3 hanger + seismic support placement.
- 2026-05-29: P1 full hydraulic network balance shipped — new src/engine/hydraulic-network.js + tests/hydraulic-network.test.js (18 hand-computed tests), re-exported (kFactorFlow, balanceNetwork) from src/engine/hydraulics.js so the network balance imports from the same hydraulics entry point as the T2 single-path estimate (T2 requiredPressureAtRiser/flagSchedule API untouched — backward compat regression-tested). kFactorFlow(k,psi)=k*sqrt(psi) (Q=K√P): hand-verified 5.6√7=14.816 gpm, 5.6√20=25.044, 8√7=21.166; rejects k<=0, treats psi<=0 as 0 flow. balanceNetwork({network|cadModel,hazard,kFactor?(def 5.6),C?(def 120),sourcePsi?,minHeadPressurePsi?(def 7)}): resolves a raw network or a cadModel.network; picks the hydraulically-remote branch (longest run); orders its heads most-remote-first; seeds the most-remote head at the min operating pressure; walks INWARD head-by-head accumulating flow — each head discharges Q=K√P at its node pressure, the accumulated flow loses Hazen-Williams friction (existing hazenWilliamsLossPsiPerFt) over each inter-head segment so the next upstream node sits HIGHER by that loss (so upstream heads flow MORE); then carries the full accumulated branch demand through cross-main + main-tie + system-riser friction and adds 0.433 psi/ft of head elevation to compute requiredSourcePsi. Returns {heads:[{node,x,y,pressurePsi,flowGpm}], totalDemandGpm, requiredSourcePsi, frictionLossPsi, elevationPsi, segments:[{name,diameterIn,lengthFt,flowGpm,lossPsiPerFt,lossPsi}], balanced:boolean, kFactor, hazard, demandContextGpm(remoteAreaDemand), disclaimer}. Optional sourcePsi -> sourceMargin + balanced=sourcePsi>=requiredSourcePsi. Pure/deterministic, browser-free, no binaries. Hand-checked cases: single K=5.6 head at 7 psi -> 14.816 gpm & requiredSourcePsi==7 (no friction/elev); a 2-head 10ft 1.049" branch -> upstream head higher psi & higher flow, totalDemand==q1+q2 computed independently, requiredSourcePsi==p2; elevation case requiredSourcePsi==7+0.433*12; real cadModel (60x40 light) per-head node flows + segments. HONESTY/fail-closed: labelled a best-effort ITERATIVE estimate — NOT a sealed Hardy-Cross solve, NOT a stamped hydraulic calc, NOT PE-reviewed, NOT AHJ-approved, NOT AutoSprink parity; clears NO claim gate. Full suite 28 files / 250 tests green (was 27/232). Backward compat preserved (hydraulics + cad-model + all spawned-server suites unchanged). Studio Hydraulics panel surfacing best-effort/deferred. Next: P2 NFPA-13 compliance checker.
- 2026-05-29: T19 Settings + documentation upload/link API shipped — new tests/settings-documents.test.js (7 spawned-server tests) + routes in src/api/server.js + a settings_documents table mirrored into BOTH server.js initDatabase and seed.js (CREATE TABLE + defensive ensureColumn for mode/url/filename/notes/evidence_id/created_by). REQUIRED_DOC_SLOTS = [catalogs, manufacturer_cut_sheets, ahj_approval, autosprink_reference, openscad_binary, pricebook_updates]; a slot reads 'missing' (satisfied:false) until a link/upload row exists. GET /api/settings/documents (authMiddleware) lists every slot + status (latest row attached). POST /api/settings/documents (authMiddleware + requireRole('admin')) body {doc_type, mode:'link'|'upload', url?, filename?, notes}: validates doc_type against the slot list (400 unknown), mode link requires url / upload requires filename (400 otherwise), rejects unsupported fields (400). On success a single transaction inserts a PRESENT project_evidence row (project_name 'HaloFire Library', evidence_type=doc_type, source_ref=url|filename) so the link/upload can satisfy resolve-gate evidence, then the settings_documents row referencing it; returns {id, evidence_id, status:'satisfied'}. GET /api/settings/dependencies (authMiddleware) reports {openscad_installed:boolean (best-effort spawnSync 'openscad --version' detection, never throws), sam_gateway:'unknown' (GX10 sam3 via OpenClaw bridge, not probed here), autosprink_reference:'linked'|'missing' (from settings_documents)}. HONESTY/fail-closed: a catalog/cut-sheet link is recorded as evidence but NEVER auto-clears a regulated claim gate — AHJ/PE/AutoSprink parity gates still require their specific approved evidence via the T5 resolve route (test proves an AUTOSPRINK_EVIDENCE_MISSING gate stays 'blocked' after a catalog upload). Also added a vitest.config.js with fileParallelism:false (several suites spawn a real server on its own port; running them in parallel oversubscribed the machine and caused intermittent 401/429/timeout — serial spawned-server execution is deterministic, full suite ~16s) and raised the API/login rate-limit ceilings only when NODE_ENV=test (production limits 100/10 unchanged) so repeated logins in the spawned-server suites don't 429. Tests cover: 401 without auth, all 6 slots missing initially, non-admin POST -> 403, admin link -> 200 + slot satisfied + a present project_evidence row exists, unknown doc_type / link-without-url -> 400, catalog upload does NOT clear a blocking gate, dependencies returns an openscad_installed boolean + sam_gateway + autosprink_reference. Full suite 25 files / 226 tests green (was 24/219). Backward compat preserved (existing suites unchanged; pre-existing parallel flakiness in the other spawned-server suites also resolved by the config). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — uploading a doc is evidence only; the AUTOSPRINK_PARITY gate stays fail-closed BLOCKED until the inventory is genuinely complete with real evidence. Next: T20 Settings UI + component-library browser (browser).
- 2026-05-29: T18 SAM 3.1 reconstruction adapter shipped — new src/components/sam-reconstruct.js + tests/sam-reconstruct.test.js (10 tests). buildSamReconstructPayload(component, imageRef) returns a DETERMINISTIC plain-object request for the GX10 SAM 3.1 service: { service:'sam-3.1', op:'reconstruct', componentKey (from a component object's key or a bare string key), imageRef, outputFormat:'stl' } — component key + image ref + desired output mesh format always present, same inputs -> identical object. async reconstructComponentModel(component, imageRef, {invoker}) builds that payload and calls the INJECTED async invoker(payload) (production wires the OpenClaw bridge / hal_sam_status — SAM 3.1 is external on GX10, never called directly here). On success returns { ok:true, source:'generated_sam', model, label:'best-effort SAM 3.1 reconstruction — NOT manufacturer-exact' } (raw or {mesh,format} returns both accepted as the model). HONESTY/fail-closed: if the invoker is absent / not a function (no bridge wired) OR throws (GX10/gateway down) OR yields an empty mesh, returns { ok:false, skipped:true, reason } WITHOUT throwing — success is never fabricated, a missing model stays missing for the parity inventory. Browser-free, no process/binary spawned in engine code. Tests cover: payload shape (key + image ref + format present), determinism, bare-string component; reconstructComponentModel ok + best-effort label with a mock invoker, raw-string model accepted, and ok:false skipped:true (no throw) for absent invoker / omitted opts / non-function invoker / throwing invoker / empty model. Full suite 24 files / 219 tests green (was 23/209). Backward compat preserved (no existing file touched; new src/components/sam-reconstruct.js). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — SAM-reconstructed models are best-effort and the AUTOSPRINK_PARITY gate stays fail-closed BLOCKED until the inventory is genuinely complete with real evidence. Next: T19 settings + documentation upload/link API.
- 2026-05-29: T17 OpenSCAD parametric generators shipped — new src/components/openscad/generators.js + tests/openscad-generators.test.js (23 tests). Deterministic, browser-free string generators returning OpenSCAD (.scad) SOURCE for each component type: sprinklerHeadScad({type,k,thread}), pipeScad({nominalIn,lengthFt,schedule}), teeScad({runIn,branchIn}), elbowScad({nominalIn,deg}), couplingScad({nominalIn}), reducerScad({fromIn,toIn}), valveScad({type,nominalIn}), hangerScad({nominalIn}). Each emits a commented header naming the component + an explicit BEST-EFFORT / NOT manufacturer-exact disclaimer (no AutoSprink/AutoCAD/manufacturer parity, no AHJ/PE approval), standardizes on mm (IN_TO_MM/FT_TO_MM conversion), sets $fn for smooth round bodies, and surfaces its params into the source (K-factor, sizes, angle, schedule, etc.). Geometry is approximate primitive massing (cylinders/spheres/difference/taper) for visualization + take-off context ONLY. generateScadFor(component) dispatches by registry category (heads->sprinkler_head, pipe->pipe, fittings->tee/elbow/coupling/reducer by key or name, grooved->coupling massing, valves->valve, hanger->hanger) and FAILS CLOSED returning null for categories with no honest parametric model (signs, trim kits, gauges, etc.) or for null/empty input — never invents fake geometry. renderScadToStl(scad,{runner}) uses an INJECTED runner and NEVER spawns a process here (the openscad CLI is external, lives behind the caller's injected fn): no runner -> {ok:false,reason:'openscad_not_installed'}; runner returns {stl}|string -> {ok:true,stl}; runner throws or yields empty -> {ok:false,reason:'render_failed'} (success is never fabricated). Tests cover: every generator returns a non-empty string with the expected module keyword + best-effort/not-manufacturer-exact labels + $fn + param values; generateScadFor dispatches each category correctly and returns null for unsupported/null/empty; renderScadToStl ok with mock runner, ok:false openscad_not_installed with no runner / no opts, render_failed on throw, string-stl accepted. Full suite 23 files / 209 tests green (was 22/186). Backward compat preserved (no existing file touched; new dir src/components/openscad/). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — generated models are best-effort and AUTOSPRINK_PARITY gate stays fail-closed BLOCKED until the inventory is genuinely complete with real evidence. Next: T18 SAM 3.1 reconstruction adapter.
- 2026-05-29: T16 component 3D-model resolver pipeline shipped — new src/components/model-resolver.js + tests/model-resolver.test.js (11 tests). resolveComponentModel(component, sourcers) tries INJECTED async sourcers in strict priority catalog -> openscad -> sam -> placeholder. Each sourcer is `(component) => {format,data,...}|null`; returns null / a model with empty (no real `data`) / throws => skip to next. Returns { key, source:'catalog'|'generated_openscad'|'generated_sam'|'placeholder', status, model|null, label }: catalog/openscad/sam are status 'present' with provenance label (generated ones labelled best-effort, NOT manufacturer-exact); placeholder is status 'missing', model null — HONESTY/fail-closed, a placeholder is NEVER reported present. A throwing sourcer is caught and treated as null (try next). resolveLibrary(components, sourcers) -> { results:[...], modelStatusByKey } where modelStatusByKey maps each key to {source, model} in registry vocabulary (catalog->'catalog', generated_openscad/generated_sam->'generated', placeholder->'placeholder', model null) so it feeds registry.buildParityInventory directly — placeholders fail closed to MISSING, only genuine catalog/generated models count present. Pure logic + injected async, browser-free, no external binaries called in engine code. Tests cover: catalog wins; fall-through to openscad then sam; all-null -> placeholder/missing; no sourcers -> placeholder; throwing sourcer skipped; all-throwing -> placeholder; empty-data model treated as null; resolveLibrary aggregation + buildParityInventory present/missing counts. Full suite 22 files / 186 tests green (was 21/175). Backward compat preserved (no existing file touched). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — AUTOSPRINK_PARITY gate stays fail-closed BLOCKED until inventory is genuinely complete with real evidence. Next: T17 OpenSCAD parametric generators.
- 2026-05-29: T15 component registry + AutoSprink parity inventory shipped — new src/components/registry.js + tests/component-registry.test.js (22 tests). COMPONENTS is a deep-frozen canonical array (33 entries) of every fire-sprinkler module a layout/bid needs, each { key, category, name, params:{nominal sizes/options}, required:boolean }. Categories + members: heads (pendent/upright/sidewall/concealed/dry_pendent/esfr), pipe (sch10/sch40), fittings (tee/elbow_90/elbow_45/coupling/reducer/cap/cross), grooved (coupling/flange_adapter), valves (alarm_check/check/butterfly/osy_gate/prv/deluge), fdc, backflow_preventer, riser_assembly, riser_trim, gauge, hanger, seismic_brace, drain (main/aux), inspector_test, identification_sign. Exports getComponent(key) (O(1) Map lookup, undefined for unknown), componentsByCategory() (groups all into {[cat]:Component[]}), buildParityInventory(modelStatusByKey) -> {total,present,missing[],byCategory:{[cat]:{present,total,missing[]}},parityComplete}. HONESTY/fail-closed: a component counts 'present' ONLY when modelStatusByKey[key].source is a REAL source ('catalog'|'generated') AND carries an actual model — a 'placeholder'/'missing' status, or a real source with no model, counts MISSING. parityComplete is true only when EVERY required component has a real model. AUTOSPRINK_PARITY_GATE = frozen claim-gate {code:'AUTOSPRINK_PARITY_INCOMPLETE', severity:'blocking', status:'blocked', blockedClaims:['AutoSprink parity','complete component library','manufacturer-exact models'], reason:...}; parityGateStatus(inventory) returns 'blocked' unless parityComplete (then 'clear'). Pure/deterministic, browser-free; no models in this module (resolver T16 feeds status). Full suite 21 files / 175 tests green (was 20/153). Backward compat preserved (no existing file touched). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — gate stays fail-closed BLOCKED until the inventory is genuinely complete with real evidence. Next: T16 component 3D-model resolver pipeline.
- 2026-05-29: studio + OpenGeometry CAD + DXF shipped (commits 3764b64, 2b5b7ba). 68 tests green.
- 2026-05-29: T1 STEP/IFC/STL export shipped — verified 79KB STEP from 23 OpenGeometry entities. Next: T2 hydraulics.
- 2026-05-29: T2 hydraulic engine shipped — src/engine/hydraulics.js + tests/hydraulics.test.js (21 hand-computed tests). Full suite 13 files / 89 tests green (was 12/68). Single-path estimate only; NOT a full network balance, NOT PE/AHJ/AutoSprink parity. Next: T3 full bid scope.
- 2026-05-29: T3 full bid scope shipped — src/engine/bid-scope.js + tests/bid-scope.test.js (11 tests). buildSystemComponents (6 core riser-assembly components + conditional fire pump via boolean or required>available pressure), buildSoftCosts (permit 2% / design 6% / freight 3% labelled assumptions, priceSource:soft_cost_assumption), buildFullScopeBid (prices components via priceResolver with flagged fallback, computes fullScopeTotal alongside bareMaterialsTotal). Full suite 14 files / 100 tests green (was 13/89). Best-effort estimate only, fail-closed; NOT a complete priced bid, NOT manufacturer-quoted, NOT AHJ/PE/AutoSprink parity. Next: T4 DXF/PDF floor-plan import.
- 2026-05-29: T4 DXF floor-plan import shipped — floorPlanFromDxf in src/engine/floorplan-import.js + 8 new tests in tests/floorplan-import.test.js. Parses ENTITIES section: LWPOLYLINE, POLYLINE/VERTEX, and best-effort closed-loop assembly from LINE segments; opts.layer filter, opts.unitsPerDrawingUnit scale-to-feet, opts.hazard default; reuses normalizeFloorPlan; skips degenerate <3-vertex shapes. generateSprinklerBid verified to run on imported plan. Full suite 14 files / 108 tests green (was 14/100). PDF import DEFERRED (not a deterministic dependency-free parse). Best-effort geometry only, NOT CAD-grade, claim gates stay fail-closed. Next: T5 resolve-gate workflow.
- 2026-05-29: T7 OpenClaw CAD automation adapter shipped — src/cad/openclaw-cad.js + tests/openclaw-cad.test.js (13 tests). buildGenerate3dModelPayload / buildGenerateDxfPayload are PURE deterministic cad-model -> plain-object args (project name, units, counts, compact solids spec: walls->boxes, pipes->sized cylinders from/to, heads->oriented points); both carry the cad model's fail-closed disclaimer. invokeOpenClawCad(toolName, payload, {invoker}) calls the INJECTED async invoker (no hardcoded network) and returns {ok:true,result} on success; if invoker is absent/not-a-function OR throws (gateway unreachable) returns {ok:false,skipped:true,reason} WITHOUT throwing — graceful degradation so HaloFire works whether or not the GX10 OpenClaw gateway is live. Full suite 16 files / 126 tests green (was 15/113). Geometry only — clears NO claim gate, makes NO AHJ/PE/AutoSprink/manufacturer parity claim; fail-closed preserved. Production wires the bridge as the invoker later. Next: T6 multi-floor / multi-zone.
- 2026-05-29: T6 multi-floor / multi-zone engine shipped — buildCadModel in src/engine/cad-model.js now accepts an optional floorPlan.floors array; offsetSolidZ/offsetRoomCadZ/buildFloorCad lift each floor's whole network (slabs/walls/pipes/heads + network elevations) in Z by baseElevationFt so floors stack, each floor keeps its own independent riser->cross-main->branch->drop->head network and NFPA schedule sizing, counts aggregate across floors, model.floors[] exposes per-floor solids/counts/sizing. Walls now carry baseZ (default 0); dxf-export.js honors baseZ when extruding wall posts (backward compatible). Legacy floorPlan.rooms still builds a single floor at base 0 — output unchanged. New tests in tests/cad-model-multifloor.test.js: 2-floor 60x40 plan yields heads in two elevation bands (some Z<14, some Z>14), 24 total heads = 12+12, per-floor independent sizing/risers, and a legacy-plan regression guard. Full suite 17 files / 131 tests green (was 16/126). Engine only — NFPA schedule sizing, NOT hydraulically balanced, NOT AHJ/PE/AutoSprink parity; claim gates stay fail-closed. Studio surfacing deferred/best-effort. Next: T8 head perf + true-scale toggle.
- 2026-05-29: T10 deploy path + review packet refresh shipped — new docs/reviews/2026-05-29-autosprink-clone-review.md (truthful: OpenGeometry CAD render, STEP/IFC/STL+DXF export, NFPA-13 schedule sizing, single-path hydraulic estimate, full-scope bid estimate, SVG+DXF import, evidence-gated resolve-gate API, OpenClaw CAD adapter — each cited to a file; exact count 17 files / 131 tests; explicit "still NOT done" = full network hydraulic balance, AutoSprink/AutoCAD parity, AHJ/PE/manufacturer approvals, studio wiring of hydraulics/multi-floor, perf instancing, surface consolidation). New docs/DEPLOY.md (env vars, npm install → seed → node src/api/server.js → /, workbench, /autosprink.html; one-command verifier). scripts/verify-internal-alpha.ps1 now runs the FULL `npx vitest run` (no file list) so it can't drift; seed + agentic-rules steps kept. Docs+script only — NO behavior change. Full suite 17 files / 131 tests green (unchanged). Fail-closed preserved; no parity claimed. Next: T8 head perf + true-scale toggle.
- 2026-05-29: T8 head perf + true-scale toggle shipped — autosprink.html now renders 900+ heads/drops via Three InstancedMesh (heads=sphere instances, drops=scaled-cylinder instances) while walls/mains/branches/riser stay OpenGeometry-kernel; render ~9s→~4s for Home Depot. New View toggle "Exaggerate pipe radius (×6)" (off=true NFPA scale) re-renders from stored currentCadModel. exportCad refactored to buildExportScene() — rebuilds the COMPLETE kernel model (walls+all pipes+all heads, true-scale) on-demand at export so STEP/IFC/STL stay complete: verified Home Depot STEP 6,805,478 bytes from 1950 entities. Full suite 17/131 green (autosprink.html not unit-tested; browser-verified via preview screenshots + true-scale toggle + STEP export). Next: T9 consolidate surfaces.
- 2026-05-29: T11 building model + multi-space drawing parser shipped — new src/engine/building-model.js (normalizeBuilding: defaults hazard 'ordinary'/thicknessFt 0.5/wall type 'interior'/ceilingHeightFt 14/baseElevationFt 0, drops degenerate spaces <3 verts + zero-length walls, clamps opening offset/width within the wall span; buildingFromFloorPlan converts legacy {rooms} or {floors} -> stories with one space per room + perimeter exterior walls, no interior walls/openings/columns). Extended src/engine/floorplan-import.js with buildingFromDxf (LWPOLYLINE on ROOMS/SPACES -> spaces; LINE/LWPOLYLINE on WALLS/WALLS-EXT/WALLS-INT -> walls typed by layer; LINE on DOOR/WINDOW -> openings attached to nearest wall; CIRCLE/POINT on COLUMN -> columns; opts.layers remaps layer names; opts.unitsPerDrawingUnit scale) and buildingFromSvg (data-space polygons, data-wall lines/polylines w/ data-wall-type, data-opening lines attached to nearest wall, data-column circles; opts.unitsPerPx scale; added hasAttr() for valueless boolean data-* attrs). Layer/attribute-convention based, best-effort, deterministic — NOT CAD object-recognition AI; claim gates stay fail-closed. New tests/building-model.test.js (8 tests: normalize defaults+cleaning+clamping, legacy {rooms}/{floors} conversion, multi-space SVG 2 rooms + interior wall + door + column, multi-space DXF same). Full suite 18 files / 139 tests green (was 17/131). Backward compat preserved (existing floorplan-import tests unchanged). Next: T9 consolidate surfaces / T12 full per-space sprinkler system.
- 2026-05-29: T5 resolve-gate API shipped — POST /api/projects/:name/claim-gates/:code/resolve (authMiddleware + requireRole('admin')) in src/api/server.js + tests/resolve-gate.test.js (5 spawned-server tests). Added resolved_by/resolved_at/resolved_evidence_ref columns via ensureColumn in BOTH server.js initDatabase and seed.js. Approved gate-clearing evidence set: ahj_approval, professional_review, pe_signoff, manufacturer_approval, autosprink_packet, employee_signoff (status treated as 'present'). REJECTS (400) evidence_type best_effort_ai_layout and status best_effort — AI/best-effort output can NEVER clear a regulated gate. On success: inserts the evidence row (present) + flips gate blocked->cleared with who/what/when provenance; GET claim-gates reflects cleared + resolved_by. Full suite 15 files / 113 tests green (was 14/108). Studio UI DEFERRED; parity (AHJ/PE/AutoSprink/manufacturer) still requires real artifacts — fail-closed preserved. Next: T6 multi-floor / multi-zone.
- 2026-05-29: T13 accurate building CAD model shipped — buildCadModel in src/engine/cad-model.js now also consumes a normalized building (from building-model.js), detected by isBuilding() (stories[] each with spaces/walls) so legacy {rooms}/{floors} plans keep their existing paths. For a building: buildBuildingCad calls layoutBuilding() (system-layout.js, per-space column-aware heads + NFPA sizePipe) then per story emits a wall solid per segment (kind 'wall', reusing the a/b/center/lengthFt/heightFt/thicknessFt/rotationY/baseZ shape, carrying type:'exterior'|'interior' + layer WALLS/WALLS-INT + openings:[{offsetFt,widthFt,heightFt,sill,type}] metadata so the viewer can cut them), a column solid per column (kind 'column' {x,y,sizeFt,heightFt,baseZ} layer COLUMNS), and per-space pipe networks (branch/drop/cross-main/riser-tie/system-riser) + pendent heads; multi-story stacked via offsetSolidZ by baseElevationFt (offsetSolidZ now also offsets column baseZ). counts = {spaces,walls,interiorWalls,columns,openings,heads,pipes}. dxf-export.js updated: walls draw on WALLS (exterior/legacy) vs WALLS-INT (interior), columns extruded on COLUMNS, opening sill/head centerlines on OPENINGS — additive, existing single-floor DXF unchanged (wall block defaults to WALLS when no type). New tests/building-cad.test.js (9 tests: 2-space building w/ 1 interior wall + 1 door + 1 column -> >=1 interior-wall solid, >=1 column solid, opening recorded on a wall, heads in both spaces, counts populated, determinism; multi-story stacks by baseElevationFt; legacy floorPlan regression guard byte-identical w/ no column solids; DXF emits WALLS/WALLS-INT/COLUMNS/OPENINGS layers). Full suite 20 files / 153 tests green (was 19/144). Backward compat preserved (cad-model/multifloor/openclaw-cad tests unchanged). Geometry only — NFPA-13 schedule sizing, best-effort layer/convention parsing NOT CAD object-recognition AI, NO hydraulic calc, NOT AutoSprink/AutoCAD/AHJ/PE/manufacturer parity; claim gates stay fail-closed. Next: T14 studio render accurate building + full system (browser).
- 2026-05-29: T12 full per-space sprinkler system shipped — new src/engine/system-layout.js (layoutBuilding(building) consumes normalizeBuilding output). For EACH story and EACH space it lays out heads independently via the existing layoutRoom() on that space's own polygon (heads fill + are clipped to every space), then COLUMN-AWARE: any head within (sizeFt/2 + 0.5ft clearance) of a story column is nudged to a deterministic ring of nearby in-polygon candidates (+x,-x,+y,-y,diagonals) or dropped if none clear. Building-wide network reuses the riser->cross-main->branch->drop->head topology + NFPA sizePipe from cad-model.js: per-space cross-main sized for space head count, per-row branch lines sized for their head count, plus a per-story FEED MAIN sized for the story head count. Returns {stories:[{level, spaces:[{name,headCount,heads,branchLines,sizing}], storyHeadCount, feedMain}], totalHeadCount, pipeSchedule (diameter->linear ft, sorted), coverageOk (true iff every non-degenerate space has >=1 head), perSpace[]}. New tests/system-layout.test.js (5 hand-checked tests: 2-space 30x20 ordinary building -> both spaces get 6 heads each, totals sum, coverageOk true; heads clipped inside their own polygon; column on a baseline head grid point removes/shifts >=1 head vs no-column baseline; cross-main/branch/feed-main diameters match sizePipe for known counts; every non-degenerate space covered). Full suite 19 files / 144 tests green (was 18/139). Backward compat preserved (no existing file touched; engine consumes building-model output). Pure Node, deterministic, NOT CAD-recognition AI, NO hydraulic calc, NOT AutoSprink/AHJ/PE/manufacturer parity — claim gates stay fail-closed. Next: T13 accurate building CAD model.
- 2026-05-29: T9 consolidate surfaces shipped — retired the 725-line legacy static fake-data app.html (now a redirect to /workbench.html; fake $revenue/dashboard arrays gone); SPA catch-all in src/api/server.js now serves index.html (landing) for unknown routes, not the old dashboard. All UI flows index->login->workbench->studio. tests/consolidate-surfaces.test.js (4 spawned-server tests: landing at /, /app.html redirects + no fake content, unknown route->landing, workbench+studio still served). Full suite 26 files / 230 tests green; agentic rules ok. Routing/content verified by the spawned-server HTTP test (authoritative for served bytes). Next: T14 studio render accurate multi-space building (openings via OG boolean).
- 2026-05-29: T14 (partial) accurate-building studio render shipped — server.js sprinkler-bid accepts buildingSvg -> buildCadModel(building) (isBuilding:true); autosprink.html adds a 'Building drawing (multi-space SVG)' source, renders walls by type (interior/exterior color), columns, full per-space system + per-space breakdown panel, and cuts door/window openings via OpenGeometry Cuboid.subtract(Opening) (rotation-aligned, try/catch fallback to solid). tests/building-studio-api.test.js (2 tests). Full suite 27 files/232 tests green; agentic rules ok. Live render executed (status 'rendered 8 heads · 20 pipe segments'; panel '2 spaces · 1 int walls · 2 cols · 1 openings'; canvas 840x852; no console errors). MARKED [~] not [x]: pixel screenshot of the door-cut blocked by a preview-tooling hang this session — re-confirm visually next run. Next: re-verify T14 screenshot, then T20 Settings UI + component-library browser.
- 2026-05-29: T14 COMPLETED (was [~]) — fixed the door-cut bug: OpenGeometry Cuboid.subtract requires an OPERAND ARRAY and returns a NEW cut mesh, not in-place mutation. cutWall() now calls wallCuboid.subtract([openings], {}) and renderModel adds the RETURNED mesh (prior single-arg subtract(opening) threw 'requires an operand array' + was swallowed by try/catch -> walls rendered solid, no holes). PROOF via window.__wallDebug on a live 2-space building render: 4 exterior walls = 36 verts (solid box) each, interior wall with 1 door opening = 84 verts (hole cut). Status 'rendered 10 heads · 22 pipe segments'. Full suite 27 files/232 tests green; agentic rules ok. Screenshot tool hung (WebGL rAF capture) so verified by geometry vertex counts. Next: T20 Settings UI + component-library browser (BROWSER).
