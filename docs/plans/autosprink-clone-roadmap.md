# HaloFire AutoSprink-Clone — Roadmap to Done

**Goal:** A working, deployable internal-alpha AutoSprink/AutoCAD-class sprinkler
CAD + auto-bidder, built on the OpenGeometry kernel, that turns a floor plan
into a 3D-correct building + fire-sprinkler layout and a sourced bid.

This file is the autonomous driver's backlog. Each run: GX10 brain preflight →
pick the **first unchecked** task → TDD → focused verification → scoped commit →
check the box here → brain postflight. Never claim AHJ/PE/AutoSprink/manufacturer
parity without real evidence; those gates stay fail-closed.

Work dir: `C:/Users/dalla/OneDrive/Documents/HaloFire`. Rules: `E:/ClaudeBot/AGENTIC_RULES.md`.
Geometry MUST be built on OpenGeometry (npm `opengeometry`). Keep the 68+ tests green.

## Definition of Done
- [ ] Floor plan (SVG/DXF) → building shell + full sprinkler system in 3D via OpenGeometry.
      PARTIAL (DoD verify wqui7b5uv): SVG path live end-to-end; structural solids
      (walls/mains/branches/riser/columns) are kernel-backed. GAPS: (a) DXF import is
      engine-only/unwired through the server+UI → see T21; (b) live viewer heads/drops are
      plain Three primitives, not rendered parts → see REAL-PART RENDER epic R1–R5.
- [x] Engineering-correct layout: NFPA-13 spacing + schedule pipe sizing + hydraulic check.
      (best-effort; single-path + full network balance + geometric compliance, all wired +
      tested. NOT a sealed PE/Hardy-Cross calc. Verified wqui7b5uv d2.)
- [x] CAD interchange export: DXF **and** STEP/IFC/STL from OpenGeometry. (DXF first-party +
      tested; STEP/IFC/STL via the OpenGeometry WASM kernel, wired but runtime-unverified in
      repo. Verified wqui7b5uv d3.)
- [ ] Full bid scope priced from real pricebooks (approaching real bid totals).
      PARTIAL (DoD verify wqui7b5uv d4): bare materials (heads/pipe/fittings/hangers) ARE
      priced from real ingested ARGCO/Victaulic/FFF rows. GAPS: buildFullScopeBid is unwired
      (system components + soft costs are fallback constants/percentages) and there is no
      calibration to the real $792,543.84 Home Depot total → see T22.
- [x] Studio UI usable end-to-end; evidence gates surfaced; nothing faked. (best-effort;
      gates fail-closed + surfaced; 45 studio/gate tests green. Verified wqui7b5uv d5.)
- [x] One-command verifier green; review packet current. (scripts/verify-internal-alpha.ps1
      runs the full suite — 34 files / 320 tests green; packet refreshed to
      docs/reviews/2026-05-29-autosprink-clone-review-current.md. Verified wqui7b5uv d6.)
- [x] Deployable (documented run/build path). (docs/DEPLOY.md local run path verified
      end-to-end; public VPS/HTTPS at halofire.rankempire.io still pending DNS+certbot.
      Verified wqui7b5uv d7.)

## Task Queue (do the first unchecked, one per run)

- [x] **T1 — OpenGeometry STEP/IFC/STL export.** DONE 2026-05-29. Studio builds an
      OGSceneManager from the rendered shapes (addCuboid/addCylinderToCurrentScene)
      and exports via exportCurrentSceneToStep/Ifc/Stl. Buttons for DXF/STEP/IFC/STL.
      Verified in browser: 40×30 room → STEP 79,068 bytes from 23 OpenGeometry
      entities (4 walls + floor + 12 pipes + 6 heads); IFC also non-empty.
- [x] **T2 — Hydraulic calc engine.** DONE 2026-05-29. `src/engine/hydraulics.js`
      (pure/deterministic): Hazen-Williams friction loss (psi/ft), velocity (fps),
      remote-area demand (NFPA 13 density × area — light 0.10/1500, ordinary
      0.18/1500, extra 0.30/2500), single-path requiredPressureAtRiser (riser →
      cross-main → remote branch friction + 0.433 psi/ft elevation + 7 psi min head),
      and flagSchedule velocity(>32 fps)/loss warnings. Hand-computed vitest cases.
      Studio surfacing still pending (engine + tests are the deliverable).
- [x] **T3 — Full bid scope.** DONE 2026-05-29. Add system components (alarm/check valve, FDC,
      backflow, riser trim, inspector's test, fire pump if required) + soft costs
      (permit/design/freight as labelled assumptions) priced via pricebook resolver,
      behind a clear "estimate" label. Tests. Show in bid.
- [x] **T4 — DXF/PDF floor-plan import.** DONE 2026-05-29. `floorPlanFromDxf` in
      floorplan-import.js parses the DXF ENTITIES section: LWPOLYLINE (code 10/20
      vertex pairs), POLYLINE/VERTEX, and best-effort closed-loop assembly from
      LINE segments by endpoint chaining. opts.layer (code 8) filter,
      opts.unitsPerDrawingUnit scale to feet, opts.hazard default ordinary; reuses
      normalizeFloorPlan; skips <3-vertex shapes. PDF import DEFERRED. Tests.
- [x] **T5 — Resolve-gate workflow.** DONE 2026-05-29. Admin-only route
      `POST /api/projects/:name/claim-gates/:code/resolve` in src/api/server.js
      (+ resolved_by/resolved_at/resolved_evidence_ref columns via ensureColumn in
      both server.js initDatabase and seed.js). Requires a real evidence object;
      only ahj_approval/professional_review/pe_signoff/manufacturer_approval/
      autosprink_packet/employee_signoff with status 'present' may clear. Rejects
      (400) best_effort_ai_layout type and best_effort status — AI output can NEVER
      clear a gate. On success inserts the evidence row (present) and flips the gate
      blocked→cleared with who/what/when. Studio UI deferred. Tests prove gate only
      clears with a real evidence row, never from AI output.
- [x] **T6 — Multi-floor / multi-zone.** DONE 2026-05-29. buildCadModel now accepts an
      optional floorPlan.floors array [{level,baseElevationFt,ceilingHeightFt?,rooms}];
      each floor builds its own independent riser->cross-main->branch->drop->head network
      with NFPA schedule sizing, then its solids are offset in Z by baseElevationFt so
      floors stack. counts aggregate across floors; legacy floorPlan.rooms still builds a
      single floor at base 0 unchanged. Walls carry baseZ (default 0) and dxf-export honors
      it. Tests in tests/cad-model-multifloor.test.js. Studio surfacing best-effort/deferred.
- [x] **T7 — OpenClaw CAD automation adapter.** `src/cad/openclaw-cad.js` that
      builds generate_3d_model / generate_dxf payloads from the cad-model and
      invokes the OpenClaw bridge when the GX10 gateway is live (graceful skip when
      unreachable). Tests on payload shape.
- [x] **T8 — Head perf + true-scale toggle.** DONE 2026-05-29. Studio renders the
      900+ heads + drops via Three InstancedMesh (one draw call each) instead of
      per-item OpenGeometry cylinders; CAD-structural geometry (walls/mains/
      branches/riser) stays on the OpenGeometry kernel. Home Depot render ~9s→~4s
      (~59 kernel shapes + 2 instanced meshes vs ~1900). Added "Exaggerate pipe
      radius (×6)" View toggle (off = true NFPA scale); re-renders from the stored
      model. STEP/IFC/STL export rebuilds the COMPLETE kernel scene on-demand
      (buildExportScene from cadModel.solids, true-scale) — verified Home Depot
      STEP = 6,805,478 bytes from 1950 entities, no regression. Browser-verified.
- [x] **T9 — Consolidate surfaces.** DONE 2026-05-29. Retired the 725-line legacy
      static fake-data app.html dashboard — app.html is now a tiny redirect to
      /workbench.html (old link still works, fake data gone). The SPA catch-all in
      server.js now serves index.html (landing) for unknown routes instead of the
      old dashboard, so all UI flows index -> login -> workbench -> studio.
      tests/consolidate-surfaces.test.js (4 spawned-server tests). Full suite
      26 files / 230 tests green.

## ACCURATE BUILDING epic (user priority): real buildings from drawings + full systems
Current gap: import treats each polygon as ONE room and grids its bounding box;
the "building" is just an extruded perimeter wall + flat slab. Real drawings have
interior walls, multiple spaces, doors/windows, and columns; the system must
cover EVERY space with a proper building-wide network. Build that.

- [x] **T11 — Building model + multi-space drawing parser.** DONE 2026-05-29. New
      `src/engine/building-model.js`: a building schema
      `{ name, units, stories:[{ level, baseElevationFt, ceilingHeightFt, spaces:[{name,polygon,hazard}], walls:[{a,b,thicknessFt,type:'exterior'|'interior',openings:[{offsetFt,widthFt,heightFt,sill?,type:'door'|'window'}]}], columns:[{x,y,sizeFt}] }] }`
      + `normalizeBuilding(spec)`. Extend floorplan-import.js with
      `buildingFromDxf(text,opts)` / `buildingFromSvg(text,opts)` that extract
      walls (LINE/LWPOLYLINE on a WALLS layer → segments w/ thickness+type),
      spaces (closed room polygons / labeled regions), doors+windows (DOOR/WINDOW
      layers or SVG attrs → wall openings), and columns (COLUMN layer / circles).
      Layer/attr-convention based, best-effort (NOT CAD recognition AI). Backward
      compat: a single-polygon input still yields one space. Deterministic; tests.
- [x] **T12 — Full per-space sprinkler system.** New `src/engine/system-layout.js`:
      `layoutBuilding(building)` lays out EACH space independently (reuse
      layoutRoom per space polygon, clipped to that space, column-aware: drop/shift
      heads that collide with columns), then builds a building-wide network — a feed
      main, per-space cross-mains/branch lines, drops, heads — so coverage fills all
      spaces. Returns per-space + whole-building head counts, pipe schedule, and a
      `coverageOk` flag (every space has heads). Deterministic; hand-checked tests.
- [x] **T13 — Accurate building CAD model.** DONE 2026-05-29. Extend `buildCadModel` to consume a
      building: emit interior + exterior wall solids (carry opening metadata
      {offsetFt,widthFt,heightFt,sill,type} so the viewer can cut them), column
      solids, per-space pipe networks, multi-story stacking. counts include
      spaces/walls/columns/openings. Backward compat with floorPlan.rooms/floors
      preserved. dxf-export emits walls/columns/openings on layers. Tests.
- [x] **T14 — Studio: render accurate building + full system.** DONE 2026-05-29.
      server.js sprinkler-bid accepts a multi-space building drawing (`buildingSvg`)
      -> buildCadModel(building) (isBuilding:true). Studio: "Building drawing
      (multi-space SVG)" source; renders walls colored by type (interior vs
      exterior), columns (Cuboid), the full per-space system, and a per-space
      breakdown panel; door/window openings cut via OpenGeometry boolean. FIXED a
      real bug: subtract requires an OPERAND ARRAY and returns a NEW cut mesh
      (cutWall now calls wallCuboid.subtract([openings], {}) and renders the
      returned mesh — the prior single-arg call threw + was swallowed -> solid wall).
      VERIFIED: tests/building-studio-api.test.js (2 tests); full suite 27 files /
      232 tests green; live studio render of a 2-space building (status "rendered 10
      heads · 22 pipe segments") with PROGRAMMATIC PROOF the door-cut worked —
      window.__wallDebug shows the interior wall (1 opening) at 84 verts vs the 4
      exterior walls at 36 verts (a hole was cut). Screenshot tool hung this session
      (WebGL rAF capture), so verified by geometry vertex counts instead.
- [x] **T10 — Deploy packaging + review packet refresh.** DONE 2026-05-29.
      New truthful packet docs/reviews/2026-05-29-autosprink-clone-review.md
      (what works w/ file cites, exact test count, explicit "still NOT done /
      NOT claimed" section). New docs/DEPLOY.md (env vars, seed + run path,
      one-command verifier). scripts/verify-internal-alpha.ps1 now runs the FULL
      `npx vitest run` (no file list) so it can't drift from the test count;
      seed + agentic-rules steps kept. Docs/script only — no behavior change.

## COMPONENT CAD LIBRARY + AUTOSPRINK PARITY epic (user priority)
Every fire-sprinkler component must have a CAD-accurate 3D model, sourced:
catalog download -> OpenSCAD-generated -> SAM 3.1-reconstructed -> gated
placeholder. Full AutoSprink parity (no missing modules/components), tracked +
fail-closed until real. Auto-bid = takeoff from the component set. Any doc we
can't auto-source (catalogs, cut sheets, approvals, AutoSprink ref, OpenSCAD
binary) is user-uploaded/linked in Settings, wired to the evidence/gate system.
Grounding: OpenSCAD NOT installed locally (generate .scad source; render needs
the binary = a Settings dependency). SAM 3.1 = GX10 'sam3' via the OpenClaw
bridge (best-effort, graceful skip). Parity claim stays BLOCKED until complete.

- [x] **T15 — Component registry + AutoSprink parity inventory.** `src/components/registry.js`:
      canonical list of every component/module (heads pendent/upright/sidewall/
      concealed/ESFR; pipe sch10/40; fittings tee/elbow/coupling/reducer/cap;
      grooved couplings; valves alarm/check/butterfly/OS&Y/PRV; FDC; backflow;
      riser+trim; gauges; hangers/seismic bracing; drains; inspector test; signs)
      + parity matrix (present vs missing) + fail-closed AUTOSPRINK_PARITY gate
      blocked until inventory complete w/ real evidence. Node-tested.
- [x] **T16 — Component 3D-model resolver pipeline.** `src/components/model-resolver.js`:
      resolve each component's model in priority order catalog-file -> OpenSCAD ->
      SAM3.1 -> placeholder+gate; record per-component source/status. Injected
      sourcers; Node-tested.
- [x] **T17 — OpenSCAD parametric generators.** `src/components/openscad/*`:
      deterministic .scad source per component type (Node string-gen, tested) +
      render adapter (openscad CLI when present, else gated 'openscad_not_installed').
- [x] **T18 — SAM 3.1 reconstruction adapter.** `src/components/sam-reconstruct.js`:
      catalog image -> SAM3.1 segment -> mesh via injected GX10/OpenClaw invoker;
      graceful skip when gateway down. Node-tested payload + skip.
- [x] **T19 — Settings + documentation upload/link API.** DONE 2026-05-29. Authenticated routes to
      upload OR link missing docs (catalogs, cut sheets, approvals, AutoSprink ref,
      OpenSCAD path); each -> project_evidence row / dependency status; missing-doc
      resolver queue. Node-tested (spawned-server pattern).
- [x] **T20 — Settings UI + component-library browser (BROWSER).** `settings.html` ships:
      doc upload/link slots (`/api/settings/documents`, admin add round-trip verified),
      stack dependencies (`/api/settings/dependencies`), the AUTOSPRINK parity matrix
      (`/api/parity` — 12 functional rows present, 3 gated rows shown gated, gate BLOCKED /
      parityAchieved false), and a component-library browser (registry.js
      `buildParityInventory({})` → all 34 components honestly shown "no model", 0 present).
      Linked from workbench + studio headers. Verified via preview_eval/DOM (1500px).
      NOTE (honesty): "render catalog/generated models in the 3D layout" is intentionally
      deferred — zero real models exist yet (all 34 gated/missing), so there is nothing to
      render; that lands when real catalog/generated models are attached and the gate is
      addressed. T20 surfaces parity/inventory but does NOT flip the gate.
- [ ] **T21 — Wire DXF import end-to-end (DoD d1 gap).** floorPlanFromDxf/buildingFromDxf
      exist + are tested but are NOT imported/called in src/api/server.js and have no UI
      control. Wire them into the sprinkler-bid pipeline (accept `dxf`/`buildingDxf` body
      like `svg`/`buildingSvg`) + add a studio planSource option, so a DXF drawing drives the
      3D model through the running system. TDD (spawned-server) + browser-verify.
- [ ] **T22 — Wire full-scope bid + calibrate (DoD d4 gap).** buildFullScopeBid is unwired;
      the live bid prices bare materials only. Wire buildFullScopeBid into the sprinkler-bid
      response (system components + soft costs, every line honestly flagged
      fallback_estimate/soft_cost_assumption), surface the full-scope total in the studio bid
      panel alongside bare-materials, and add a best-effort calibration note vs the real
      Home Depot $792,543.84 bid-log total (NOT a parity/accuracy claim). TDD + browser.

## AUTOSPRINK PARITY epic (the goal): close functional gaps to AutoSprink feature parity
HONESTY CONTRACT: "parity" is tracked, not asserted. The `AUTOSPRINK_PARITY_INCOMPLETE`
gate (registry.js) stays BLOCKED until every parity-matrix row is genuinely PRESENT
*and* the regulated/manufacturer rows have real evidence (manufacturer-exact catalog
models + licensed PE/AHJ review, user-supplied via Settings/T19). The P-tasks below
build the FUNCTIONAL engine parity we can build honestly; they NEVER flip the gate or
claim AutoSprink/AutoCAD parity, AHJ/permit/PE/manufacturer/fabrication readiness.

### Parity matrix (AutoSprink feature area -> HaloFire status)
- Drawing import (DXF/SVG) — PRESENT (T4/T11); PDF import — MISSING (deferred)
- Building + multi-space modeling, walls/openings/columns — PRESENT (T11-T14)
- Head layout (NFPA-13 spacing, hazard) — PRESENT (sprinkler-layout)
- Pipe routing + schedule sizing — PRESENT; demand-balanced hydraulic sizing — PARTIAL (single-path est, T2)
- Full hydraulic network balance (K-factor Q=K√P, node pressures, system demand) — PRESENT (P1, best-effort iterative est)
- NFPA-13 compliance checker (spacing/coverage/area/density/obstruction/branch limits) — PRESENT (P2, best-effort geometric, clears no gate)
- Hanger / seismic support placement + schedule — MISSING -> P3
- Component CAD model library (per-component models) — SCAFFOLD only (T15/16/17); generated models — MISSING -> P4; manufacturer-exact catalog models — GATED (user/Settings)
- Submittal package (head/pipe schedules, hydraulic summary, BOM) + PDF report — MISSING -> P5
- Parity matrix + status surface + gate wiring — MISSING -> P6
- 3D model + CAD export (DXF/STEP/IFC/STL) — PRESENT (T1/T8/T13)
- Bid / takeoff / BOM / full scope — PRESENT (T3)
- Evidence gates + resolve workflow + Settings doc upload — PRESENT (T5/T19)
- AHJ/PE stamp, manufacturer approval — GATED by design (real-world evidence only)

- [x] **P1 — Full hydraulic network balance.** DONE 2026-05-29. New
      `src/engine/hydraulic-network.js` (re-exported from hydraulics.js):
      `kFactorFlow(k,psi)=k√psi` head discharge + `balanceNetwork({network|cadModel,
      hazard,kFactor?,C?,sourcePsi?,minHeadPressurePsi?})` — picks the remote branch,
      orders heads remote-first, seeds the most-remote head at min operating psi, walks
      inward accumulating Q=K√P node-by-node (each upstream node = downstream psi +
      Hazen-Williams friction over the inter-head segment carrying the accumulated flow),
      then adds cross-main/main-tie/riser friction (full branch demand) + 0.433 psi/ft
      head elevation to get requiredSourcePsi; returns {heads[],totalDemandGpm,
      requiredSourcePsi,segments[],balanced}. Best-effort ITERATIVE estimate, NOT a sealed
      Hardy-Cross solve / stamped calc. Hand-computed vitest cases in
      tests/hydraulic-network.test.js. Studio Hydraulics panel surfacing best-effort/deferred.
- [x] **P2 — NFPA-13 compliance checker.** DONE 2026-05-29. `src/engine/nfpa-compliance.js`: check a
      layout/building against public NFPA-13 rules (max coverage area & spacing & min
      spacing & max-to-wall by hazard, max heads per branch/system, obstruction note) ->
      pass/fail findings list. Deterministic; tests. Surface as a compliance panel + a
      COMPLIANCE_REVIEW gate note (still requires PE sign-off; never auto-clears).
- [x] **P3 — Hanger + seismic support placement.** DONE 2026-05-29. `src/engine/supports.js`: place hangers
      along pipes at NFPA-13 max spacing (size-dependent) + seismic brace intervals -> support
      solids + counts feeding the BOM/takeoff. Tests. Render in studio + add to cad-model.
- [x] **P4 — Generate component models -> populate parity inventory.** DONE 2026-05-29. Pipeline that runs the
      T17 OpenSCAD generators for every registry component to produce a model (and renders STL
      where the openscad binary exists, e.g. VPS), marking each 'generated' in
      buildParityInventory. Generated != manufacturer-exact, so the PARITY gate stays blocked
      on catalog+PE evidence; this raises functional coverage + drives the BOM model refs. Tests.
- [x] **P5 — Submittal package + PDF report.** DONE 2026-05-29. `src/engine/submittal.js`: assemble a submittal
      package (project header, head schedule, pipe schedule, hydraulic summary, BOM, gate
      status) as structured data + drive OpenClaw generate_pdf_report via an injected invoker
      (graceful skip). Labelled best-effort, NOT a stamped submittal. Tests on package shape.
- [x] **P6 — Parity matrix module + status API + gate wiring.** DONE 2026-05-29. `src/engine/parity-matrix.js`:
      compute the matrix above from actual module/inventory state -> {area,status,evidence}.
      `GET /api/parity` returns it + AUTOSPRINK_PARITY gate (blocked until all required areas
      PRESENT with real evidence). Tests prove the gate stays blocked while any area is missing
      and that generated-only component models do NOT satisfy the manufacturer/PE rows.
- [x] **P7 — Parity + component-library + compliance UI (BROWSER).** DONE 2026-05-29.
      Parity matrix + component library shipped in T20 (settings.html). P7 adds the
      studio-side compliance + full-hydraulics + submittal surface. API (ultra workflow
      wn1mpkom8, TDD, 3 adversarial verifiers all PASS): sprinkler-bid now returns
      `hydraulicNetwork` (P1 balanceNetwork full balance) + `compliance` (P2 checkCompliance
      NFPA-13), and a new `POST /api/projects/:name/submittal` returns a downloadable
      buildSubmittal package (Content-Disposition attachment). UI (autosprink.html): the
      Hydraulics panel now shows the iterative network balance (req @ source, network demand)
      alongside the single-path estimate; a Compliance panel lists per-rule PASS/WARN/FAIL +
      the "professional review required" honesty note; a "Download submittal (.json)" button.
      Browser-verified (preview_eval/DOM, 1500px): Home Depot → network 509 gpm / 48.4 psi @
      source, compliance 2 fails surfaced honestly, submittal downloads with submittalReady
      false + all header honesty flags false + AUTOSPRINK_PARITY blocked. Full suite 34 files
      / 320 tests green (+1 file / +6 tests). HONESTY/fail-closed held — nothing flips a gate;
      compliance "pass" is geometric-only; building path skips the balance rather than
      fabricating numbers.

## REAL-PART RENDER epic: the 3D builder renders actual CAD part meshes, not primitives
USER REQUIREMENT (2026-05-29): "the 3d builder must use autocad 3d rendered parts for the
sprinklers and all other components used to construct the sprinkler system." Today the studio
(autosprink.html) draws PRIMITIVES — SphereGeometry heads + CylinderGeometry drops/pipes. The
component-model pipeline (T15–T19, P4) generates per-component OpenSCAD/SAM models but they are
NOT wired into the 3D render. This epic instances a real 3D part mesh at every component.
HONESTY CONTRACT (unchanged, fail-closed): parts come from (a) manufacturer/AutoCAD CATALOG
models (DWG/STEP→web mesh, user-supplied/licensed via Settings) — the true "AutoCAD parts"; or
(b) BEST-EFFORT recreations via the OpenSCAD/SAM stack. Generated parts render real geometry but
are NOT manufacturer-exact and NEVER satisfy AUTOSPRINK_PARITY / manufacturer / AHJ / PE gates —
those stay BLOCKED until real licensed catalog models + professional review are attached. Each
rendered part must show its true source (catalog | generated | missing); a generated part is
never labelled manufacturer-exact.
- [x] **R1 — Part-mesh build pipeline.** DONE 2026-05-29 (workflow we28777td, TDD). New
      src/components/part-mesh.js: buildPartManifest({scadRunner,outDir,write}) walks the registry,
      runs T17 generateScadFor + renderScadToStl through an INJECTED runner (never spawns openscad
      itself), writes parts/<key>.stl only when a real STL renders (never fabricated), and emits the
      manifest (key→{source:'generated'|'missing',file,format,manufacturerExact:false,present}).
      scripts/build-parts.mjs ("npm run build:parts") wraps the real openscad CLI, degrades to an
      all-'missing' manifest (exit 0) when the binary is absent. .gitignore ignores parts/*.stl
      (allows parts-manifest.json). With a mock runner: generated=22, missing=12 (34 total).
- [x] **R2 — Parts manifest API + resolver.** DONE 2026-05-29. `GET /api/parts` (authMiddleware,
      before the SPA catch-all) serves the on-disk manifest or a live all-'missing' registry view;
      manufacturerExactCount + a per-entry sanitize (coerce manufacturerExact→false unless source is
      a real catalog/manufacturer source) so a tampered manifest can't leak a false claim; parity
      gate hardcoded fail-closed 'blocked'. parts/<key>.stl reachable via the repo-root static mount.
      Verified live: all-missing branch (34 missing, count 0, blocked) + tamper test (generated
      +manufacturerExact:true → coerced false; catalog kept; count derived). Full suite 36 files /
      329 tests green (+2 files, +9 tests). Honesty/correctness/test-truth adversarially verified
      (the lone 'fail' verdict was a wrong-cwd false negative — searched E:/ClaudeBot, not HaloFire;
      independently re-confirmed files exist + suite green).
- [x] **R3 — Studio render swap (BROWSER).** DONE 2026-05-29 (heads). autosprink.html now loads
      the head part mesh from GET /api/parts via STLLoader (/parts/head_pendent.stl), normalizes
      (center, OpenSCAD Z-up→three Y-up, scale to ~1ft) and renders all 936 sprinkler heads with
      InstancedMesh using the real STL geometry (verified: usingPartMesh=true, 1716 verts/head vs
      ~150 for the old sphere), fail-soft to a primitive when no part mesh, status reads "heads:
      generated CAD part (best-effort, not mfg-exact)". Browser-verified via preview_eval/__headDebug.
      Generated locally with the official OpenSCAD portable (build:parts → 22 STLs); gate stays blocked.
      SCOPE NOTE (honest): only HEADS render as discrete part meshes today because buildCadModel
      only emits head solids as placeable points; fittings/valves/riser/FDC part placement needs
      the cad-model to emit those component positions → R6.
- [ ] **R6 — cad-model emits discrete component placements + studio renders their part meshes.**
      buildCadModel should emit placeable solids for fittings (tee/elbow/coupling at pipe joints),
      valves + riser assembly + FDC (at the riser), and hangers (from supports.js) so the studio
      can instance their part meshes (valve_butterfly, fdc, riser_assembly, fitting_tee, ...) the
      same way heads now render. TDD on the cad-model emission + browser-verify the render.
- [ ] **R4 — Settings per-component part upload/link (BROWSER+API).** Attach a manufacturer
      AutoCAD/STEP/glTF/STL part per component (extends T19 doc slots to per-component assets);
      a real catalog part with evidence overrides the generated mesh and marks source 'catalog'
      (still PE/AHJ-gated; does not by itself clear AUTOSPRINK_PARITY). Browser-verified.
- [ ] **R5 — VPS parts render + serve + redeploy.** Ensure openscad on the VPS renders the parts,
      serve `parts/`, redeploy the stack, and confirm the studio loads real meshes over the web.

## AUTO-SOURCE epic: OpenClaw autonomously finds/creates every part; user only fills the residual
USER REQUIREMENT (2026-05-29): "openclaw [is] to be proactively finding all the cad parts or
creating them from image catalogs. this is to be ai automated. the user at most will point the
halofire cad studio at a document or url for parts that need to be imported but only ones
openclaw hasn't already found on its own." This plugs OpenClaw-driven sourcers into the existing
T16 model-resolver (resolveComponentModel tries injected sourcers in priority) and runs them as
an autonomous loop across the whole registry.
GROUNDED IN REAL OPENCLAW TOOLS (gx10_status capabilities, 2026-05-29): web_search, web_fetch,
browser (catalog discovery); generate_3d_model (bridge alias) + hal_sam_status / SAM 3.1
(create-from-image); generate_dxf; image_generate. All reached via the governed
HAL `/codex-bridge/invoke` path (mcp openclaw-gx10 / hal-bridge).
HONESTY CONTRACT (fail-closed, unchanged): autonomously FOUND (public web/TraceParts/GrabCAD)
or CREATED (SAM/generate_3d_model/OpenSCAD) parts are BEST-EFFORT with recorded provenance +
license; they are NOT manufacturer-exact and NEVER clear AUTOSPRINK_PARITY / manufacturer / AHJ
/ PE gates (stay BLOCKED). Only a genuinely licensed manufacturer model with real evidence is
'catalog/manufacturer-exact', and even then parity needs PE/AHJ review. The loop must be
FAIL-SOFT: OpenClaw is intermittently unreachable (direct probe HTTP_UNREACHABLE while the bridge
reports ready) — when it is down, sourcing degrades to OpenSCAD/missing, never fabricates a found
model or a license.
- [x] **S1 — OpenClaw catalog-discovery sourcer.** DONE 2026-05-29 (workflow wliod8ee5, TDD).
      src/components/auto-source.js makeCatalogSourcer({invoker}) + buildCatalogSearchQuery: drives
      the injected OpenClaw bridge to FIND public CAD; fail-soft (absent/non-function/throws/empty →
      null); on a usable {url,format,data?,license?,manufacturer?} returns source:'catalog' with
      provenance+license passed through (never fabricated), manufacturerExact:FALSE. Honesty:
      machine-found provenance is unverified → registrySourceFor downgrades resolver 'catalog' →
      registry 'generated' so it can never count toward the manufacturer gate.
- [x] **S2 — OpenClaw image→3D creation sourcer.** DONE 2026-05-29. makeImageReconstructSourcer
      ({invoker,gen3dInvoker}) reuses T18 reconstructComponentModel (SAM 3.1) with a generate_3d_model
      fallback; source='generated' subtype sam|gen3d, manufacturerExact:false; fail-soft → null.
- [x] **S3 — Autonomous sourcing loop.** DONE 2026-05-29. runAutoSource walks the registry via
      resolveComponentModel (priority catalog→openscad→sam), buckets found/created/generated/missing,
      builds fail-closed modelStatusByKey, optional mesh write + injected recorder (Brain logging),
      idempotent + fully fail-soft. HONESTY FIX (adversarial verifier caught a real violation): the
      returned parityGateStatus now derives from manufacturerParityComplete (gate-grade) — ALWAYS
      'blocked' for auto-sourced models — NOT from the functional inventory (which counts 'generated'
      as present and would wrongly read 'clear' at 100% coverage); functional coverage reported
      separately as `functionalCoverage` (explicitly NOT a parity claim). Test proves: catalog finds
      all 34 → functionalCoverage.complete=true BUT parityGateStatus='blocked'. Suite 37 files/349
      tests green (+1 file/+20 tests). NOTE: built+unit-tested with MOCK invokers; real autonomous
      run needs OpenClaw reachable (currently HTTP_UNREACHABLE) → validated on the VPS in S5. Cron
      scheduling of the loop is part of S5.
- [x] **S4 — "Needs import" surface (studio/Settings) (BROWSER).** DONE 2026-05-29. settings.html
      Component Library now reads the LIVE GET /api/parts manifest (was the registry all-missing
      inventory): shows "<N> auto-sourced (best-effort, <M> manufacturer-exact) · <K> need import.
      OpenClaw finds/creates parts automatically; you only import the rest. Parity gate stays
      blocked." A dedicated "Needs import (K)" block lists only the residual parts OpenClaw hasn't
      sourced; clicking any needs-import pill jumps to + prefills the documentation importer
      (manufacturer_cut_sheets slot) for that part. Browser-verified (22 auto-sourced/12 need import,
      0 manufacturer-exact, gate blocked; click fitting_cap → docType=manufacturer_cut_sheets,
      placeholder "catalog URL / file for: fitting_cap", input focused). NOTE: the current per-part
      import path is the existing Settings doc slots; a dedicated per-component asset-upload endpoint
      that OVERRIDES the generated mesh with a user-attached catalog model is R4 (still pending).
- [ ] **S5 — VPS ↔ OpenClaw wiring + schedule.** The VPS HaloFire stack reaches OpenClaw (GX10)
      via the governed bridge/SSH for agentic runs; schedule the S3 loop; fail-soft + observable
      (records last-run, found/created/failed counts). Confirm end-to-end on the VPS.

## Log
- 2026-05-29: S4 residual "needs import" surface shipped (browser-verified). settings.html loadLibrary now fetches the LIVE GET /api/parts manifest instead of the registry all-missing inventory: groups components by category with per-part source pills (catalog=green / auto-built=gold / needs-import=red), a summary "<N> components · <gen> auto-sourced (best-effort, <mfg> manufacturer-exact) · <miss> need import. OpenClaw finds/creates parts automatically; you only import the rest. Parity gate stays <status>.", and a dedicated "Needs import (K)" block listing only the residual parts OpenClaw hasn't sourced. Clicking any needs-import pill sets docType=manufacturer_cut_sheets, prefills the importer placeholder "catalog URL / file for: <key>", and focuses+scrolls to the doc importer. Verified via preview_eval/DOM: 34 components, 22 auto-sourced / 0 manufacturer-exact / 12 need import, gate 'blocked'; click fitting_cap → importer focused + prefilled. HONESTY held: auto-built parts labeled best-effort/not-manufacturer-exact, gate stays blocked, the residual is exactly what the user imports (matches the requirement "user at most points the studio at a doc/URL for parts OpenClaw hasn't already found"). Static page over the tested /api/parts endpoint; suite unchanged (37 files/349 green). NOTE: per-part import currently routes to the existing Settings doc slots; a dedicated per-component asset-upload endpoint that overrides the generated mesh = R4 (pending). Next: R4 (per-component upload override), S5 (VPS real OpenClaw run + cron), R6 (cad-model component placements), T21/T22.
- 2026-05-29: S1+S2+S3 OpenClaw autonomous part-sourcing shipped (ultra workflow wliod8ee5, TDD; 37 files/349 tests green, +1 file/+20 tests). New src/components/auto-source.js (no .html touched; reuses T16 resolveComponentModel, T18 reconstructComponentModel, T17 generators, R1 part-mesh handoff, T7 injected-invoker shape). makeCatalogSourcer (S1: OpenClaw web_search/web_fetch find public CAD, fail-soft, provenance+license passed through never fabricated, manufacturerExact:false), makeImageReconstructSourcer (S2: SAM 3.1 via T18 + generate_3d_model fallback, fail-soft), makeOpenScadSourcer (wraps T17), runAutoSource (S3: resolveComponentModel priority catalog→openscad→sam, buckets found/created/generated/missing, fail-closed modelStatusByKey, optional mesh-write + injected Brain recorder, idempotent, fully fail-soft). HONESTY: registrySourceFor DOWNGRADES resolver 'catalog'→registry 'generated' (machine-found provenance unverified; never registry 'catalog'/'manufacturer') so machine coverage can't satisfy the manufacturer gate. ADVERSARIAL VERIFIER CAUGHT A REAL VIOLATION (honesty verdict FAIL, correctly): the returned parityGateStatus was computed from registry.buildParityInventory→parityGateStatus, and since registry hasRealModel counts 'generated' as a real source, a sourcer covering all required components flipped parityGateStatus to 'clear' — an overclaim for best-effort geometry (the core manufacturer gate via manufacturerParityComplete stayed false, but the exposed field could read 'clear'). FIX (this session): runAutoSource.parityGateStatus now derives from manufacturerParityComplete(modelStatusByKey) (gate-grade) → ALWAYS 'blocked' for auto-sourced models; functional coverage moved to a separate `functionalCoverage:{present,total,complete}` field explicitly labeled NOT a parity claim; docstrings corrected; added a test asserting that with catalog finding ALL 34 components functionalCoverage.complete=true YET parityGateStatus='blocked' (would have failed pre-fix). correctness + test-truth verdicts PASS (confirmed reuse signatures, fail-soft, no fabrication, no .skip/.only, assertions non-vacuous). Full suite 37 files/349 tests green. NOT manufacturer/AutoSprink/AHJ/PE parity — gate stays BLOCKED. CAVEAT: built+tested with MOCK invokers; OpenClaw currently HTTP_UNREACHABLE so the REAL autonomous run + cron scheduling is validated on the VPS (S5). Next: S4 (residual "needs import" UI), S5 (VPS run + cron + wiring), R6 (cad-model component placements), T21/T22 (DoD gaps).
- 2026-05-29: R3 studio render swap (heads) shipped + the 3D builder now renders a real CAD part mesh for the sprinklers. autosprink.html: import STLLoader (three/examples/jsm/loaders/STLLoader.js — served via /vendor/three → node_modules/three); new loadPartMeshes() fetches GET /api/parts, finds head_pendent (present+file), STLLoader.loadAsync('/parts/head_pendent.stl'), normalizes geometry (computeBoundingBox → center at origin, rotateX(-90°) OpenSCAD Z-up→three Y-up, scale to ~1ft for visibility, computeVertexNormals), stores headPartGeo + partsSource ('generated'|'catalog'); fail-soft (load error → primitive). addInstancedHeads uses headPartGeo (InstancedMesh, metalness 0.55) when present, else SphereGeometry(0.6) fallback. initStudio awaits loadPartMeshes after kernel; status shows head-part source; end-of-generate status appends "heads: generated CAD part (best-effort, not mfg-exact)" or "primitive (no part mesh)". window.__headDebug hook for verification. To get real STLs locally (openscad not installed): downloaded the official OpenSCAD 2021.01 portable (files.openscad.org, 21.8MB) and made scripts/build-parts.mjs honor OPENSCAD_BIN (the VPS .env already sets it) — ran OPENSCAD_BIN=<portable> npm run build:parts → 22 STLs generated, 12 missing, manufacturerExact 0, gate blocked. .gitignore now ignores parts/ wholesale (per-host build artifact; a committed manifest would reference uncommitted STLs). Browser-verified (preview_eval/DOM): init "OpenGeometry ready · head part: generated"; after generate, __headDebug = {usingPartMesh:true, source:'generated', vertsPerHead:1716, instances:936} (1716 verts >> ~150 for the old sphere, proving the STL geometry is in use), status "rendered 936 heads · 1010 pipe segments · heads: generated CAD part (best-effort, not mfg-exact)". /api/parts serves the on-disk manifest (22 generated/12 missing, manufacturerExactCount 0, gate blocked); /parts/head_pendent.stl served 200 (96,897 bytes). Full suite 34→ still 36 files / 329 tests green (HTML + build-script edits don't touch tests). HONESTY/fail-closed held: generated head mesh is best-effort, NOT manufacturer-exact; status + gate say so; AUTOSPRINK_PARITY stays blocked. SCOPE: only HEADS render as discrete parts (cadModel only emits head solids as placeable points) — fittings/valves/riser/FDC placement deferred to R6. Next: R6 (cad-model component placements) and/or AUTO-SOURCE S1–S5 (OpenClaw autonomous sourcing). NOTE: openscad portable lives at %TEMP%\\openscad-portable (not committed); real render on the VPS is R5.
- 2026-05-29: R1+R2 part-mesh pipeline + GET /api/parts shipped (ultra workflow we28777td, TDD, 36 files/329 tests green; +2 files/+9 tests). R1 src/components/part-mesh.js buildPartManifest({scadRunner,outDir,write}) walks registry COMPONENTS, runs T17 generateScadFor + renderScadToStl via an INJECTED scadRunner (openscad binary never spawned inside the module), writes parts/<key>.stl ONLY when a real STL renders (write+outDir+rendered.ok) — never fabricated; null generator / no runner / throw / empty → source:'missing', file:null. manufacturerExactCount hardcoded 0; parityGateStatus derived from buildParityInventory of present generated entries and STRUCTURALLY 'blocked' (required non-generatable parts identification_sign/riser_trim/fdc/gauge/fitting_cap/... return null in generateScadFor → can never be 'generated'). scripts/build-parts.mjs ("npm run build:parts") builds a real spawnSync openscad runner, degrades to all-'missing' (exit 0) when absent; writes parts/parts-manifest.json. .gitignore: parts/*.stl ignored, parts-manifest.json allowed. R2 GET /api/parts (authMiddleware, registered after /api/parity, before SPA catch-all) serves on-disk manifest else live buildPartManifest({}) all-missing; added a per-entry sanitizePartEntry that coerces manufacturerExact→false unless source∈{catalog,manufacturer} (closes the honesty verdict's tampered-manifest leak), derives manufacturerExactCount from sanitized entries, and hardcodes parityGateStatus 'blocked' (found/generated never clears parity). parts/<key>.stl reachable via existing repo-root express.static. HONESTY/fail-closed VERIFIED (3 adversarial agents: honesty PASS, test-truth PASS; the correctness 'fail' was a WRONG-CWD false negative — that agent globbed E:/ClaudeBot not C:/Users/dalla/OneDrive/Documents/HaloFire; I independently re-confirmed all 4 files exist, /api/parts at server.js:929, suite 36/329 green). Live preview verification: all-missing branch (34 missing, generated 0, manufacturerExactCount 0, gate blocked, openscad absent locally) + tamper test (generated entry falsely flagged manufacturerExact:true → coerced FALSE; catalog entry kept true; missing false; count derived=1; gate still blocked). openscad NOT installed locally → real STL render is a VPS/R5 step; pipeline is openscad-agnostic via injected runner. NOT AutoSprink/AutoCAD/manufacturer/AHJ/PE parity — generated meshes are best-effort, gate stays BLOCKED. Next: R3 studio render swap (instance real part meshes; needs real STLs — render on VPS or install openscad locally to verify), then AUTO-SOURCE S1–S5 (OpenClaw autonomous sourcing).
- 2026-05-29: Definition-of-Done adversarial verification (ultra workflow wqui7b5uv — 7 parallel DoD verifiers + a review-packet refresh, 8 agents). HONEST RESULT: 5/7 criteria met, 2 genuine gaps found. SATISFIED: d2 engineering layout (NFPA-13 spacing + cad-model.js schedule sizing + single-path hydraulics + balanceNetwork full balance + geometric compliance, all wired+tested, best-effort), d3 CAD export (DXF first-party+tested; STEP/IFC/STL via OpenGeometry WASM kernel, wired but runtime-unverified in repo), d5 studio UX (usable end-to-end, gates fail-closed+surfaced, 45 studio/gate tests green), d6 verifier+packet (scripts/verify-internal-alpha.ps1 runs full suite — independently re-ran: 34 files/320 tests green; packet refreshed), d7 deployable (docs/DEPLOY.md local run path verified end-to-end: seed exit 0, server boots, /, /workbench.html, /autosprink.html, /api/health all 200). NOT SATISFIED: d1 (SVG→3D live + structural solids kernel-backed, BUT floorPlanFromDxf/buildingFromDxf are engine-only/unwired in server.js, and the live viewer heads/drops are plain Three primitives not rendered parts) → added T21 (wire DXF) + the REAL-PART RENDER epic R1–R5 (render real part meshes); d4 (bare materials ARE priced from real ARGCO/Victaulic/FFF rows, BUT buildFullScopeBid is unwired — system components + soft costs are fallback constants/percentages — and no calibration to the real Home Depot $792,543.84 total) → added T22. Overclaim audit: CODE is conspicuously honest/fail-closed everywhere (every module disclaims AutoSprink/AHJ/PE/permit/manufacturer/fabrication; AUTOSPRINK_PARITY stays blocked from empty inventory; best_effort evidence rows are not gate-clearing); the only overclaim risks were in the DoD WORDING vs reality (d1 "DXF works"/"everything kernel-rendered", d4 "full scope...approaching real totals") — now annotated honestly in the Definition of Done. Stale prior review packets (claimed 17 files/131 tests, listed committed P1-P7/T8/T9 as "not done") superseded by docs/reviews/2026-05-29-autosprink-clone-review-current.md (320 tests, maps all 7 DoD criteria to verdicts, explicit "still NOT done" list incl. 0/34 real component models, public HTTPS pending). No code changed by this verification run. Honesty constraint upheld — "project complete" is NOT yet true; the verified remaining backlog is T21 (DXF wiring), T22 (full-scope bid), and the REAL-PART RENDER epic (the user's AutoCAD-3D-parts requirement).
- 2026-05-29: P7 studio compliance + full-hydraulics + submittal surface shipped (ultra workflow wn1mpkom8, TDD, 3 adversarial verifiers honesty/correctness/test-truth all PASS). API (src/api/server.js): extracted runSprinklerPipeline(req) so sprinkler-bid + a new submittal route share input-resolution/bid/scene/cadModel; imported balanceNetwork (../engine/hydraulic-network.js), checkCompliance (../engine/nfpa-compliance.js), buildSubmittal+renderSubmittalPdf (../engine/submittal.js). POST /api/projects/:name/sprinkler-bid response ADDS (back-compat preserved — bid/scene/cadModel/hydraulics/isBuilding unchanged): hydraulicNetwork = balanceNetwork({network: cadModel.rooms[0].network, hazard}) in try/catch -> {error} (CRITICAL: balanceNetwork resolves cadModel.network at TOP LEVEL which buildCadModel never produces — the network is at rooms[0].network on the floorPlan path; the buildingSvg path has no rooms[].network so the balance SKIPS gracefully with {error} rather than fabricating numbers), and compliance = checkCompliance({stories:[{spaces: bid.rooms.map(r=>({name,hazard,...r.layout}))}]}, hazard) in try/catch (real heads/spacing/bbox geometry). NEW POST /api/projects/:name/submittal (authMiddleware, async): runs the same pipeline, returns buildSubmittal({project:{name},bid,cadModel,hydraulics:(network balance if it ran else single-path),compliance}) as a Content-Disposition: attachment JSON download; optional body.pdf calls renderSubmittalPdf (no server invoker wired -> {skipped}, never throws). UI (autosprink.html): Hydraulics panel now shows the iterative network balance (network demand gpm, network friction, req @ source psi balanced) beneath the single-path estimate, each labelled best-effort/NOT-a-sealed-PE-calc; new Compliance (NFPA-13, geometric) panel lists per-rule PASS/WARN/FAIL + the always-present "Professional review required" honesty note; new "Download submittal (.json)" button (disabled until a layout generates) hitting /submittal via the existing blob-download pattern + a NOT-stamped/AHJ/PE/permit caption. HONESTY/fail-closed VERIFIED in tests + by 3 adversarial agents: buildSubmittal.submittalReady stays HARDCODED false; header.{stamped,ahjApproved,peReviewed,permitReady} all false; AUTOSPRINK_PARITY_INCOMPLETE stays blocked (buildParityInventory({}) empty); checkCompliance always appends the 'warn' NFPA13_REVIEW_NOTE (a geometric pass is NOT approval and clears NO gate); every surfaced result carries its module disclaimer; the only evidence write is the pre-existing best_effort row (not a gate-clearing type). New tests/p7-studio-surface.test.js (6 spawned-server tests, PORT 3196): compliance+numeric network balance on the floorPlan path, honest {error} skip on the building path, the NFPA13_REVIEW_NOTE warn finding, back-compat fields, /submittal honesty flags + attachment header, and legacy-SVG path never 500s / never submittalReady true. Browser-verified (preview_eval/DOM at 1500px; preview server restarted to load the new routes): Home Depot -> hydraulics single-path 270gpm/19.0psi + network 509gpm/48.4psi@source, compliance "2 fail(s)" (min-spacing FAIL, max-distance-to-wall FAIL, max-heads WARN) + honesty note, submittal button downloads halofire-submittal.json with status "submittal downloaded (best-effort package)". Direct API check: submittalReady false, all header flags false, AUTOSPRINK_PARITY blocked. Full suite 34 files / 320 tests green (was 33/314; +1 file / +6 tests). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — gates stay fail-closed BLOCKED. P7 was the last Task-Queue item; remaining = Definition-of-Done verification + review-packet refresh.
- 2026-05-29: T20 Settings UI + component-library browser shipped — new settings.html (no new endpoint; consumes the already-tested T19/P6 APIs + the statically-served registry module). Four panels: (1) AUTOSPRINK parity — GET /api/parity renders the 15-row matrix (12 functional rows 'present', the 3 regulated rows AHJ/PE/manufacturer-exact shown with a 'gated' badge + status 'gated'), with a banner reading "AUTOSPRINK PARITY GATE: BLOCKED · parityAchieved: false"; (2) Required Documentation — GET /api/settings/documents lists all 6 slots (catalogs/manufacturer_cut_sheets/ahj_approval/autosprink_reference/openscad_binary/pricebook_updates) with satisfied/missing, plus an admin add form (doc_type+link/upload) POSTing to /api/settings/documents; (3) Stack Dependencies — GET /api/settings/dependencies (openscad_installed/sam_gateway/autosprink_reference); (4) Component Library — dynamic import('/src/components/registry.js') + reg.buildParityInventory({}) (honest empty-status inventory) groups all 34 components across 15 categories, each shown with its real present/missing model status. Linked from workbench.html + autosprink.html headers ("Settings & Parity"). HONESTY/fail-closed: the page SURFACES parity/inventory but never flips a gate — the library inventory is derived from buildParityInventory({}) exactly like the server's /api/parity gate, so it honestly reports 0/34 with real models, all 34 'no model', gate BLOCKED, parityAchieved false; a catalog/cut-sheet link records evidence but does NOT clear a regulated gate. Browser-verified via preview_eval/DOM at 1500px against the running preview server (had to restart the stale 64-min preview process so /api/parity — added in P6 — was loaded; route order on disk confirmed correct, parity@787 before SPA catch-all@856): gate banner BLOCKED + parityAchieved false; 15 matrix rows incl. 3 gated; 6 doc slots; admin link add round-trip flips catalogs -> satisfied with no error; deps openscad_installed=false / sam_gateway=unknown / autosprink_reference=missing; library 34 components / 0 with models / 34 missing; both nav links present. DEFERRED (honesty, not overclaimed): "render catalog/generated models in the 3D layout" — zero real models exist yet (all 34 gated/missing), so there is nothing to render; lands when real models are attached. Full suite 33 files / 314 tests green (unchanged — settings.html is a static page over already-tested APIs, browser-verified like the other studio HTML surfaces). Backward compat preserved (new file + 2 one-line header link insertions; no JS/route change). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — AUTOSPRINK_PARITY gate stays fail-closed BLOCKED. Next: P7 parity + component-library + compliance UI (studio-side: surface compliance findings + hydraulic summary + submittal download in the workbench/studio, beyond the Settings overview T20 ships).
- 2026-05-29: P6 parity matrix module + status API + gate wiring shipped — new src/engine/parity-matrix.js + tests/parity-matrix.test.js (14 tests: 12 unit + 2 spawned-server). buildParityMatrix(state) -> { areas:[{key,name,status:'present'|'partial'|'missing'|'gated',evidence,gated}], requiredComplete:boolean, gated:[...], disclaimer } covering all 15 roadmap parity areas (drawing_import, building_modeling, head_layout, schedule_sizing, hydraulic_network, nfpa_compliance, supports, component_library, submittal, cad_export, bid_bom, evidence_settings + the three GATED rows ahj_approval, pe_review, manufacturer_exact). Non-gated areas derive present/missing from the corresponding state flag; the three GATED rows are NEVER auto-present — status stays 'gated' unless their state evidence is REAL (one of catalog/pe/ahj/manufacturer; generated/best-effort/AI does NOT qualify). PARITY_AREA_KEYS exports the frozen ordered key list. parityAchieved(matrix, inventory) is fail-closed: false unless requiredComplete (every non-gated area present) AND every gated row is cleared with real evidence; a generated-only inventory ({generatedOnly:true}) NEVER achieves parity. New GET /api/parity route (authMiddleware) in src/api/server.js returns { matrix, parityAchieved:false, gate:{code:'AUTOSPRINK_PARITY_INCOMPLETE',severity,status:'blocked',blockedClaims,reason}, disclaimer } — the gate is derived from buildParityInventory({}) (empty inventory -> no manufacturer-exact models -> parityGateStatus 'blocked'), so the AUTOSPRINK_PARITY gate stays fail-closed BLOCKED and the surface never asserts AHJ/PE/manufacturer evidence. Pure/deterministic, browser-free, no binaries. Tests: matrix lists every area with status+evidence; AutoSprink feature areas covered; AHJ/PE/manufacturer-exact gated (never auto-present); a missing functional area -> missing + requiredComplete false; parityAchieved false while a required area missing, false when gated rows lack evidence, false for generated-only inventory, false when gated rows carry only 'generated' evidence, true ONLY with catalog/pe/ahj real evidence on every gated row, re-blocks when one gated row loses evidence; PARITY_MATRIX_DISCLAIMER on the matrix; spawned-server /api/parity requires auth, lists areas, keeps gate 'blocked' + parityAchieved false. HONESTY/fail-closed: PARITY_MATRIX_DISCLAIMER labels this best-effort functional coverage only — NOT AutoSprink/AutoCAD parity, NOT AHJ-approved, NOT PE-reviewed, NOT permit-ready, NOT manufacturer-approved, NOT fabrication-ready; generated (OpenSCAD/SAM) models never satisfy the gated rows; clears NO claim gate and never flips AUTOSPRINK_PARITY. Full suite 33 files / 314 tests green (was 32/300). Backward compat preserved (new engine file + new test; server.js only gained imports + the /api/parity route). UI surfacing deferred to P7. Next: P7 parity + component-library + compliance UI (browser).
- 2026-05-29: P5 submittal package + PDF report shipped — new src/engine/submittal.js + tests/submittal.test.js (16 tests). buildSubmittal({project, bid, cadModel, hydraulics?, compliance?}) -> structured package { header, headSchedule, pipeSchedule, hydraulicSummary, billOfMaterials, gateStatus, bidSummary, disclaimer }. Pure/deterministic, browser-free, no binaries. header carries project/client/address/units + EXPLICIT honesty flags stamped:false, ahjApproved:false, peReviewed:false, permitReady:false (none asserted). headSchedule groups head solids by orientation -> [{orientation,count}] (counts sum to the cadModel head total). pipeSchedule groups pipe solids by nominal diameter -> [{diameterIn,count,totalLengthFt}] (3D segment length, ordered ascending). billOfMaterials is a BEST-EFFORT takeoff (head line per orientation; pipe footage + a rough fitting estimate per diameter — every line estimate:true, NOT a manufacturer-quoted material list). hydraulicSummary is a compact view of the supplied balanceNetwork() result (requiredSourcePsi/totalDemandGpm/friction/elevation/kFactor/hazard/balanced/source margin + note); NULL when hydraulics omitted (graceful — package still assembles). gateStatus: AUTOSPRINK_PARITY gate is FAIL-CLOSED (buildParityInventory({}) -> empty inventory -> parityGateStatus 'blocked', always in blockedGates), compliancePassed reflects the supplied compliance.passed (null when omitted; a failed compliance adds an NFPA13_COMPLIANCE_FAILED blocked gate), and submittalReady is ALWAYS false — a best-effort package can never satisfy a regulated submittal gate. buildPdfReportPayload(pkg) is a pure cadModel-style builder -> { tool:'generate_pdf_report', title:'HaloFire Submittal (best-effort) — <project>', sections:[Project, Head Schedule, Pipe Schedule, Hydraulic Summary, Bill of Materials, Gate Status, (Bid Summary)], disclaimer }. async renderSubmittalPdf(pkg, {invoker}) builds that payload and calls the INJECTED async invoker (supports both (toolName,payload) and (payload) signatures) — on success { ok:true, result }; absent/non-function/throwing invoker (or a build error) -> { ok:false, skipped:true, reason } WITHOUT throwing (graceful degradation; the OpenClaw generate_pdf_report gateway on GX10 is external, wired in production, mocked in tests). HONESTY/fail-closed: SUBMITTAL_DISCLAIMER on every package + the PDF payload labels this a best-effort internal alpha — explicitly NOT a stamped/sealed submittal, NOT AHJ-approved, NOT PE-reviewed, NOT permit-ready, NOT manufacturer-approved, NOT fabrication-ready, NOT AutoSprink/AutoCAD parity; clears NO claim gate and never reads as an approved submittal. Tests cover: all sections present + disclaimer + honesty-flag header; head schedule count == cadModel head total; pipe schedule grouped by diameter w/ lengths; hydraulic summary mirrors the balance result; BOM lists head + pipe items; gateStatus.autosprinkParity 'blocked' + blockedGates non-empty + submittalReady false; a failing compliance reflected in compliancePassed; graceful with omitted hydraulics/compliance (hydraulicSummary null, still blocked); renderSubmittalPdf ok with a 2-arg mock invoker (asserts generate_pdf_report toolName + title + sections + disclaimer), ok with a 1-arg invoker, and ok:false skipped:true (no throw) for no opts / empty opts / non-function invoker / throwing invoker. Full suite 32 files / 300 tests green (was 31/284). Backward compat preserved (new files only; no existing file touched). Next: P6 parity matrix module + status API + gate wiring.
- 2026-05-29: P4 generate component models -> populate parity inventory shipped — new src/components/model-pipeline.js + tests/model-pipeline.test.js (10 tests). generateLibrary(components=COMPONENTS, {scadRunner?}) runs the T17 generateScadFor() across the registry, KEEPS only components T17 can honestly model (heads, pipe, tee/elbow/coupling/reducer fittings, grooved, valves, hanger — fdc/backflow/riser/trim/gauge/drains/signs/inspector_test/seismic_brace/cross return null and are SKIPPED, no fake geometry), optionally renders each .scad via the injected scadRunner through generators.renderScadToStl (absent -> source-only, hasStl:false; a throwing runner fails closed to hasStl:false — never fabricates STL). Returns { results:[{key, source:'generated', hasScad:true, hasStl:boolean, scad}], modelStatusByKey:{ [key]:{ source:'generated', model:{format:'scad',source,stl,manufacturerExact:false} } } }. Feeding modelStatusByKey to registry.buildParityInventory raises FUNCTIONAL present-count (0 -> the modelable subset) but parityComplete STAYS false because required signs/trim/fdc/etc have no honest generator (still MISSING). Added gate-grade helpers isManufacturerExact(entry) (true ONLY for source 'catalog'|'manufacturer' WITH a real model — 'generated' deliberately excluded) and manufacturerParityComplete(modelStatusByKey) (true ONLY when EVERY required component has manufacturer-exact evidence). Test proves: even an all-'generated' map where buildParityInventory.parityComplete==true still yields manufacturerParityComplete()==false, so the AUTOSPRINK_PARITY / manufacturer / PE claim stays gated; it flips true only with catalog/manufacturer evidence on every required row, and knocking one required row back to 'generated' re-blocks it. No real binary call (injected runner or source-only). Pure/deterministic, browser-free. HONESTY/fail-closed: generated geometry is BEST-EFFORT massing, explicitly NOT manufacturer-exact, confers NO AutoSprink/AutoCAD/manufacturer parity and NO AHJ/PE approval; disclaimer rides in every .scad header. Full suite 31 files / 284 tests green (was 30/274). Backward compat preserved (new files only; registry.js + generators.js + model-resolver.js untouched). BOM model-ref wiring + parity matrix surfacing deferred to P6/P7. Next: P5 submittal package + PDF report.
- 2026-05-29: P3 hanger + seismic support placement shipped — new src/engine/supports.js + tests/supports.test.js (13 hand-counted tests). placeSupports(cadModel, opts?) walks the SAME pipe solids buildCadModel/buildRoomCad emit ({kind:'pipe', role, from:[x,y,z], to:[x,y,z], diameterIn}) and returns { hangers:[{position:[x,y,z],pipe,role:'hanger',diameterIn,spacingFt}], braces:[{position,pipe,role:'seismic_brace',braceType:'lateral',diameterIn,intervalFt}], counts:{hangers,braces}, note }. maxHangerSpacingFt(nominalIn): NFPA-13 public max hanger spacing by nominal size — <=1.25" -> 12 ft, >=1.5" -> 15 ft. Hangers placed on HORIZONTAL branch/main runs only (drops/risers/ties skipped): offsets = end hanger near each end (within ~1ft: 0 and len) + ceil(len/spacing)-1 interior nodes at the max spacing, deduped, clamped to >=1 per run; no consecutive gap exceeds the max spacing. SEISMIC_BRACE_INTERVAL_FT=40: a lateral sway brace every ~40 ft (floor(len/interval) nodes) along branch/main runs; opts.seismicIntervalFt overrides. Pure/deterministic, browser-free, no binaries — same cad-model in, identical supports out. Hand-checked cases: 30ft 2" branch -> 3 hangers at {0,15,30} (ceil(30/15)=2 + ends, deduped) & 0 braces (run < 40ft); 12ft 1" branch -> 2 end hangers at {0,12} (one 12ft span = max); tiny 0.5ft run -> >=1 hanger (clamp); empty model -> 0/0; vertical drop+riser -> 0 hangers; 100ft 4" main -> 2 lateral braces at {40,80}; injected interval 25 -> 4 braces at {25,50,75,100}; real 60x40 light buildRoomCad model -> deterministic (a==b), hangers>0 all on branch/cross-main pipes. Feeds the BOM/takeoff (hanger + seismic_brace components already in src/components/registry.js). HONESTY/fail-closed: SUPPORTS_NOTE export + note field on every result label this a BEST-EFFORT spacing estimate — NOT a seismic engineering design, NOT PE-reviewed, NOT AHJ-approved, NOT AutoSprink parity; clears NO claim gate. Full suite 30 files / 274 tests green (was 29/261). Backward compat preserved (new files only; no existing file touched). Studio render + cad-model wiring deferred to P7 (UI). Next: P4 generate component models -> populate parity inventory.
- 2026-05-29: P2 NFPA-13 compliance checker shipped — new src/engine/nfpa-compliance.js + tests/nfpa-compliance.test.js (11 tests). checkCompliance(layoutOrBuilding, hazard) -> { findings:[{code,severity:'pass'|'warn'|'fail',rule,detail}], passed:boolean, note }. Accepts EITHER a single layoutRoom output / hand-built layout ({heads,spacingX,spacingY,coveragePerHeadSqFt,bbox}) OR a building/system-layout ({stories:[{spaces:[{name,hazard,heads,...}]}]}) via collectSpaces (each space checked under its own hazard, falling back to the supplied default). Reuses HAZARD_RULES/getHazardRule from sprinkler-layout.js (light 225/ord 130/extra 100 sqft max area; light&ord 15ft / extra 12ft max spacing; 6ft min spacing all hazards; max-to-wall = half max spacing). Rules emitted as findings: NFPA13_MAX_COVERAGE_AREA (coveragePerHeadSqFt or spacingX*spacingY vs hazard max), NFPA13_MAX_SPACING (actual nearest-neighbour max gap from head positions vs hazard max), NFPA13_MIN_SPACING (closest head gap vs 6ft), NFPA13_MAX_DISTANCE_TO_WALL (worst head's nearest-wall distance from bbox vs half max spacing), NFPA13_MAX_HEADS_PER_SYSTEM (total heads vs ~100 light/ordinary guideline). passed=false iff ANY finding is severity 'fail'. HONESTY/fail-closed: NFPA_NOTE export + an always-present 'warn'-severity NFPA13_REVIEW_NOTE finding state passing these geometric checks is NOT AHJ approval / NOT PE review / NOT permit-ready / NOT AutoSprink parity and clears NO gate — geometric pass NEVER reads as approval. Pure/deterministic, browser-free, no binaries. Hand-checked vitest cases: 20x20 ordinary layoutRoom -> passed true & all rule findings 'pass'; over-spaced 20ft / over-area 400sqft layout -> NFPA13_MAX_SPACING + NFPA13_MAX_COVERAGE_AREA 'fail' & passed false; over-area-only 140>130 -> fail; 3ft min-spacing -> NFPA13_MIN_SPACING fail; center head 10ft from nearest wall (>7.5) -> NFPA13_MAX_DISTANCE_TO_WALL fail; 130 heads -> NFPA13_MAX_HEADS_PER_SYSTEM fail; building input with an over-spaced space -> fail; empty/no-heads -> no fails + note present; unknown hazard -> throws. Full suite 29 files / 261 tests green (was 28/250). Backward compat preserved (new files only; no existing file touched). NOT AHJ/PE/AutoSprink/manufacturer parity; AUTOSPRINK_PARITY gate stays fail-closed BLOCKED. Surface as a compliance panel + COMPLIANCE_REVIEW gate note deferred to P7 (UI). Next: P3 hanger + seismic support placement.
- 2026-05-29: P1 full hydraulic network balance shipped — new src/engine/hydraulic-network.js + tests/hydraulic-network.test.js (18 hand-computed tests), re-exported (kFactorFlow, balanceNetwork) from src/engine/hydraulics.js so the network balance imports from the same hydraulics entry point as the T2 single-path estimate (T2 requiredPressureAtRiser/flagSchedule API untouched — backward compat regression-tested). kFactorFlow(k,psi)=k*sqrt(psi) (Q=K√P): hand-verified 5.6√7=14.816 gpm, 5.6√20=25.044, 8√7=21.166; rejects k<=0, treats psi<=0 as 0 flow. balanceNetwork({network|cadModel,hazard,kFactor?(def 5.6),C?(def 120),sourcePsi?,minHeadPressurePsi?(def 7)}): resolves a raw network or a cadModel.network; picks the hydraulically-remote branch (longest run); orders its heads most-remote-first; seeds the most-remote head at the min operating pressure; walks INWARD head-by-head accumulating flow — each head discharges Q=K√P at its node pressure, the accumulated flow loses Hazen-Williams friction (existing hazenWilliamsLossPsiPerFt) over each inter-head segment so the next upstream node sits HIGHER by that loss (so upstream heads flow MORE); then carries the full accumulated branch demand through cross-main + main-tie + system-riser friction and adds 0.433 psi/ft of head elevation to compute requiredSourcePsi. Returns {heads:[{node,x,y,pressurePsi,flowGpm}], totalDemandGpm, requiredSourcePsi, frictionLossPsi, elevationPsi, segments:[{name,diameterIn,lengthFt,flowGpm,lossPsiPerFt,lossPsi}], balanced:boolean, kFactor, hazard, demandContextGpm(remoteAreaDemand), disclaimer}. Optional sourcePsi -> sourceMargin + balanced=sourcePsi>=requiredSourcePsi. Pure/deterministic, browser-free, no binaries. Hand-checked cases: single K=5.6 head at 7 psi -> 14.816 gpm & requiredSourcePsi==7 (no friction/elev); a 2-head 10ft 1.049" branch -> upstream head higher psi & higher flow, totalDemand==q1+q2 computed independently, requiredSourcePsi==p2; elevation case requiredSourcePsi==7+0.433*12; real cadModel (60x40 light) per-head node flows + segments. HONESTY/fail-closed: labelled a best-effort ITERATIVE estimate — NOT a sealed Hardy-Cross solve, NOT a stamped hydraulic calc, NOT PE-reviewed, NOT AHJ-approved, NOT AutoSprink parity; clears NO claim gate. Full suite 28 files / 250 tests green (was 27/232). Backward compat preserved (hydraulics + cad-model + all spawned-server suites unchanged). Studio Hydraulics panel surfacing best-effort/deferred. Next: P2 NFPA-13 compliance checker.
- 2026-05-29: T19 Settings + documentation upload/link API shipped — new tests/settings-documents.test.js (7 spawned-server tests) + routes in src/api/server.js + a settings_documents table mirrored into BOTH server.js initDatabase and seed.js (CREATE TABLE + defensive ensureColumn for mode/url/filename/notes/evidence_id/created_by). REQUIRED_DOC_SLOTS = [catalogs, manufacturer_cut_sheets, ahj_approval, autosprink_reference, openscad_binary, pricebook_updates]; a slot reads 'missing' (satisfied:false) until a link/upload row exists. GET /api/settings/documents (authMiddleware) lists every slot + status (latest row attached). POST /api/settings/documents (authMiddleware + requireRole('admin')) body {doc_type, mode:'link'|'upload', url?, filename?, notes}: validates doc_type against the slot list (400 unknown), mode link requires url / upload requires filename (400 otherwise), rejects unsupported fields (400). On success a single transaction inserts a PRESENT project_evidence row (project_name 'HaloFire Library', evidence_type=doc_type, source_ref=url|filename) so the link/upload can satisfy resolve-gate evidence, then the settings_documents row referencing it; returns {id, evidence_id, status:'satisfied'}. GET /api/settings/dependencies (authMiddleware) reports {openscad_installed:boolean (best-effort spawnSync 'openscad --version' detection, never throws), sam_gateway:'unknown' (GX10 sam3 via OpenClaw bridge, not probed here), autosprink_reference:'linked'|'missing' (from settings_documents)}. HONESTY/fail-closed: a catalog/cut-sheet link is recorded as evidence but NEVER auto-clears a regulated claim gate — AHJ/PE/AutoSprink parity gates still require their specific approved evidence via the T5 resolve route (test proves an AUTOSPRINK_EVIDENCE_MISSING gate stays 'blocked' after a catalog upload). Also added a vitest.config.js with fileParallelism:false (several suites spawn a real server on its own port; running them in parallel oversubscribed the machine and caused intermittent 401/429/timeout — serial spawned-server execution is deterministic, full suite ~16s) and raised the API/login rate-limit ceilings only when NODE_ENV=test (production limits 100/10 unchanged) so repeated logins in the spawned-server suites don't 429. Tests cover: 401 without auth, all 6 slots missing initially, non-admin POST -> 403, admin link -> 200 + slot satisfied + a present project_evidence row exists, unknown doc_type / link-without-url -> 400, catalog upload does NOT clear a blocking gate, dependencies returns an openscad_installed boolean + sam_gateway + autosprink_reference. Full suite 25 files / 226 tests green (was 24/219). Backward compat preserved (existing suites unchanged; pre-existing parallel flakiness in the other spawned-server suites also resolved by the config). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — uploading a doc is evidence only; the AUTOSPRINK_PARITY gate stays fail-closed BLOCKED until the inventory is genuinely complete with real evidence. Next: T20 Settings UI + component-library browser (browser).
- 2026-05-29: T18 SAM 3.1 reconstruction adapter shipped — new src/components/sam-reconstruct.js + tests/sam-reconstruct.test.js (10 tests). buildSamReconstructPayload(component, imageRef) returns a DETERMINISTIC plain-object request for the GX10 SAM 3.1 service: { service:'sam-3.1', op:'reconstruct', componentKey (from a component object's key or a bare string key), imageRef, outputFormat:'stl' } — component key + image ref + desired output mesh format always present, same inputs -> identical object. async reconstructComponentModel(component, imageRef, {invoker}) builds that payload and calls the INJECTED async invoker(payload) (production wires the OpenClaw bridge / hal_sam_status — SAM 3.1 is external on GX10, never called directly here). On success returns { ok:true, source:'generated_sam', model, label:'best-effort SAM 3.1 reconstruction — NOT manufacturer-exact' } (raw or {mesh,format} returns both accepted as the model). HONESTY/fail-closed: if the invoker is absent / not a function (no bridge wired) OR throws (GX10/gateway down) OR yields an empty mesh, returns { ok:false, skipped:true, reason } WITHOUT throwing — success is never fabricated, a missing model stays missing for the parity inventory. Browser-free, no process/binary spawned in engine code. Tests cover: payload shape (key + image ref + format present), determinism, bare-string component; reconstructComponentModel ok + best-effort label with a mock invoker, raw-string model accepted, and ok:false skipped:true (no throw) for absent invoker / omitted opts / non-function invoker / throwing invoker / empty model. Full suite 24 files / 219 tests green (was 23/209). Backward compat preserved (no existing file touched; new src/components/sam-reconstruct.js). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — SAM-reconstructed models are best-effort and the AUTOSPRINK_PARITY gate stays fail-closed BLOCKED until the inventory is genuinely complete with real evidence. Next: T19 settings + documentation upload/link API.
- 2026-05-29: T17 OpenSCAD parametric generators shipped — new src/components/openscad/generators.js + tests/openscad-generators.test.js (23 tests). Deterministic, browser-free string generators returning OpenSCAD (.scad) SOURCE for each component type: sprinklerHeadScad({type,k,thread}), pipeScad({nominalIn,lengthFt,schedule}), teeScad({runIn,branchIn}), elbowScad({nominalIn,deg}), couplingScad({nominalIn}), reducerScad({fromIn,toIn}), valveScad({type,nominalIn}), hangerScad({nominalIn}). Each emits a commented header naming the component + an explicit BEST-EFFORT / NOT manufacturer-exact disclaimer (no AutoSprink/AutoCAD/manufacturer parity, no AHJ/PE approval), standardizes on mm (IN_TO_MM/FT_TO_MM conversion), sets $fn for smooth round bodies, and surfaces its params into the source (K-factor, sizes, angle, schedule, etc.). Geometry is approximate primitive massing (cylinders/spheres/difference/taper) for visualization + take-off context ONLY. generateScadFor(component) dispatches by registry category (heads->sprinkler_head, pipe->pipe, fittings->tee/elbow/coupling/reducer by key or name, grooved->coupling massing, valves->valve, hanger->hanger) and FAILS CLOSED returning null for categories with no honest parametric model (signs, trim kits, gauges, etc.) or for null/empty input — never invents fake geometry. renderScadToStl(scad,{runner}) uses an INJECTED runner and NEVER spawns a process here (the openscad CLI is external, lives behind the caller's injected fn): no runner -> {ok:false,reason:'openscad_not_installed'}; runner returns {stl}|string -> {ok:true,stl}; runner throws or yields empty -> {ok:false,reason:'render_failed'} (success is never fabricated). Tests cover: every generator returns a non-empty string with the expected module keyword + best-effort/not-manufacturer-exact labels + $fn + param values; generateScadFor dispatches each category correctly and returns null for unsupported/null/empty; renderScadToStl ok with mock runner, ok:false openscad_not_installed with no runner / no opts, render_failed on throw, string-stl accepted. Full suite 23 files / 209 tests green (was 22/186). Backward compat preserved (no existing file touched; new dir src/components/openscad/). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — generated models are best-effort and AUTOSPRINK_PARITY gate stays fail-closed BLOCKED until the inventory is genuinely complete with real evidence. Next: T18 SAM 3.1 reconstruction adapter.
- 2026-05-29: T16 component 3D-model resolver pipeline shipped — new src/components/model-resolver.js + tests/model-resolver.test.js (11 tests). resolveComponentModel(component, sourcers) tries INJECTED async sourcers in strict priority catalog -> openscad -> sam -> placeholder. Each sourcer is `(component) => {format,data,...}|null`; returns null / a model with empty (no real `data`) / throws => skip to next. Returns { key, source:'catalog'|'generated_openscad'|'generated_sam'|'placeholder', status, model|null, label }: catalog/openscad/sam are status 'present' with provenance label (generated ones labelled best-effort, NOT manufacturer-exact); placeholder is status 'missing', model null — HONESTY/fail-closed, a placeholder is NEVER reported present. A throwing sourcer is caught and treated as null (try next). resolveLibrary(components, sourcers) -> { results:[...], modelStatusByKey } where modelStatusByKey maps each key to {source, model} in registry vocabulary (catalog->'catalog', generated_openscad/generated_sam->'generated', placeholder->'placeholder', model null) so it feeds registry.buildParityInventory directly — placeholders fail closed to MISSING, only genuine catalog/generated models count present. Pure logic + injected async, browser-free, no external binaries called in engine code. Tests cover: catalog wins; fall-through to openscad then sam; all-null -> placeholder/missing; no sourcers -> placeholder; throwing sourcer skipped; all-throwing -> placeholder; empty-data model treated as null; resolveLibrary aggregation + buildParityInventory present/missing counts. Full suite 22 files / 186 tests green (was 21/175). Backward compat preserved (no existing file touched). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — AUTOSPRINK_PARITY gate stays fail-closed BLOCKED until inventory is genuinely complete with real evidence. Next: T17 OpenSCAD parametric generators.
- 2026-05-29: T15 component registry + AutoSprink parity inventory shipped — new src/components/registry.js + tests/component-registry.test.js (22 tests). COMPONENTS is a deep-frozen canonical array (33 entries) of every fire-sprinkler module a layout/bid needs, each { key, category, name, params:{nominal sizes/options}, required:boolean }. Categories + members: heads (pendent/upright/sidewall/concealed/dry_pendent/esfr), pipe (sch10/sch40), fittings (tee/elbow_90/elbow_45/coupling/reducer/cap/cross), grooved (coupling/flange_adapter), valves (alarm_check/check/butterfly/osy_gate/prv/deluge), fdc, backflow_preventer, riser_assembly, riser_trim, gauge, hanger, seismic_brace, drain (main/aux), inspector_test, identification_sign. Exports getComponent(key) (O(1) Map lookup, undefined for unknown), componentsByCategory() (groups all into {[cat]:Component[]}), buildParityInventory(modelStatusByKey) -> {total,present,missing[],byCategory:{[cat]:{present,total,missing[]}},parityComplete}. HONESTY/fail-closed: a component counts 'present' ONLY when modelStatusByKey[key].source is a REAL source ('catalog'|'generated') AND carries an actual model — a 'placeholder'/'missing' status, or a real source with no model, counts MISSING. parityComplete is true only when EVERY required component has a real model. AUTOSPRINK_PARITY_GATE = frozen claim-gate {code:'AUTOSPRINK_PARITY_INCOMPLETE', severity:'blocking', status:'blocked', blockedClaims:['AutoSprink parity','complete component library','manufacturer-exact models'], reason:...}; parityGateStatus(inventory) returns 'blocked' unless parityComplete (then 'clear'). Pure/deterministic, browser-free; no models in this module (resolver T16 feeds status). Full suite 21 files / 175 tests green (was 20/153). Backward compat preserved (no existing file touched). NOT AutoSprink/AutoCAD/manufacturer parity, NOT AHJ/PE approved — gate stays fail-closed BLOCKED until the inventory is genuinely complete with real evidence. Next: T16 component 3D-model resolver pipeline.
- 2026-05-29: studio + OpenGeometry CAD + DXF shipped (commits 3764b64, 2b5b7ba). 68 tests green.
- 2026-05-29: T1 STEP/IFC/STL export shipped — verified 79KB STEP from 23 OpenGeometry entities. Next: T2 hydraulics.
- 2026-05-29: T2 hydraulic engine shipped — src/engine/hydraulics.js + tests/hydraulics.test.js (21 hand-computed tests). Full suite 13 files / 89 tests green (was 12/68). Single-path estimate only; NOT a full network balance, NOT PE/AHJ/AutoSprink parity. Next: T3 full bid scope.
- 2026-05-29: T3 full bid scope shipped — src/engine/bid-scope.js + tests/bid-scope.test.js (11 tests). buildSystemComponents (6 core riser-assembly components + conditional fire pump via boolean or required>available pressure), buildSoftCosts (permit 2% / design 6% / freight 3% labelled assumptions, priceSource:soft_cost_assumption), buildFullScopeBid (prices components via priceResolver with flagged fallback, computes fullScopeTotal alongside bareMaterialsTotal). Full suite 14 files / 100 tests green (was 13/89). Best-effort estimate only, fail-closed; NOT a complete priced bid, NOT manufacturer-quoted, NOT AHJ/PE/AutoSprink parity. Next: T4 DXF/PDF floor-plan import.
- 2026-05-29: T4 DXF floor-plan import shipped — floorPlanFromDxf in src/engine/floorplan-import.js + 8 new tests in tests/floorplan-import.test.js. Parses ENTITIES section: LWPOLYLINE, POLYLINE/VERTEX, and best-effort closed-loop assembly from LINE segments; opts.layer filter, opts.unitsPerDrawingUnit scale-to-feet, opts.hazard default; reuses normalizeFloorPlan; skips degenerate <3-vertex shapes. generateSprinklerBid verified to run on imported plan. Full suite 14 files / 108 tests green (was 14/100). PDF import DEFERRED (not a deterministic dependency-free parse). Best-effort geometry only, NOT CAD-grade, claim gates stay fail-closed. Next: T5 resolve-gate workflow.
- 2026-05-29: T7 OpenClaw CAD automation adapter shipped — src/cad/openclaw-cad.js + tests/openclaw-cad.test.js (13 tests). buildGenerate3dModelPayload / buildGenerateDxfPayload are PURE deterministic cad-model -> plain-object args (project name, units, counts, compact solids spec: walls->boxes, pipes->sized cylinders from/to, heads->oriented points); both carry the cad model's fail-closed disclaimer. invokeOpenClawCad(toolName, payload, {invoker}) calls the INJECTED async invoker (no hardcoded network) and returns {ok:true,result} on success; if invoker is absent/not-a-function OR throws (gateway unreachable) returns {ok:false,skipped:true,reason} WITHOUT throwing — graceful degradation so HaloFire works whether or not the GX10 OpenClaw gateway is live. Full suite 16 files / 126 tests green (was 15/113). Geometry only — clears NO claim gate, makes NO AHJ/PE/AutoSprink/manufacturer parity claim; fail-closed preserved. Production wires the bridge as the invoker later. Next: T6 multi-floor / multi-zone.
- 2026-05-29: T6 multi-floor / multi-zone engine shipped — buildCadModel in src/engine/cad-model.js now accepts an optional floorPlan.floors array; offsetSolidZ/offsetRoomCadZ/buildFloorCad lift each floor's whole network (slabs/walls/pipes/heads + network elevations) in Z by baseElevationFt so floors stack, each floor keeps its own independent riser->cross-main->branch->drop->head network and NFPA schedule sizing, counts aggregate across floors, model.floors[] exposes per-floor solids/counts/sizing. Walls now carry baseZ (default 0); dxf-export.js honors baseZ when extruding wall posts (backward compatible). Legacy floorPlan.rooms still builds a single floor at base 0 — output unchanged. New tests in tests/cad-model-multifloor.test.js: 2-floor 60x40 plan yields heads in two elevation bands (some Z<14, some Z>14), 24 total heads = 12+12, per-floor independent sizing/risers, and a legacy-plan regression guard. Full suite 17 files / 131 tests green (was 16/126). Engine only — NFPA schedule sizing, NOT hydraulically balanced, NOT AHJ/PE/AutoSprink parity; claim gates stay fail-closed. Studio surfacing deferred/best-effort. Next: T8 head perf + true-scale toggle.
- 2026-05-29: T10 deploy path + review packet refresh shipped — new docs/reviews/2026-05-29-autosprink-clone-review.md (truthful: OpenGeometry CAD render, STEP/IFC/STL+DXF export, NFPA-13 schedule sizing, single-path hydraulic estimate, full-scope bid estimate, SVG+DXF import, evidence-gated resolve-gate API, OpenClaw CAD adapter — each cited to a file; exact count 17 files / 131 tests; explicit "still NOT done" = full network hydraulic balance, AutoSprink/AutoCAD parity, AHJ/PE/manufacturer approvals, studio wiring of hydraulics/multi-floor, perf instancing, surface consolidation). New docs/DEPLOY.md (env vars, npm install → seed → node src/api/server.js → /, workbench, /autosprink.html; one-command verifier). scripts/verify-internal-alpha.ps1 now runs the FULL `npx vitest run` (no file list) so it can't drift; seed + agentic-rules steps kept. Docs+script only — NO behavior change. Full suite 17 files / 131 tests green (unchanged). Fail-closed preserved; no parity claimed. Next: T8 head perf + true-scale toggle.
- 2026-05-29: T8 head perf + true-scale toggle shipped — autosprink.html now renders 900+ heads/drops via Three InstancedMesh (heads=sphere instances, drops=scaled-cylinder instances) while walls/mains/branches/riser stay OpenGeometry-kernel; render ~9s→~4s for Home Depot. New View toggle "Exaggerate pipe radius (×6)" (off=true NFPA scale) re-renders from stored currentCadModel. exportCad refactored to buildExportScene() — rebuilds the COMPLETE kernel model (walls+all pipes+all heads, true-scale) on-demand at export so STEP/IFC/STL stay complete: verified Home Depot STEP 6,805,478 bytes from 1950 entities. Full suite 17/131 green (autosprink.html not unit-tested; browser-verified via preview screenshots + true-scale toggle + STEP export). Next: T9 consolidate surfaces.
- 2026-05-29: T11 building model + multi-space drawing parser shipped — new src/engine/building-model.js (normalizeBuilding: defaults hazard 'ordinary'/thicknessFt 0.5/wall type 'interior'/ceilingHeightFt 14/baseElevationFt 0, drops degenerate spaces <3 verts + zero-length walls, clamps opening offset/width within the wall span; buildingFromFloorPlan converts legacy {rooms} or {floors} -> stories with one space per room + perimeter exterior walls, no interior walls/openings/columns). Extended src/engine/floorplan-import.js with buildingFromDxf (LWPOLYLINE on ROOMS/SPACES -> spaces; LINE/LWPOLYLINE on WALLS/WALLS-EXT/WALLS-INT -> walls typed by layer; LINE on DOOR/WINDOW -> openings attached to nearest wall; CIRCLE/POINT on COLUMN -> columns; opts.layers remaps layer names; opts.unitsPerDrawingUnit scale) and buildingFromSvg (data-space polygons, data-wall lines/polylines w/ data-wall-type, data-opening lines attached to nearest wall, data-column circles; opts.unitsPerPx scale; added hasAttr() for valueless boolean data-* attrs). Layer/attribute-convention based, best-effort, deterministic — NOT CAD object-recognition AI; claim gates stay fail-closed. New tests/building-model.test.js (8 tests: normalize defaults+cleaning+clamping, legacy {rooms}/{floors} conversion, multi-space SVG 2 rooms + interior wall + door + column, multi-space DXF same). Full suite 18 files / 139 tests green (was 17/131). Backward compat preserved (existing floorplan-import tests unchanged). Next: T9 consolidate surfaces / T12 full per-space sprinkler system.
- 2026-05-29: T5 resolve-gate API shipped — POST /api/projects/:name/claim-gates/:code/resolve (authMiddleware + requireRole('admin')) in src/api/server.js + tests/resolve-gate.test.js (5 spawned-server tests). Added resolved_by/resolved_at/resolved_evidence_ref columns via ensureColumn in BOTH server.js initDatabase and seed.js. Approved gate-clearing evidence set: ahj_approval, professional_review, pe_signoff, manufacturer_approval, autosprink_packet, employee_signoff (status treated as 'present'). REJECTS (400) evidence_type best_effort_ai_layout and status best_effort — AI/best-effort output can NEVER clear a regulated gate. On success: inserts the evidence row (present) + flips gate blocked->cleared with who/what/when provenance; GET claim-gates reflects cleared + resolved_by. Full suite 15 files / 113 tests green (was 14/108). Studio UI DEFERRED; parity (AHJ/PE/AutoSprink/manufacturer) still requires real artifacts — fail-closed preserved. Next: T6 multi-floor / multi-zone.
- 2026-05-29: T13 accurate building CAD model shipped — buildCadModel in src/engine/cad-model.js now also consumes a normalized building (from building-model.js), detected by isBuilding() (stories[] each with spaces/walls) so legacy {rooms}/{floors} plans keep their existing paths. For a building: buildBuildingCad calls layoutBuilding() (system-layout.js, per-space column-aware heads + NFPA sizePipe) then per story emits a wall solid per segment (kind 'wall', reusing the a/b/center/lengthFt/heightFt/thicknessFt/rotationY/baseZ shape, carrying type:'exterior'|'interior' + layer WALLS/WALLS-INT + openings:[{offsetFt,widthFt,heightFt,sill,type}] metadata so the viewer can cut them), a column solid per column (kind 'column' {x,y,sizeFt,heightFt,baseZ} layer COLUMNS), and per-space pipe networks (branch/drop/cross-main/riser-tie/system-riser) + pendent heads; multi-story stacked via offsetSolidZ by baseElevationFt (offsetSolidZ now also offsets column baseZ). counts = {spaces,walls,interiorWalls,columns,openings,heads,pipes}. dxf-export.js updated: walls draw on WALLS (exterior/legacy) vs WALLS-INT (interior), columns extruded on COLUMNS, opening sill/head centerlines on OPENINGS — additive, existing single-floor DXF unchanged (wall block defaults to WALLS when no type). New tests/building-cad.test.js (9 tests: 2-space building w/ 1 interior wall + 1 door + 1 column -> >=1 interior-wall solid, >=1 column solid, opening recorded on a wall, heads in both spaces, counts populated, determinism; multi-story stacks by baseElevationFt; legacy floorPlan regression guard byte-identical w/ no column solids; DXF emits WALLS/WALLS-INT/COLUMNS/OPENINGS layers). Full suite 20 files / 153 tests green (was 19/144). Backward compat preserved (cad-model/multifloor/openclaw-cad tests unchanged). Geometry only — NFPA-13 schedule sizing, best-effort layer/convention parsing NOT CAD object-recognition AI, NO hydraulic calc, NOT AutoSprink/AutoCAD/AHJ/PE/manufacturer parity; claim gates stay fail-closed. Next: T14 studio render accurate building + full system (browser).
- 2026-05-29: T12 full per-space sprinkler system shipped — new src/engine/system-layout.js (layoutBuilding(building) consumes normalizeBuilding output). For EACH story and EACH space it lays out heads independently via the existing layoutRoom() on that space's own polygon (heads fill + are clipped to every space), then COLUMN-AWARE: any head within (sizeFt/2 + 0.5ft clearance) of a story column is nudged to a deterministic ring of nearby in-polygon candidates (+x,-x,+y,-y,diagonals) or dropped if none clear. Building-wide network reuses the riser->cross-main->branch->drop->head topology + NFPA sizePipe from cad-model.js: per-space cross-main sized for space head count, per-row branch lines sized for their head count, plus a per-story FEED MAIN sized for the story head count. Returns {stories:[{level, spaces:[{name,headCount,heads,branchLines,sizing}], storyHeadCount, feedMain}], totalHeadCount, pipeSchedule (diameter->linear ft, sorted), coverageOk (true iff every non-degenerate space has >=1 head), perSpace[]}. New tests/system-layout.test.js (5 hand-checked tests: 2-space 30x20 ordinary building -> both spaces get 6 heads each, totals sum, coverageOk true; heads clipped inside their own polygon; column on a baseline head grid point removes/shifts >=1 head vs no-column baseline; cross-main/branch/feed-main diameters match sizePipe for known counts; every non-degenerate space covered). Full suite 19 files / 144 tests green (was 18/139). Backward compat preserved (no existing file touched; engine consumes building-model output). Pure Node, deterministic, NOT CAD-recognition AI, NO hydraulic calc, NOT AutoSprink/AHJ/PE/manufacturer parity — claim gates stay fail-closed. Next: T13 accurate building CAD model.
- 2026-05-29: T9 consolidate surfaces shipped — retired the 725-line legacy static fake-data app.html (now a redirect to /workbench.html; fake $revenue/dashboard arrays gone); SPA catch-all in src/api/server.js now serves index.html (landing) for unknown routes, not the old dashboard. All UI flows index->login->workbench->studio. tests/consolidate-surfaces.test.js (4 spawned-server tests: landing at /, /app.html redirects + no fake content, unknown route->landing, workbench+studio still served). Full suite 26 files / 230 tests green; agentic rules ok. Routing/content verified by the spawned-server HTTP test (authoritative for served bytes). Next: T14 studio render accurate multi-space building (openings via OG boolean).
- 2026-05-29: T14 (partial) accurate-building studio render shipped — server.js sprinkler-bid accepts buildingSvg -> buildCadModel(building) (isBuilding:true); autosprink.html adds a 'Building drawing (multi-space SVG)' source, renders walls by type (interior/exterior color), columns, full per-space system + per-space breakdown panel, and cuts door/window openings via OpenGeometry Cuboid.subtract(Opening) (rotation-aligned, try/catch fallback to solid). tests/building-studio-api.test.js (2 tests). Full suite 27 files/232 tests green; agentic rules ok. Live render executed (status 'rendered 8 heads · 20 pipe segments'; panel '2 spaces · 1 int walls · 2 cols · 1 openings'; canvas 840x852; no console errors). MARKED [~] not [x]: pixel screenshot of the door-cut blocked by a preview-tooling hang this session — re-confirm visually next run. Next: re-verify T14 screenshot, then T20 Settings UI + component-library browser.
- 2026-05-29: T14 COMPLETED (was [~]) — fixed the door-cut bug: OpenGeometry Cuboid.subtract requires an OPERAND ARRAY and returns a NEW cut mesh, not in-place mutation. cutWall() now calls wallCuboid.subtract([openings], {}) and renderModel adds the RETURNED mesh (prior single-arg subtract(opening) threw 'requires an operand array' + was swallowed by try/catch -> walls rendered solid, no holes). PROOF via window.__wallDebug on a live 2-space building render: 4 exterior walls = 36 verts (solid box) each, interior wall with 1 door opening = 84 verts (hole cut). Status 'rendered 10 heads · 22 pipe segments'. Full suite 27 files/232 tests green; agentic rules ok. Screenshot tool hung (WebGL rAF capture) so verified by geometry vertex counts. Next: T20 Settings UI + component-library browser (BROWSER).
