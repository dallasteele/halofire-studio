# Cooperative 1881 supplier-lead identity — iteration 1

The predecessor loop reached its iteration budget after the materialized Kier
shared-drive windows at offsets 300, 600, and 900 found no project-token path
candidate. The successor keeps the local hash-bound corpus separate and adds a
narrow recovery path for generically named shared-drive supplier packages.

Only a structural supplier/truss/lumber-named shared file may enter this lead
queue. A lead becomes Cooperative 1881 evidence only if its path or its
bounded cover/index text contains `1881` or `cooperative`. Unmatched arbitrary
files remain excluded; a generic supplier package whose text omits the project
stays an explicit unverified lead and cannot reach member, clearance, routing,
fabrication, code, employee, or VPS gates.

The live replay at cursor 900 completed with the real local roots (577 files)
and the Kier shared window (259 files). It yielded 32 project-identified local
documents, zero shared candidates, zero unverified supplier leads, and no
materialized structural supplier source. The shared cursor advanced to 1200.
The clearance preflight remains blocked with
`ROOF_FRAMING_OBSTRUCTION_GEOMETRY_UNRESOLVED`; automatic routing and per-head
obstruction clearance are false.

An Egnyte `scandir` failure is now a typed
`SOURCE_CORPUS_DIRECTORY_UNREADABLE` artifact issue with the same cursor for a
safe retry rather than an uncaught Node process error.

Focused source/roof/coordination regression passes 77 JavaScript tests with no
skips. Structural registration passes 25 Python tests with no skips; the Vite
production build, loop guard, and agentic rulebook check pass. The GX10 formal
prover health endpoint at `http://127.0.0.1:8810/api/provers/health` refused
the connection, so no formal proof claim is made for this source-discovery
slice.
